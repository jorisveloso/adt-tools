# adt-query

Ferramenta de consultas SAP via `adt-client` — **esqueleto**.

Placeholder/placeholder de ferramenta consumidora sobre o motor `adt-client`, no monorepo
`adt-tools`. `index.mjs` expõe apenas `PACOTE` e `descricao` — ainda não há consultas
implementadas.

## Estado

Esqueleto. Sem lógica além do esqueleto de pacote; aguarda definição do escopo de "consultas SAP".
