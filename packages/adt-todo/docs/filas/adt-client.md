# Fila adt-client

- [x] 1. Canal WebGUI (SAP GUI for HTML) dirigido por CDP — provado no SXD 816/100 em 03/09/2026: Chrome headless falando CDP cru (WebSocket nativo do Node, sem playwright/puppeteer) abre /sap/bc/gui/sap/its/webgui autenticado por Network.setExtraHTTPHeaders (basic), renderiza a dynpro, le o innerText e tira print PNG por Page.captureScreenshot. Vale como CANAL NOVO da lib, porque resolve o buraco do GUI Scripting: nao exige SAP GUI instalado nem sessao de dialogo visivel, entao roda sem ninguem na frente da tela. Prototipo em sap-accelerate/work/POC_4029823_j1b1n/scripts/webgui.mjs (abrirChrome/ir/avaliar/preencher/clicar/clicarBotao/tecla/lerTela/print/carimbo/esperarMudanca). Decidir se vira webgui.mjs na lib + receita-webgui.md, e qual o lugar dele na ordem ADT -> SOAP RFC -> classrun -> BDC -> WebGUI -> GUI Scripting.
> medido: O canal foi usado PONTA A PONTA em 03/09/2026 (SXD 816/100), o que e o insumo que faltava para a decisao: abriu a J1B1N preenchida por URL de transacao, leu os campos, preencheu duas datas obrigatorias e ACIONOU o Gravar, criando a NF 0000000082 — confirmada em outra LUW. Sem dependencia nova (CDP cru; o Playwright serviu so de instrumento). Duas correcoes no prototipo scripts/webgui.mjs, que devem entrar na receita: (1) o polyfill de crypto.randomUUID por Page.addScriptToEvaluateOnNewDocument, sem o qual a pagina fica inerte; (2) a espera de tela pronta NAO pode exigir document.title — medido que a tela chamada por ~transaction=*TCODE ...;DYNP_OKCODE=ONLI monta inteira (3063 elementos com ct, 10 inputs visiveis em 3 s) com o titulo VAZIO, e exigir titulo faz a espera estourar o teto de 60 s e ler uma tela pronta como se fosse zero campo. O sinal certo e contagem de controles + campos de entrada visiveis. Limite conhecido do canal: nao ha via de SAIDA (item 13). Evidencia: sap-accelerate/work/POC_4029823_j1b1n/medicoes/tu-item7-webgui.md.
> SIM: virou canal da lib. packages/adt-client/webgui.mjs (export adt-client/webgui) + webgui.test.mjs (7 testes puros, suite inteira 694/694) + docs/receita-webgui.md; lugar fixado na ordem ADT -> SOAP RFC -> classrun -> BDC -> WebGUI -> GUI Scripting (README, gui.mjs e receita-gui-scripting.md alinhados; o GUI Scripting deixa de ser o unico que ve a tela e passa a ser o que o WebGUI nao alcanca: a SAIDA e o ALV/table control como objeto). O porte trocou o .env cravado pelo cfg da lib, unificou clicar/preencher num alvo unico ({id}|{seletor}|{okcode}) e foi PROVADO no s4h 758/250 em 04/09/2026, so leitura: SE16 aberta em 9 s, campos e botoes lidos, T000 preenchida, btn[0] acionado e a tela virou "Data Browser: tabela T000: tela de selecao". Dois achados do porte, medidos e ja na receita: (1) cookie SAP_SESSIONID com secure sobre HTTP puro e DESCARTADO pelo Chrome (o fetch do Node nao impoe a politica e parece 200 feliz) -> 400 Session not found em sessao ANONIMA; o conserto e --unsafely-treat-insecure-origin-as-secure + --test-type (origemSegura, ligada por default), medido dos dois lados: isSecureContext true/false, cookie no jar sim/nao, [ct] 201/0; (2) o piso de campos visiveis da espera de tela pronta era 3 (herdado da J1B1N) e a 1a tela da SE16 tem UM input — abertura caiu de 64 s para 9 s com o piso em 0. Aberto: sonda do canal no probe (item 14) e o par SXD+bandeira (item 15, bloqueado sem VPN); a saida segue sem via (item 13).

- [x] 2. abrirSapGui nao abre sessao no SXD — medido em 03/09/2026 na maquina do Joris (SAP GUI 8.00, sapshcut 8000.1.4.10): com -pw a linha de comando SAI COM EXIT 0 e nenhuma sessao sobe (nem janela, nem ROT), tanto com -system=SXD (o systemid do SAPUILandscape.xml, conferido) quanto com -conn=/H/172.31.28.129/S/3200; rede OK (3200 e 8000 acessiveis). SEM -pw a janela SAP GUI abre normalmente na tela de logon. Ou seja: o que quebra e o logon automatico, nao o sapshcut. Levantar a causa (senha na linha de comando? politica do GUI 8.00? SNC?) e corrigir/documentar abrirSapGui, que hoje so estoura nenhuma sessao no ROT em 30000 ms sem dizer isso.
> Causa parcialmente levantada e abrirSapGui corrigido. DESMENTIDO o -pw: medido no S4H (sem VPN — logo a causa é LOCAL, não do SXD) que usuário existente não abre sessão com ou sem -pw, com senha certa ou errada, enquanto usuário inexistente abre a tela de logon. Achado que explica o sintoma relatado: sessoesAbertas() custava 120 s sem sessão (120157/120186/120142 ms em três chamadas) e ENGOLIA o timeout, devolvendo {sessoes:[],erro:null} — por isso o laço de 500 ms fazia UMA amostra e reportava um prazo falso. Agora abrirSapGui amostra por tasklist (processosSapGui/interpretarTasklist), só paga o ROT quando há janela de usuário, e falha em ~2 s com a trilha do processo; rodarVbs sinaliza `expirou` e sessoesAbertas({ timeout }) reporta erro em vez de mentir. Doc: packages/adt-client/docs/receita-gui-scripting.md § Quando o sapshcut não abre sessão. Causa-raiz do encerramento segue aberta no item 16.

- [x] 3. GUI Scripting so enxerga sessao JA LOGADA — medido em 03/09/2026: com a janela SAP GUI aberta na TELA DE LOGON, sessoesAbertas() devolve { sessoes: [], erro: null } (o GetObject(SAPGUI) funciona, o ROT so nao tem a sessao). O vazio sem erro e indistinguivel de GUI fechado e manda a investigacao para o lado errado (RZ11, registro). Documentar em receita-gui-scripting.md e, se der, distinguir os dois casos em sessoesAbertas (ha janela SAPgui? ha processo?).
> DESMENTIDA a premissa do item: com a janela parada na TELA DE LOGON o GetObject("SAPGUI") tambem NAO responde — expira igual ao GUI fechado (medido 04/09/2026, S4H: 15669 ms com o GUI fechado vs 15663 ms com a janela no logon). Aquele { sessoes: [], erro: null } de 03/09 era o timeout ENGOLIDO, nao um engine que respondeu. O prazo nao distingue nada; quem distingue e o tasklist. Entregue: diagnosticarRot (puro, exportado, testado) classifica com-sessao / logon-pendente / sem-janela / gui-fechado / erro-scripting, e sessoesAbertas passa a devolver { sessoes, estado, erro, processos } — vazio nunca mais sai mudo. logon-pendente reproduzido de verdade com sapshcut -user=<inexistente> (janelas abertas na medicao foram fechadas). Doc: docs/receita-gui-scripting.md § O ROT so existe depois do LOGON. Suite: 696 testes verdes. Achado derivado virou o item 17 (mojibake do tasklist).

- [x] 4. WebGUI nomeia os botoes da toolbar com o mesmo btn[n] do SAP GUI — medido no SXD 816/100 em 03/09/2026 na SE38: os ids sao M0:48::btn[8] (title " (F8)"), M0:55::btn[15] (Encerrar Shift+F3), M0:56::btn[3] (Voltar F3), M0:49::btn[71] (Procurar), M0:49::btn[86] (Imprimir); o prefixo M0:nn muda por tela, o sufixo ::btn[n] nao. A tela tambem tem um input ToolbarOkCode (title "Inserir codigo de transacao", invisivel) — a via de OK-code. Isso da um enderecamento ESTAVEL para dirigir o WebGUI (clicarBotao por sufixo de id). Mapear e virar parte da receita do canal WebGUI.
> Mapeado e virou parte do canal. O endereco estavel e o SUFIXO: o prefixo M0:nn muda por tela (medido no SXD 816/100 em 03/09/2026 — na mesma sessao da SE38 os botoes sairam em M0:48, M0:49, M0:55 e M0:56, e no Writer da J1B1N em M0:50) e o ::btn[n] NAO muda; o n e o mesmo SID do SAP GUI (wnd[0]/tbar[0]/btn[n]) que aparece cru no POST do ITS. Entregue em packages/adt-client: (1) OKCODES exportado — btn[0] Enter, btn[3] Voltar F3, btn[8] Executar F8, btn[11] Gravar Ctrl+S, btn[12] Cancelar Escape, btn[15] Encerrar Shift+F3, btn[71] Procurar, btn[86] Imprimir, cada linha com a medicao que a pos la (sistema + data); e APELIDO, nao whitelist — o resto da convencao do SAP GUI ficou de fora de proposito, para o mapa nao virar palpite com cara de medicao; (2) okcodeDe (puro) aceita btn[11], 11 e o apelido medido (sem caixa e sem acento), deixa passar btn[n] fora do mapa e estoura COM A LISTA no apelido inventado — errar o nome nao vira mais "nao esta na tela" 20 s depois; jsDoAlvo e nomeDoAlvo passam por ele, entao acionar(s, "Gravar") funciona; (3) botoes() devolve nome/tecla anotados (nome: null fora do mapa). Doc: receita-webgui.md § O btn[n] e o endereco ESTAVEL da barra (tabela com a medicao de cada linha) + README. Suite 700/700 verde (4 testes puros novos). Sem medicao nova neste item: tudo veio do que ja estava medido no SXD e no s4h. O ToolbarOkCode (invisivel, rect 0x0) segue sem via — item 13.

- [x] 5. Clique sintetico por CDP nao aciona o Unified Renderer do WebGUI — medido no SXD 816/100 em 03/09/2026: Input.dispatchMouseEvent no centro do M0:48::btn[8] da SE38 (com mouseMoved antes e buttons:1 no press) chega nas coordenadas certas e a tela NAO troca (carimbo titulo+contagem+texto identico depois de 30 s); teclado tambem nao (F8 por dispatchKeyEvent). A pagina tem 2 iframes — a hipotese e que o alvo real do evento esteja em outro frame (o dispatch vai para o frame principal). Investigar: descobrir o frame do conteudo (Page.getFrameTree / Target por frame) e dispatchar nele, ou achar a API JS do UR que envia o okcode. Sem isso o canal WebGUI so LE a tela, nao a dirige.
> medido: o clique CHEGA e mesmo assim nao dispara — no SXD 816/100 em 03/09/2026, na tela de selecao da transacao YJBV4029823: (a) o mesmo clique por CDP FOCA o campo de entrada e o insertText grava 47 nele, entao mouse e teclado alcancam a pagina; (b) duas causas de clique no vazio ja corrigidas no prototipo: o scrollIntoView e assincrono e o rect lido no mesmo tick vem ANTIGO (rect y=873, clique enviado para y=452), e o container M0:nn::btn[n] engloba texto oculto (Executar + Destacado) — o alvo com o rect do botao e o -cnt; (c) com o alvo certo, elementFromPoint no ponto devolve o proprio botao (nao ha overlay: o unico iframe, ls-blindlayer-0, esta em y=-10000); (d) mesmo assim NENHUMA via move a tela e nenhuma requisicao de rede sai: clique CDP, F8, Alt+E (accesskey=E do botao), Enter, .click() do JS e eventos sinteticos pointer+mouse+click. O botao e do renderer lightspeed: lsevents={"Press":[{},{"1":"action/3",...}]}, classes lsButton, e DOMDebugger.getEventListeners mostra ZERO listener no botao, no form e no document — os listeners estao no WINDOW (click, message x4, load, beforeunload, unload, pagehide, error) e os globais sao its_frame_child/its_frame_root/its_frame_mgr. Proxima hipotese: a acao trafega por postMessage entre os frames do ITS, e o dispatch precisa ir para o frame/janela certos. Proximo passo decidido com o Joris: tentar pelo MCP do Playwright (configurado em 03/09/2026, escopo local do projeto sap-accelerate: npx -y @playwright/mcp@latest --browser chrome) antes de continuar no CDP cru.
> confirmado em segunda tela: no Writer da J1B1N (mesma sessao, tela "Criar Nota Fiscal Saida (NF-e)") os botoes sao M0:50::btn[11] Gravar e M0:50::btn[12] Cancelar, com o mesmo lsevents Press action/3. O clique no -cnt do btn[12] acertou o alvo — elementFromPoint no ponto devolveu o proprio M0:50::btn[12] e coberto=false — e a tela NAO mudou. Confirma que o problema nao e mira nem overlay nem tela especifica: e o acionamento do renderer lightspeed por evento sintetico. Sem isso o canal WebGUI LE e PREENCHE, mas nao aciona.
> CAUSA MEDIDA em 03/09/2026 (SXD 816/100, Chrome headless + Playwright): o clique nao era o problema — a PAGINA estava morta. O WebGUI e servido por HTTP puro (ICM 8000) e crypto.randomUUID SO EXISTE EM CONTEXTO SEGURO (https/localhost); o boot do ITS quebra em cascata (crypto.randomUUID is not a function -> leitura de testAPCCapability em undefined -> b.GetOption is not a function nos onUpdate de page e cookie), nenhum controle ganha listener (DOMDebugger.getEventListeners no btn[12]: NENHUM; o document so escuta teclado, nunca mousedown/mouseup/click) e a tela fica pintada e inerte. Prova: com um polyfill de crypto.randomUUID injetado ANTES do JS da pagina (context.addInitScript / CDP Page.addScriptToEvaluateOnNewDocument) os erros zeram e o clique passa a POSTar (200 + delta-update de 4,5 MB aplicado na tela). Contraprova: sem o polyfill, 0 POST; fase ociosa de 12 s tambem 0 POST, entao o POST e do clique. Nao era frame errado (a pagina tem 1 iframe, ls-blindlayer, about:blank) nem gesto mal feito (o clique do Playwright, com hit-test e actionability, falhava igual).

- [x] 6. ITS aceita transacao com parametros de tela na URL? — a explorar: ~transaction=*TCODE CAMPO=valor;DYNP_OKCODE=xxx (a sintaxe classica de SAP shortcut / skip first screen). Se funcionar, o canal WebGUI chega direto na tela que interessa sem clicar em nada, o que contorna o achado do clique sintetico. Medir no SXD 816/100 com *SA38 RS38M-PROGRAMMA=<report>;DYNP_OKCODE=ONLI e com *SE38 ...;DYNP_OKCODE=STRT, e conferir tambem o TSTCP de transacao de parametro (/*<called> CAMPO=valor;) como via equivalente.
> medido: a via de transacao FUNCIONA e ja resolve metade do problema: no SXD 816/100 em 03/09/2026, criada a transacao YJBV4029823 (report YJBV_POC_R_4029823_J1B1N, $TMP) por deployTransaction da lib — TSTC dypno=1000 cinfo=80, TSTCC webgui=1, TADIR $TMP, confirmada em outra LUW por readTransaction. Chamando ~transaction=YJBV4029823 no WebGUI a tela de SELECAO do report abre direto, sem passar pela SE38 e sem clicar em nada (print em work/POC_4029823_j1b1n/medicoes/webgui-tran-selecao.png). Falta medir a outra metade: a sintaxe *TCODE CAMPO=valor;DYNP_OKCODE=xxx, que pularia tambem a tela de selecao — e que, se funcionar, contorna por inteiro o achado do item 5.
> APROVADO: a sintaxe *TCODE CAMPO=valor FUNCIONA no ITS — medido no SXD 816/100 em 03/09/2026: ~transaction=*YJBV4029823 P_DOCNUM=47 executa o report e abre a J1B1N PREENCHIDA, sem clicar em nada (print em sap-accelerate/work/POC_4029823_j1b1n/medicoes/webgui-skip-a.png). Tres variantes medidas, todas chegaram na mesma tela: "P_DOCNUM=47", "P_DOCNUM=0000000047;" e "P_DOCNUM=47;DYNP_OKCODE=ONLI" — ou seja o valor entra com ou sem zeros a esquerda, o ; final e opcional e o DYNP_OKCODE nao e necessario (a transacao de report ja executa). Isso contorna o item 5 para a tela de SELECAO, mas NAO para as telas seguintes: no Writer o acionamento continua sem via.
> FECHADO com as duas metades que faltavam, medidas no s4h 758/250 em 04/09/2026 (o SXD segue sem VPN, item 15) — e as duas viraram REGRA na receita, porque as duas falham CALADAS. (1) NOME DO CAMPO E O SID DA DYNPRO, nao o da doc de shortcut: *SE38 RS38M-PROGRAMMA=RSPARAM (o nome classico, com A) NAO preenche nada — o ITS ignora o par sem erro e sem aviso; o campo real e RS38M-PROGRAMM, sem o A, e com ele entra. Pior: com DYNP_OKCODE junto o fcode dispara mesmo assim, com a dynpro vazia, e volta "O programa  não existe" — mensagem que parece do sistema e e o parametro errado. Quem sabe o nome certo e a propria tela (lsdata["21"].SID = wnd[0]/usr/ctxtRS38M-PROGRAMM, snippet rodado como esta) e, de segunda fonte independente, a TSTCP standard: SA38PARAMETER grava /*SA38 RS38M-PROGRAMM=PFCG_TIME_DEPENDENCY;. Virou o item 18 (sids(sessao) na lib). (2) DYNP_OKCODE E O FCODE DAQUELA DYNPRO, nao um "executar" generico — e fcode errado nao e ignorado: ele SUBSTITUI o Enter implicito e a tela fica parada e preenchida. Oito expressoes medidas: *SE16 DATABROWSE-TABLENAME=T000 (sem okcode) -> tela de selecao da T000, 34 campos; a MESMA com ;DYNP_OKCODE=ONLI ou =XXXX -> 1a tela; *SE38/*SA38 RS38M-PROGRAMM=RSPARAM (sem okcode) -> 1a tela preenchida; com ;DYNP_OKCODE=SHOW ou =ONLI -> nada; com ;DYNP_OKCODE=STRT -> EXECUTOU nos dois ("Exibir parametro de perfil SAP"). Regra pratica: transacao de report -> ONLI (era o caso da J1B1N no SXD, por isso funcionou la); 1a tela da SE38/SA38 -> STRT; SE16 -> nenhum, o default ja avanca. (3) A TRANSACAO DE PARAMETRO (TSTCP /*<chamada> CAMPO=valor;) e a MESMA via, medida com standards, sem criar objeto: SE16T000 chegou na tela de selecao da T000 (identico a URL) e SA38PARAMETER parou na 1a tela preenchida — porque o TSTCP nao traz DYNP_OKCODE. A URL ganha por nao precisar de objeto; o TSTCP ganha quando a tela tem de existir para uma pessoa (deployTransaction type parameter). Doc: docs/receita-webgui.md §§ "Entrar na tela ja preenchida" (reescrita, com a tabela das oito expressoes e o snippet do SID) e "A transacao de PARAMETRO e a via equivalente". Achados derivados: itens 18 (sids) e 19 (a espera estoura 60 s quando a acao executa report: 64184 e 62920 ms contra 2,4-8,2 s das que so preenchem).

- [x] 7. Falar o protocolo do ITS por HTTP PURO, sem browser — a explorar, e possivelmente o achado mais valioso da leva. Medido no SXD 816/100 em 03/09/2026: a pagina do WebGUI tem um form webguiform0 com action apontando para /sap(<token de sessao base64>)/bc/gui/sap/its/webgui/ e campos InputField=<valor digitado> e moin=<token que muda a cada tela>. Se o par (token da URL, moin) for o estado inteiro da conversa, da para POSTar a tela sem Chrome nenhum — o que elimina de uma vez o browser, o CDP e o achado do clique sintetico (item 5), e transforma o canal WebGUI em mais um canal HTTP como o ADT e o SOAP RFC. Medir: capturar as requisicoes que o WebGUI faz ao trocar de tela (Network do CDP), reproduzir uma delas com fetch e ver se a tela avanca. Se pegar, e o caminho definitivo; se nao, fica documentado por que nao.
> medido: 03/09/2026: o corpo do POST do ITS foi capturado e e JSON simples com os SIDs DO SAP GUI. Exemplo real do clique no botao Sair: [{"post":"action/304/wnd[0]/usr/tabsTABSTRIP1/tabpTAB1/ssubHEADER_TAB:SAPLJ1BB2:2100/tblSAPLJ1BB2ITEM_CONTROL/ctxtJ_1BDYLIN-ITMTYP[1,1]","content":"position=0","logic":"ignore"},{"post":"focus/<SID>","logic":"ignore"},{"post":"action/3/wnd[0]/tbar[0]/btn[15]"},{"get":"state/ur"}] — verbos post/get, action/<n>/<SID>, focus/<SID>, value/<SID> com content, e state/ur no fim. A resposta e <updates><delta-update> com <start-script> CDATA. O SID e o MESMO do GUI Scripting (wnd[0]/tbar[0]/btn[15]), o que torna o canal enderecavel por id estavel em vez dos ids volateis do DOM.
> PEGOU — o ITS fala HTTP PURO e o canal WebGUI roda SEM NAVEGADOR. Medido no s4h 758/250 em 04/09/2026: (1) GET /sap/bc/gui/sap/its/webgui cria a sessao e devolve so o SHELL (ScreenId=screenarea0, cuatitle vazio) — dele saem o token no action do form webguiform0, o var moin e o cookie SAP_SESSIONID no set-cookie; (2) o PRIMEIRO POST em {action}batch/json?~RG_WEBGUI=X& monta a dynpro (e e o que o crypto.randomUUID quebrava no navegador) — acao mandada nele e PERDIDA, medido: action/3/tbar[1]/btn[8] no 1o POST devolveu a tela de selecao e o MESMO comando no 2o devolveu a lista; (3) os POSTs seguintes dirigem a tela. Verbos medidos: get state/ur (fecha todo batch), focus/<SID> logic ignore, value/<SID> com content (ESCREVE), action/3/<SID> (ACIONA); action/0/wnd[0] NAO existe (X-Code -101 not supported) e action/304 position=N e dispensavel. PROVA sem navegador nenhum: SE16 na T000, txtMAX_SEL de 200 para 2 + btn[8] devolveu "Data Browser: Tabela T000 2 acertos" contra os 5 acertos da mesma sessao sem tocar no campo — o valor chegou ao ABAP e mudou o resultado. Custo ~0,95 s (GET 341 + boot 423 + acao 190 ms) contra 9 s da via do Chrome. Duas formas de resposta, ambas HTTP 200: XML <updates><delta-update> quando houve update (o estado sai dos sap.its.aParams: cuatitle, ScreenId, dynpro, d-num, moin novo) e multipart/mixed --SAP_RESTGUI_BATCH_STEP com X-Order/X-Code/X-Status quando NAO — ou seja este canal falha FALANDO, ao contrario do clique por CDP. Contra-provas: sem o cookie de sessao 400 em 48 ms (Service nicht erreichbar, HTML alemao) = obrigatorio; sem o header moin resposta IDENTICA = dispensavel; SAP-PASSPORT/sap-statistics/X-Requested-With nunca enviados e tudo funcionou. Equivalencia com o navegador confirmada num caso de erro: preencher txtI1-LOW=250 (que e MTEXT, nao MANDT) e executar da a MESMA tela de selecao com "Nao foi encontrada nenhuma entrada em tabela" nos dois canais. Doc: packages/adt-client/docs/receita-webgui.md § O protocolo do ITS por HTTP puro. Commit 0f5ed4b.

- [x] 8. Navegacao por OK-code no WebGUI (ToolbarOkCode) — a explorar. Medido no SXD 816/100 em 03/09/2026: toda tela do WebGUI tem um input id=ToolbarOkCode (title "Inserir codigo de transacao", type text, rect 0x0, display flex — presente e invisivel). E a caixa de comando do SAP GUI: se der para escrever nela e submeter, sai de graca /nXXXX, /o, /nend e — o que interessa aqui — os codigos de funcao da tela (ex.: /8 para F8). Como o acionamento por clique nao funciona (item 5), medir se o OK-code tem via propria de submit (Enter no campo, ou o mesmo caminho HTTP do item do form). Seria o jeito mais SAP de dirigir o canal, e o mais estavel entre telas.
> PEGOU — a navegacao por OK-code funciona por HTTP PURO, e quem responde e a propria tela. Medido no s4h 758/250 em 04/09/2026, sem navegador nenhum: o lsevents do campo okcd diz "Enter":[{},{"1":"vkey/0/ses[0]","2":true}] — ou seja o disparo e {"post":"vkey/0/ses[0]"}, NAO um action. Sete candidatos medidos, sessao nova em cada: value/okcd sozinho NAO navega (fica no SAP Easy Access, o que prova que o disparo e o vkey), action/3/.../okcd volta -101 not supported, e value+vkey/0 chega na SE16. O minimo sao DOIS comandos: value/wnd[0]/tbar[0]/okcd + vkey/0/ses[0] (focus e okcode/ses[0] sao dispensaveis). Navegacao ponta a ponta numa sessao, sem tocar em botao nenhum: menu -> /nSE16 -> T000 + vkey/0 (que e tambem o Enter da dynpro) -> ONLI (EXECUTOU: "Data Browser: Tabela T000 5 acertos", bate com o item 7) -> /nSE38 (saiu de DENTRO da lista ALV) -> /n (volta ao menu), 85-150 ms por salto. Vocabulario medido: /nXXXX de qualquer tela, /n, fcode da dynpro (ONLI), /8 valendo como tecla F8, /o e /oXXXX abrindo POPUP wnd[1] (lista de modos — nao janela nova), /nend abrindo popup Pergunta Sim/Nao, e /nex ENCERRANDO a sessao (200 text/html "Adeus, o logoff foi efetuado"; POST seguinte 400 Session Timed Out). Isso resolve tambem a SAIDA do item 13. Doc: packages/adt-client/docs/receita-webgui.md § A caixa de comando (OK-code) — a navegacao generica do canal. Evidencia: sap-accelerate/work/POC_webgui_okcode/medicoes/okcode-http.md (cliente its.mjs + fases B/C/D).

- [x] 9. Ler a tela do WebGUI de forma ESTRUTURADA pelos atributos lsdata/lsevents — a explorar. Medido no SXD 816/100 em 03/09/2026: cada controle carrega lsdata (JSON com rotulo, estado e flags — ex. {"0":"Executar","2":"EMPHASIZED","4":" (F8)","17":"E"}) e lsevents (os eventos que ele publica — ex. {"Press":[{},{"1":"action/3"}]}), alem de ct (tipo do controle: B botao, CBS campo com busca, PAGE, PL, T...), ti, role e data-sap-ls-accesskey. Isso e um modelo de tela legivel, nao pixel: da para extrair campos, botoes, estado de habilitacao e ate a tecla de atalho de cada um, sem heuristica de DOM. Mapear o vocabulario (o que e cada indice numerico do lsdata, por tipo de controle) e virar a funcao lerTela do canal — hoje ela e um filtro de DOM improvisado.
> Vocabulario mapeado por medicao (s4h 758/250, 04/09/2026, 7 telas, 49 ct distintos, 37 com lsdata) e lerTela reescrito na lib: devolve { titulo, janela, mensagem, statusbar, campos, radios, checkboxes, botoes, grids, okcode }, cada peca com o SID e o nome do campo da URL. Tres achados: (1) o indice do SID MUDA por ct (PL 1, MB 11, G 12, radio 13, checkbox 14, label 19, campo 21, botao 27, grid 34) e falha calado — por isso o SID e procurado pelo VALOR que o carrega, nao por posicao; (2) o lsdata e o estado do SERVIDOR, nao o da tela: clicar no checkbox nao mexeu um byte dele, quem virou foi o aria-checked, entao marcacao sai do ARIA; (3) a mensagem vem tipada (constante ERROR no indice 1; o messageType de dentro do SID vem TRADUZIDO). Codigo em packages/adt-client/webgui.mjs (sidDoLsdata, campoDoSid, teclaDoBotao, rotuloLimpo, interpretarControle, montarTela, lerTela), teste com os lsdata REAIS da medicao (707 testes do pacote passam), receita em docs/receita-webgui.md § O vocabulario lsdata. Medicao: sap-accelerate/work/POC_webgui_lsdata/medicoes/item9-lsdata.md. Ficou fora e virou item: lsevents, ALV por STCS, menu.

- [x] 10. Playwright como driver do canal WebGUI — a medir assim que a sessao reiniciar. MCP configurado em 03/09/2026 no escopo local do projeto sap-accelerate (npx -y @playwright/mcp@latest --browser chrome, conectado e saudavel), mas as ferramentas so entram na sessao no restart. A pergunta a responder: o Playwright aciona o renderer lightspeed onde o CDP cru nao aciona (item 5)? Ele nao usa Input.dispatchMouseEvent cru — tem snapshot de acessibilidade, espera automatica e clique por ref. Se acionar, decidir o que a lib adota: Playwright como dependencia (peso, browsers baixados, CI) ou o achado de COMO ele aciona, reimplementado no CDP cru sem dependencia. Se nem o Playwright acionar, a hipotese do postMessage entre frames do ITS vira a principal.
> medido: 03/09/2026: o Playwright NAO e a resposta — ele tambem nao acionava (mesmo comportamento do CDP cru). O que destrava e o polyfill de crypto.randomUUID (item 5). Ou seja: o canal continua podendo ser CDP cru, sem dependencia nova; o Playwright serviu como instrumento de medicao (getEventListeners por CDPSession, actionability, captura de postData). ⚠ httpCredentials do Playwright NAO autentica no ICF (o SAP devolve a PAGINA de logon, nao 401): o Basic tem de ir em extraHTTPHeaders.
> Playwright NAO e a resposta e NAO vira dependencia: o clique dele falha igual ao CDP cru; quem destrava e o polyfill de crypto.randomUUID (item 5). A lib fica em CDP cru, zero dependencia nova (package.json sem playwright). O que ele vale — instrumento de bancada (postData por pagina.on(request), erros de boot por pageerror, getEventListeners, actionability) e os equivalentes addInitScript/extraHTTPHeaders (nunca httpCredentials, que nao autentica no ICF) — virou secao na receita: adt-tools/packages/adt-client/docs/receita-webgui.md, § Playwright — instrumento de bancada, nunca dependencia.

- [x] 11. Pre-requisito do canal WebGUI: o no ICF precisa estar ativo — e isso muda por sistema. Medido no SXD 816/100 em 03/09/2026: GET /sap/bc/gui/sap/its/webgui?sap-client=100 devolveu 200 com 36 KB de HTML (ativo), no mesmo ICM da porta 8000 que o ADT ja usa, com a MESMA credencial basic — nao ha setup adicional. Mas num sistema de cliente o no costuma estar desativado na SICF, e o canal simplesmente nao existe la. O canal precisa de uma sonda barata que responda "da para usar WebGUI neste sistema?" antes de subir Chrome nenhum — e a sonda deve distinguir no desativado de credencial errada de ICM fora do ar. Medir o que cada caso devolve (status, corpo, header) e virar a funcao de disponibilidade do canal, no espirito do verificarScriptingNoServidor do GUI Scripting.
> sondarWebgui + interpretarSonda em webgui.mjs (adt-tools/packages/adt-client), 4 casos medidos no s4h 758/250 em 04/09/2026 — achado central: credencial errada devolve 200 com a PAGINA DE LOGON, nao 401, e quem prova o canal e o cookie SAP_SESSIONID; ausente e desativado sao o MESMO 404. Doc em docs/receita-webgui.md secao sondarWebgui; 4 testes puros novos (710 passando). Aberto -> itens 27 e 28.

- [x] 12. Transacao Z + WebGUI: qualquer report ou dynpro vira tela chamavel por URL — receita a escrever. Provado ponta a ponta no SXD 816/100 em 03/09/2026: deployTransaction da lib criou YJBV4029823 para o report YJBV_POC_R_4029823_J1B1N em $TMP (TSTC dypno 1000 cinfo 80, TSTCC webgui=1 pelo default gui.html, TADIR $TMP, confirmado em outra LUW por readTransaction) e ~transaction=*YJBV4029823 P_DOCNUM=47 abriu a tela final direto. O par tran.mjs + WebGUI resolve um problema que nenhum outro canal resolve: rodar e VER um objeto de DIALOGO (dynpro, Writer, tela de selecao) sem GUI e sem ninguem na frente da tela — o classrun dumpa com DYNPRO_SEND_IN_BACKGROUND. Escrever a receita com o ciclo inteiro, inclusive o deleteTransaction no fim (a transacao e objeto, nao pode ficar de lixo no sistema do cliente).
> Receita escrita: adt-client/docs/receita-tran.md § O par tran + WebGUI — ver e DIRIGIR um objeto de dialogo sem GUI. Traz o ciclo inteiro (deployTransaction -> abrirNavegador/abrirTransacao -> lerTela/preencher/acionar -> assert em outra LUW -> deleteTransaction no finally), por que nenhum outro canal serve (classrun dumpa DYNPRO_SEND_IN_BACKGROUND, BDC e cego, GUI Scripting exige sessao logada e visivel), cinco regras medidas (report fixa DYPNO 1000/CINFO 80 e o fcode e ONLI; nome do parametro e o SID da tela; S_WEBGUI vem do default gui.html; nao ha via de saida — a saida e fechar; apagar e parte da receita) e o que o par nao resolve (nao ha tipo OO, no ICF inativo, ambiente com request). Ponteiro cruzado em receita-webgui.md § E se o objeto NAO tem transacao. Dois achados viraram itens: 29 (contra-prova do S_WEBGUI, nunca medida) e 30 (a YJBV4029823 pode ter ficado no SXD).

- [x] 13. Enviar OK-code arbitrario no WebGUI — a peca que falta para dirigir a tela. Medido no SXD 816/100 em 03/09/2026: (1) o campo ToolbarOkCode existe em toda tela (lsdata SID wnd[0]/tbar[0]/okcd) mas e INVISIVEL (rect 0x0) — click/fill do Playwright recusam por actionability, e focus() por JS + digitacao nativa nao poe texto nele (o valor foi parar no campo da tabela que tinha o cursor); (2) nenhum botao da barra devolve o fcode EXIT: btn[12] (Cancelar) e btn[15] (Sair) POSTam e voltam 200, mas o programa reabre a mesma dynpro. Vias a medir: montar o POST do protocolo do item 7 com {"post":"value/wnd[0]/tbar[0]/okcd","content":"EXIT"} + {"post":"action/0/wnd[0]"} (fetch de dentro da pagina, reaproveitando a sessao); ou achar como o WebGUI expoe a caixa de comando (o botao de expandir da barra). Sem isso o canal preenche, le e clica, mas nao manda comando.
> pista: O OK-CODE E ACEITO POR HTTP PURO — medido no s4h 758/250 em 04/09/2026 (item 7): {"post":"value/wnd[0]/tbar[0]/okcd","content":"ONLI"} volta X-Code: 0 / X-Status: OK no multipart. Ou seja o obstaculo do NAVEGADOR (o campo ToolbarOkCode e invisivel, rect 0x0, e recusa digitacao) NAO existe na via HTTP: la nao ha DOM nem actionability, so o SID. O que falta e o comando que DISPARA o okcode — action/0/wnd[0] nao e (X-Code -101 failed to fire action: not supported). Proximo passo: capturar pelo CDP o batch que o navegador manda ao apertar Enter num campo e ver qual action/<n> ele usa; com o disparo, a SAIDA (fcode EXIT/BACK) e o item 8 (navegacao por OK-code) caem juntos pela via HTTP.
> medido: A SAIDA CAIU pela via HTTP pura (item 8, s4h 758/250, 04/09/2026): o que faltava era o DISPARO, e ele e {"post":"vkey/0/ses[0]"} — o Enter que o proprio campo okcd publica no lsevents, nao um action. Com value/wnd[0]/tbar[0]/okcd + vkey/0: /nex encerra a sessao (200 text/html "Adeus", POST seguinte 400 Session Timed Out), /n volta ao menu e /nSE38 sai de DENTRO de uma lista ALV. Ou seja o obstaculo era do NAVEGADOR (campo invisivel, rect 0x0), nao do canal. O que resta deste item e so a via do navegador: se ainda vale insistir nela agora que a HTTP resolve, e o que fazer com btn[15]/Shift+F3 la.
> FECHADO — a via do NAVEGADOR EXISTE e vale, medida no s4h 758/250 em 04/09/2026. O obstaculo nunca foi o campo, foi o GESTO: o okcd e 0x0 (click/fill recusam por actionability e a digitacao nativa cai no campo com o cursor), entao o que funciona e escrever o value POR JS e despachar o Enter NO PROPRIO elemento (dispatchEvent nao passa por actionability). Nao e gambiarra: o lsevents do campo declara "Enter":[{},{"1":"vkey/0/ses[0]","2":true}] e o fonte do renderer (lsgui/js/webgui_min.js) mostra o handler do GuiOKCodeField chamando submitOkCode(texto), que monta [{post:okcode/ses[0],content},{post:vkey/0/ses[0]},{get:state/ur}] — o MESMO batch da via HTTP do item 8, gravado no navegador por hook no XMLHttpRequest.send. Tres candidatos medidos em sessao nova cada: value+Enter PEGOU (/nSE16 -> Data Browser), sap.its.enqueueEvent com o evento montado PEGOU (/nSE38 -> Editor ABAP) e oOkCd.setText nao existe; contra-prova: so o value, sem Enter, nao navega (0 POST). sap.g4h.$, onde mora o submitOkCode, NAO esta exposto. Navegacao pela funcao da lib numa sessao: /nSE16 -> /3 -> /nSE38 -> /n, 1,55-1,82 s por salto (contra 85-150 ms da via HTTP). A pergunta do btn[15]/Shift+F3 morre junto: a saida deste canal e o OK-code (/3, /n, /nex) — o /15 no menu abre a pergunta de logoff, que e popup e cai no item 23. Entregue: jsComando + comandar em packages/adt-client/webgui.mjs (teste puro; suite 711/711), nota do btn[15] no mapa OKCODES corrigida, receita § "A caixa de comando (OK-code) pelo navegador" + § "O que este canal NAO faz" item 1 reescrito. Medicao: sap-accelerate/work/POC_webgui_okcode_browser/medicoes/okcode-browser.md. Limite medido que virou o item 31: o OK-code NAO leva o que foi digitado na dynpro.

- [x] 14. Sonda do canal WebGUI no probe e no registro de canais. Hoje probe(cfg) mede ADT, SOAP RFC e classrun, e o canais.json tem tres colunas — o WebGUI nao aparece, mas depende de DUAS condicoes que o probe ja poderia medir de graca no mesmo GET: (1) o no SICF /sap/bc/gui/sap/its/webgui ativo, e (2) se o set-cookie do SAP_SESSIONID vem com secure, que e o que decide se o Chrome precisa da bandeira --unsafely-treat-insecure-origin-as-secure (medido: no s4h 758/250 vem secure, e sem a bandeira a tela e 400 Session not found; no SXD 816/100 nao vem). Uma sonda so-leitura resolve as duas e entra na tabelaMarkdown do landscape.
> probe(cfg) sonda o WebGUI em paralelo (sondarWebgui): resumo traz webgui { ok, causa, cookieSeguro }; canais.json grava canais.webgui + cookieSeguro; tabelaMarkdown ganha coluna WebGUI (✅ 🔒 / ✅ / ❌ com motivo / — para medição anterior). Medido 2026-09-04: s4h 758/250 ✅ 🔒 (cookie secure), SXD 816/100 ✅ sem secure — o contraste previsto. Testes 715/715. Commit em adt-tools.

- [ ] 15. Re-medir o canal WebGUI no SXD 816/100 com origemSegura ligada. O SXD provou o canal em 03/09/2026 SEM a bandeira de origem segura (la o cookie de sessao nao vem secure); a bandeira nasceu no porte para a lib, medida no s4h em 04/09/2026, e hoje e o DEFAULT do abrirNavegador — ou seja, o par SXD + bandeira nao foi medido. Conferir se --unsafely-treat-insecure-origin-as-secure + --test-type nao muda o comportamento do Unified Renderer la e, de quebra, se com crypto.randomUUID NATIVO o btn[15] continua sem devolver fcode de saida (item 13).
> bloqueado: sem VPN para o SXD

- [x] 16. Por que o SAPgui.exe encerra em ~0,5 s depois de um logon que o servidor aceitaria — a causa-raiz que o item 2 deixou aberta. JÁ MEDIDO E DESCARTADO (04/09/2026, S4H 758/250, SAP GUI 8.00 + sapshcut 8000.1.4.10): o -pw NÃO é a causa (sem -pw dá o mesmo), ordem dos argumentos, -maxgui, -command=SESSION_MANAGER, -type=Transaction, credencial (ADT discovery 200), USR02 USTYP=A / UFLAG=0 / GLTGB=99991231, registro SAPShortcut\Security ausente, UserScripting=1, sessão de desktop ativa (qwinsta console/1/Ativo), nenhum crash no Event Viewer, e o trace do SAP GUI (Trace\Enable=1) não gera arquivo. O divisor medido, alternado para descartar sequência: usuário que EXISTE -> processo morre sem janela de usuário; usuário INEXISTENTE -> tela de logon fica de pé. Próxima técnica: capturar o tráfego do dispatcher (3200) ou instrumentar o SAPgui.exe, já que o trace oficial não sai. Enquanto isso o WebGUI cobre a necessidade sem depender do GUI local.
> MEDIDO 04/09/2026 (S4H 758/250, SAP GUI 8.00): o SAPgui.exe do atalho e so LANCADOR — entrega o atalho ao saplogon.exe (inicia um se nao houver) e sai com exit 0 em 0,6-1,8 s, sem janela e com zero conexoes ao dispatcher (proxy local). A sessao nasce DENTRO do saplogon.exe (janela classe SAP_FRONTEND_SESSION, no ROT ~3-6 s depois); com -pw o logon PASSA (sem chave SAPShortcut/Security). O abrirSapGui antigo vigiava so SAPgui.exe e tomava a saida do lancador por falha. Corrigido: processosSapGui inclui saplogon.exe, janelasSapGui/lerJanelas por EnumWindows, diagnosticarRot com pede-senha/mensagem e tela de logon no ROT (usuario vazio, S000). E2E: abrirSapGui devolve a sessao logada em 10,6 s. Receita: § A sessao vive no saplogon.exe.

- [x] 17. interpretarTasklist devolve titulo com MOJIBAKE — medido 04/09/2026: o tasklist /FO CSV sai na codepage OEM do console e o titulo chega "Entrada do nome do usu?rio" (visto no processos[] de sessoesAbertas e nas mensagens de diagnostico do ROT). Nao quebra a logica (a regra e titulo vazio ou nao), mas suja toda mensagem. Medir se cmd /U, chcp 65001 ou ler por Get-Process/WMI resolve, e cobrir com teste em interpretarTasklist.
> chcp 65001 antes do tasklist (linhaTasklist puro + cmd /c verbatim em processosSapGui); cmd /U, TextDecoder e WMI descartados por medicao; teste em gui.test.mjs e gotcha na receita-gui-scripting.md; achado do PowerShell virou item proprio

- [x] 18. sids(sessao): a tela ENTREGA o nome tecnico de cada campo — virar funcao da lib. Medido no s4h 758/250 em 04/09/2026: o atributo lsdata de cada input carrega o SID do SAP GUI no indice "21" (ex.: {"21":{"SID":"wnd[0]/usr/ctxtRS38M-PROGRAMM"}}), e o snippet [...document.querySelectorAll("input")].filter(visivel).map(e => JSON.parse(e.getAttribute("lsdata"))["21"].SID) rodou como esta e devolveu wnd[0]/usr/ctxtRS38M-PROGRAMM (SE38) e wnd[0]/usr/ctxtDATABROWSE-TABLENAME (SE16). Isso resolve o modo de falha CALADO do item 6 (nome de campo errado na URL nao preenche nada e nao avisa): a lib pode responder "qual o parametro certo para a URL desta tela" antes de tentar. Alem do SID, o lsdata tem rotulo, estado e flags (item 9 quer o vocabulario inteiro) — este item e so a peca do SID: sids(sessao) devolvendo [{ id, title, sid, campo }] com campo = SID sem wnd[0]/usr/ e sem o prefixo de tipo (ctxt/txt/cmb/chk), que e exatamente o nome que a expressao ~transaction quer.
> pista: O item 9 (04/09/2026) ENTREGOU esta peca dentro do lerTela: cada campo sai como { id, sid, campo, rotulo, dica, valor, maxlen, editavel }, com campo = SID sem wnd[0]/usr/ e sem o prefixo de tipo — exatamente o que a URL ~transaction quer. E corrigiu a premissa deste item: o SID NAO mora sempre no indice 21 (isso vale so para o CBS; botao e 27, rotulo 19, radio 13, checkbox 14), entao o snippet escrito aqui so funciona para campo de entrada. O que resta decidir: se sids(sessao) ainda vale como funcao propria ou se lerTela().campos ja e a resposta.
> Decidido: vale como funcao propria, mas como RECORTE puro do lerTela, nao como leitura paralela — sidsDaTela(tela) + sids(sessao) em webgui.mjs devolvendo [{ id, title, sid, campo, rotulo }] dos campos de entrada visiveis, sem o okcd. Teste sobre as fixtures medidas (23 passando), receita-webgui.md e README apontam sids antes de montar a URL. Commit 653cfda no adt-tools.

- [x] 19. A espera de tela pronta estoura o teto de 60 s quando a acao EXECUTA um report. Medido no s4h 758/250 em 04/09/2026, mesma sessao e mesmo Chrome: as chamadas que so preenchem dynpro levaram 2,4-8,2 s (SE16 2,8 s; SE38 2,5 s; SA38 2,5 s; SE16T000 8,2 s), enquanto as duas que executaram o RSPARAM por DYNP_OKCODE=STRT levaram 64184 ms e 62920 ms — o teto de 60 s de `ir()` mais o assentar, nao o tempo do report. Hipotese: jsTelaPronta nunca da true na tela de saida do report (a tela de selecao do RSPARAM tem 1 checkbox e pouca coisa com [ct]), entao o laco roda ate o teto e so depois o esperarQuieto le a tela certa — o resultado sai correto, mas custa 60 s por chamada. Medir qual das tres condicoes (texto >= 200, [ct] > 5, inputs visiveis > 0) nao fecha nessa tela, e decidir o sinal para tela de LISTA/relatorio, que nao tem campo de entrada nenhum.
> Medido (s4h 758/250, 04/09/2026, POC_webgui_lsdata/medicoes/tela-pronta.md): a condicao que nao fecha e inputs > 0 — selecao do RSPARAM, lista ALV (bruto do item 9) e menu tem ZERO input visivel; texto/[ct]/readyState fecham em 0,5 s. E o casco (47 ct, 3574 chars) chega antes da dynpro e ja satisfaz texto e [ct]. Sinal novo em webgui.mjs: RE_SID_DA_DYNPRO (controle com SID em wnd[n]/usr/ ou wnd[n]/tbar[1]/; o casco tem zero), minimoCampos vira piso opcional >= 0. Contra-prova com a lib: selecao 64,2 s -> 2,5 s, SE16 T000 8,2 s -> 3,5 s, lerTela le o mesmo. Commit f8ba9c9 no adt-tools; receita-webgui.md § tela pronta.

- [x] 20. Portar o protocolo HTTP puro do ITS para a lib — o canal WebGUI sem navegador. O item 7 provou o protocolo (receita-webgui.md § O protocolo do ITS por HTTP puro) mas ele e RECEITA, nao modulo: hoje webgui.mjs so sabe a via do Chrome+CDP. Portar como via alternativa do mesmo canal (abrir/boot/postar/lerTela por parser de XML em vez de DOM), com as regras medidas embutidas: jar de cookie obrigatorio, primeiro POST e boot (acao nele e perdida), X-Code do multipart como o sinal de que a acao NAO pegou, e o SID como endereco. Decidir o desenho: mesma API do webgui.mjs com { via: "http" }, ou modulo proprio its.mjs. Ganho medido: ~0,95 s contra 9 s, e roda onde nao ha Chrome (CI, container, servidor). O que a via HTTP nao tem: print de tela e leitura por DOM.
> SIM: virou modulo proprio. packages/adt-client/its.mjs (export adt-client/its) + its.test.mjs (9 testes puros, suite 730/730) + README + receita-webgui.md § O modulo its.mjs. Desenho: modulo proprio com o MESMO vocabulario do webgui.mjs (abrirTransacao/preencher/acionar/enter/enviar/comandar/fechar), reusando urlWebgui/autorizacao/interpretarSonda/okcodeDe/campoDoSid — nao { via: http }, porque sessao, endereco e gesto sao outros. Regras embutidas: jar de cookie, boot antes de tudo, forma da resposta (delta/multipart/logoff/sem-sessao) como veredito com X-Code no motivo, SID tirado do lsdata da propria tela (sidsDaResposta) e alvo fora da tela estourando ANTES do POST com a lista. E2E no s4h 758/250 em 04/09/2026 (sap-accelerate/work/POC_webgui_its_lib/medicoes/its-lib.md): SE16 T000 MAX_SEL 200->2 + btn[8] = 2 acertos; GET+boot 657 ms. Medicao nova: valor mandado em POST separado PERSISTE para a acao do POST seguinte (3 acertos).

- [x] 21. lerTela por PARSER do delta-update — a leitura da via HTTP pura. Medido no s4h em 04/09/2026 que o XML de resposta carrega a tela inteira: cuatitle/ScreenId/dynpro/d-num nos sap.its.aParams e cada <input> com lsdata trazendo SID, title e value (foi assim que a prova do item 7 achou o wnd[0]/usr/txtMAX_SEL entre os campos preenchidos). Escrever o parser puro que devolve { titulo, campos: [{ sid, title, valor }], botoes: [{ okcode, title }] } a partir do XML — o equivalente do lerTela/campos/botoes sem DOM. Cruza com o item 9 (vocabulario do lsdata) e com o item 18 (sids): pela via HTTP os tres viram o MESMO parser.
> nota: o its.mjs (item 20) ja expoe sids/campos/botoes por regex sobre o lsdata (sid, tipo, campo, okcode, value, maxlen, applicationText) — o que falta aqui e o MODELO da tela: montarTela do webgui.mjs sobre um parser dos controles do delta-update (rotulo costurado pelo label, valor legivel, mensagem, grid, popup), com as mesmas fixtures do webgui.test.mjs
> FECHADO — a leitura da via HTTP pura e o MESMO modelo do navegador. its.mjs ganhou controlesDoHtml/controlesDoDelta (scanner de tags sobre o HTML dentro dos CDATA do delta-update, no MESMO formato do JS_DESPEJO_CONTROLES do DOM), popupDaTela, telaDoDelta e lerTela(sessao)/parametrosDaTela — o montarTela do webgui.mjs passou a servir as DUAS vias (nao foi reescrito; ganhou so a lista `rotulos`). CRUZADO na MESMA SE38 (c4-nse38.txt por HTTP x se38.json pelo Chrome, s4h 758/250, 04/09/2026): titulo, campo (nome/rotulo/dica/maxlen), os 16 botoes (okcode/rotulo/tecla), os 5 radios, mensagem, rotulos e okcd IGUAIS pelas duas vias; 325 controles em ~25-40 ms. A unica divergencia (selecionado dos radios) e ERRO DO DESPEJO DOM daquela POC, que guardou el.checked (sempre false num span role=radio) — o XML traz o aria-checked certo. 3 regras do texto medidas no markup: inline COLA (o span urAccessKey separa a letra de atalho — separador daria "P" + quebra + "rograma" e o rotulo viraria "P"), so bloco quebra linha, e subarvore invisivel nao da texto (o " Destacado" do btn[0] e pseudoHidden DENTRO do botao). Medido tambem que o delta e a tela INTEIRA em todo POST (nao diff) e que com POPUP aberto o steploop0 vem vazio e a wnd[0]/usr some (item 23 anotado). Codigo: adt-tools/packages/adt-client/its.mjs + its.test.mjs (7 testes novos, suite 738/738) + webgui.mjs (rotulos) + docs/receita-webgui.md § Ler a tela do delta-update. Medicao: sap-accelerate/work/POC_webgui_its_lib/medicoes/lertela-xml-x-dom.md + scripts/cruzar-lertela.mjs

- [x] 22. Mapa do vkey/<n>/ses[0] — o teclado do canal WebGUI por HTTP puro. Medido no s4h em 04/09/2026 (item 8) que vkey/0/ses[0] e o Enter, e que ele dispara tanto o OK-code quanto o Enter da dynpro. Falta medir o resto do mapa, e se ele dispensa o okcd: vkey/8 (F8 executar), vkey/3 (F3 voltar), vkey/15 (Shift+F3 sair), vkey/11 (Ctrl+S gravar), vkey/12 (F12 cancelar) — e se o numero e o mesmo do SAP GUI. Se o mapa fechar, o canal dirige a tela por TECLA, o endereco mais estavel que existe (nao depende de btn[n] nem do fcode da dynpro), e a lib ganha tecla(sessao, "F8") na via HTTP igual a que ja tem na do navegador.
> PEGOU — o mapa fechou inteiro e virou tecla(s,"F8") na lib. Medido no s4h 758/250 em 04/09/2026, sem navegador, cada tecla em sessao nova na SE16 da T000: vkey/3 (F3 voltar: lista -> selecao -> 1a tela), vkey/4 (F4: com foco no campo abriu popup wnd[1] de pesquisa de tabelas), vkey/8 (F8 executar: "5 acertos", IGUAL ao btn[8] e ao OK-code /8), vkey/11 (F11 gravar: "Atributos variante" SAPLSVAR), vkey/12 (F12 cancelar), vkey/15 (Shift+F3: sai num salto). O n E o mesmo numero de tecla do SAP GUI — tres provas: F8 == btn[8] == /8; Shift+Fn = 12+n; e o proprio lsevents de campo com match code declara FieldHelpPress: vkey/4. As tres perguntas de decisao fecharam com sim: a tecla DISPENSA o okcd (vkey/8 sozinho executou), LEVA o que foi preenchido no mesmo POST (MAX_SEL=2 + vkey/8 -> "2 acertos", ao contrario do OK-code) e o sufixo /ses[0] e OBRIGATORIO (sem ele: multipart -1002 <control-id> is expected). Entrou na lib: VKEYS + numeroDaTecla + tecla(sessao, nome) em its.mjs (apelidos Executar/Voltar/Sair/Cancelar/Gravar/Ctrl+S; nome fora do mapa estoura ali listando o medido; vkey(s, n) segue crua para medir). E2E pela lib na fase I. Aberto: F12 x Shift+F3 nao se distinguem nas telas da SE16 (item 23, popup), e F1/F2/F5-F7/F9/F10/Ctrl+Fn ficaram fora de proposito. Doc: packages/adt-client/docs/receita-webgui.md § O teclado — o mapa do vkey/<n>. Evidencia: sap-accelerate/work/POC_webgui_vkey/medicoes/mapa-vkey.md

- [x] 23. Dirigir POPUP (wnd[1]) na via HTTP pura do WebGUI. Medido no s4h em 04/09/2026 (item 8): /o abre a lista de modos e /nend abre a Pergunta Sim/Nao, e nos DOIS casos o popup vem no MESMO delta-update, com SIDs wnd[1]/usr/... e botoes com title ("Sim", "Nao", "Eliminar sessao"). Falta medir como RESPONDER: acionar o botao do popup por action/3, se o cuatitle passa a ser o do popup (a leitura de hoje pega o da janela de tras, o que faz a tela parecer inalterada) e como saber que ha popup aberto. Sem isso o canal trava em qualquer transacao que pergunta algo — e o popup e o caso comum de gravacao, exclusao e saida.
> pista: O item 21 (04/09/2026) entrega o ENDERECO LEGIVEL do popup: lerTela(s).popup traz { sid: wnd[1], id, titulo, textos, botoes, campos } lido do delta — no /nend, titulo "Efetuar logoff", textos SPOP-TEXTLINE1/2 e botoes wnd[1]/usr/btnSPOP-OPTION1 "Sim" / OPTION2 "Nao"; no /o, a wnd[1] tem barra PROPRIA (wnd[1]/tbar[0]/btn[0] Avancar, btn[12] Cancelar) e sub-tela sub:RSM04000_ALV_NEW:2000. MEDIDO tambem que com popup aberto o steploop0 vem VAZIO e a wnd[0]/usr some do delta (0 SIDs contra 48 do mesmo menu sem popup), e um state/ur posterior devolve o mesmo — lerTela poe isso em tela.aviso. O que falta e so o DISPARO: acionar(s, { sid: tela.popup.botoes[0].sid }) — os botoes do popup NAO sao btn[n], entao acionar(s, "Sim") nao os acha. Detalhe em sap-accelerate/work/POC_webgui_its_lib/medicoes/lertela-xml-x-dom.md
> medido: o popup trava tambem o canal do NAVEGADOR: no s4h 758/250 em 04/09/2026 (item 13) o okcode /15 (Shift+F3) no menu abriu a pergunta de logoff — sap.its.getPopupCount() foi de 0 para 1 — e dali em diante o campo okcd de wnd[0] parou de postar (o /nSE16 seguinte ficou 20 s sem sair da tela, com o texto no campo). Bissecao: a mesma sequencia SEM o /15 anda inteira. Entao dirigir wnd[1] vale para os DOIS canais, e o teste do item pode ser feito no navegador tambem (getPopupCount + aPopups sao a sonda barata de "tem popup aberto").
> em andamento: Parte 1 MEDIDA (sem rede, sobre os brutos do item 8, em sap-accelerate/work/POC_webgui_popup/medicoes/dirigir-popup.md): (a) o cuatitle NAO muda com popup aberto — /nend e /o seguem SAP Easy Access/SMEN/SAPLSMTR_NAVIGATION, entao o sinal de popup e tela.popup, nunca o titulo; (b) so a wnd[0]/usr some do delta — o okcd e os 13 botoes da wnd[0] CONTINUAM enderecaveis com o modal na frente, que e a forma exata da trava do item 13; (c) tela.botoes MISTURA as duas janelas quando o popup tem barra propria (/o: 17 = 13 da wnd[0] + 4 da wnd[1]), e o btn[n] so resolve para a wnd[1] porque o bloco webguiPopups vem PRIMEIRO no delta (medido em d2-o.txt e d3-nend.txt). Falta so o DISPARO, que exige o sistema: roteiro A→I pronto e nao rodado em work/POC_webgui_popup/scripts/popup.mjs (okcode curto x SID explicito, comandar com popup aberto, vkey/12, contra-prova do apelido, e o Sim do logoff). Proximo passo: rodar node popup.mjs quando o s4h voltar.
> bloqueado até 2026-09-05 08:00: s4h fora do ar no meio da medicao em 04/09/2026 — TCP na 8000 aceita (TcpTestSucceeded True), mas o ICM nao responde: o /sap/public/ping deu 200 nas 4 primeiras chamadas e depois 14/14 tentativas em 11 min estouraram (UND_ERR_CONNECT_TIMEOUT ~10,6 s ou TimeoutError 20-30 s), igual em Node e em PowerShell.
> MEDIDO no s4h 758/250 em 05/09/2026 — roteiro A→I rodou inteiro (work/POC_webgui_popup/scripts/popup.mjs, medicao em work/POC_webgui_popup/medicoes/dirigir-popup.md). O DISPARO e o SID do botao: acionar(s, { sid: tela.popup.botoes[i].sid }) fecha o SPOP ("Nao" → popup null, sidsWnd1 11→0, os 6 campos da wnd[0] voltam) e o "Sim" do logoff volta forma:logoff com s.aberta=false. O apelido NAO alcanca (acionar(s,"Nao") estoura: os botoes sao usr/btnSPOP-OPTION1|2, fora do espaco de OK-code) e a TECLA NAO fecha (vkey 12 volta pegou:true e o popup continua). O okcode curto btn[12] SO resolve na wnd[1] quando o popup tem barra propria (/o: sidResolvido wnd[1]/tbar[0]/btn[12], fechou sem tocar nos 34 campos da SE16) — por isso o passo E ficou redundante. Sinal de popup e tela.popup, nunca o cuatitle. Divergencia com o item 13 (comandar passou com popup /o aberto) e o falso positivo do pegou viraram itens novos.

- [x] 24. Mapear o lsevents por tipo de controle — o mapa de DISPARO do WebGUI. O item 9 mapeou o lsdata (o que a tela E) e deixou o lsevents (o que ela FAZ) por medir. Visto de passagem no s4h 758/250 em 04/09/2026: o campo CBS publica ActionItemActivate -> {"1":"vkey/0/ses[0]"} (o Enter, que e exatamente o disparo que o item 13 cacou na via HTTP) e FieldHelpPress -> vkey/4 (o F4); o botao B publica Press -> {"1":"action/3"}; o Change/Select do campo carrega "1":"value". Ou seja a propria tela declara, controle a controle, qual comando do protocolo do ITS aciona cada coisa — e isso vale para as DUAS vias (navegador e HTTP puro, item 20). Medir: coletar lsevents de todo controle das telas ja em work/POC_webgui_lsdata/medicoes/raw/, agregar por ct como o agregar.mjs fez com o lsdata, e responder se da para DERIVAR o batch de POST de um controle qualquer sem capturar trafego.
> pista: o lsevents pode ser DISPARADO de dentro da pagina, sem gesto nenhum: a unica API de evento exposta e sap.its.enqueueEvent(p), com p = { sEvName, oItsParams: { modalNo, id, code, submit, type, content }, oUrParams: { Id, Value } } — medido no s4h em 04/09/2026 (item 13) que enfileirar { sEvName: "Enter", oItsParams: { code: "vkey/0/ses[0]", submit: true, type: "GuiOKCodeField" }, oUrParams: { Id: "ToolbarOkCode" } } navegou para a SE38 com o batch okcode+vkey+state/ur. O code sai do proprio lsevents do controle (indice 1), o type sai do lsdata — ou seja o mapa do item 24 vira um EXECUTOR: ler o lsevents e mandar. Fonte do renderer: /sap/public/icmandir/its/~cache-*/lsgui/js/webgui_min.js (submitOkCode, onEvent do GuiOKCodeField).
> Mapa feito e regra de composicao respondida. scripts/agregar-lsevents.mjs -> medicoes/vocabulario-lsevents.md: 1049/1713 controles com lsevents em 7 telas do s4h, 38 eventos, 19 comandos, por ct; a forma e sempre array[2] (2069/2069), a posicao 0 so traz opcoes de transporte e o indice 1 da posicao 1 E o comando. Da para derivar o batch, mas por FAMILIA, nao por concatenacao unica: action/<n> e value levam o SID, okcode/ses[0] e vkey/<n>/ses[0] ja vem auto-enderecados, e o vkey/<n> que o lsevents publica sem sufixo NAO se posta com o SID do campo — o alvo do teclado e a sessao (vkey/4/ses[0]) e o campo entra pelo focus anterior (medido no item 22). Leitura em medicoes/item24-lsevents.md; receita-webgui.md ganhou a secao De onde sai o COMANDO. Contra-prova de execucao (derivar.mjs) nao rodou: s4h fora do ar em 04/09/2026 -> item 25; lsevents ausente no despejo do its.mjs -> item 26.

- [x] 25. Ler o ALV do WebGUI pelo lsdata do STCS, sem varrer celula. Medido no s4h 758/250 em 04/09/2026 (item 9): o grid da lista do RSPARAM traz, no lsdata indice 34, { SID: wnd[0]/usr/cntlGRID1/shellcont/shell, Type: GuiGridView, ColumnIDs: [NAME,USER_VALUE,DEFAULT_VALUE,DEFAULT_USUBS_VALUE,DESCR], totalRows: 1617, editable: false, events: {clickRowCol, doubleclickRowCol, f1...} } — colunas e total de linhas de graca, sem tocar no DOM da tabela. O que falta e a CELULA: o DOM so tem a janela rolada (27 linhas de 1617; ct CP para cabecalho de coluna e SCB com o scroll, cujo lsdata traz posicao e total). Medir: como pedir as proximas linhas, se ha via de exportar, e o que o lerGrid do canal deve devolver. E o par WebGUI do lerGrid do GUI Scripting.
> RESOLVIDO e maior que o enunciado. O ALV nao se le varrendo celula: pede-se ao servidor com o RequestData do proprio renderer — POST action/710/<SID> content position=<n>&fragments=<de>,<ate>; mais get state/ur/<SID>. 1617 de 1617 linhas do RSPARAM num pedido so: 12,4 MB, 1,7 s de rede, 42 ms de parse, zero linha faltando (s4h 758/250, 04/09/2026). O enunciado estava ERRADO num ponto: o DOM nao tem so a janela de 27 — tem 166 linhas (so 27 <tr> com altura), porque o grid e scrolling:client com clientCellThreshold 10000. Cinco regras medidas: position e obrigatorio (sem ele volta multipart de 185 B); o action/61 e dispensavel; fragments e 0-based e o que volta e 1-based (lsMatrixRowIndex, absoluto); o ; NAO entrega faixas disjuntas (10,19;100,109; devolveu 26 contiguas); o servidor devolve no minimo uma janela (pedir 3 linhas trouxe 26), entao o avanco e pelo que FALTA. Custo linear e caro: ~7,7 KB por linha, 450-939 linhas/s — le lista de tela, nao faz extracao em massa. VIA DE EXPORTAR: desnecessaria, o fragments=0,total-1 E a exportacao (item 45 para o gesto de exportar por arquivo). Virou lib no its.mjs: batchFragmento, celulasDoGrid, linhasDoGrid, faltaNaFaixa (puros, com teste sobre o XML real) e lerGrid(sessao, alvo?, { de, ate, lote=500, tetoMs }) -> { id, sid, colunas, total, de, ate, linhas: [{ _linha, <ColumnID>: valor }], pedidos, bytes, ms, truncado }. BUG que a medicao expos e corrigiu: a resposta do 710 e um delta PARCIAL sem sap.its.aParams, e o postar a tomava pela tela — zerava sids/titulo/grids da sessao inteira; agora lerResposta marca parcial e o postar guarda em sessao.parcial sem mexer no delta. Medicao em sap-accelerate/work/POC_webgui_grid/medicoes/item25-grid.md; receita em docs/receita-webgui.md secao O ALV: ler as LINHAS do grid. Achados novos: itens 45 (exportar por arquivo), 46 (lerGrid do navegador) e 47 (escrever/ordenar/filtrar no grid).

- [x] 26. Navegar pelo MENU da tela no WebGUI — a via que dispensa saber o tcode. Achado de passagem no item 9 (s4h 758/250, 04/09/2026): os controles de menu tem lsdata e nunca foram olhados — POMN (role=menu, nas 7 telas), POMNI (role=menuitemradio, 49 controles), MG (role=group, 15 no menu inicial) e TV (role=button, 30 no SAP Easy Access, que e a arvore do menu de usuario). O botao Menu da barra ja aponta o alvo: lsdata {"13":true,"14":"mnu0_9718","25":"TOGGLE"}. Se o menu for legivel e acionavel, o canal ganha o caminho que o BDC e a URL nao tem: chegar numa tela por CAMINHO DE MENU, util quando a transacao nao e conhecida ou nao existe. Medir o vocabulario desses ct e provar um acionamento.
> Menu da barra medido e ACIONADO no s4h 758/250 (04/09/2026). A hipotese do item estava errada: POMN/POMNI das 7 telas do item 9 sao o menu de INFORMACAO DO SISTEMA, e MG/TV sao a arvore do SAP Easy Access — a barra de menu da aplicacao NAO existe no DOM antes de ser aberta pelo cua2sapmenu_btn. Aberta, e o modelo mais legivel do canal: o id de cada item E o caminho (wnd[0]/mbar/menu[5]/menu[3]/menu[0]) e e igual ao SID do SAP GUI. Vocabulario fechado em 121 itens varridos da SE38, sete indices e NENHUM sobrando: [1] rotulo, [4] separador acima (provado por posicao y), [6] tem submenu (1:1 com [7] e aria-haspopup), [7] id do popup filho (volatil), [15] atalho, [18] {SID, GuiMenu} (=== id do DOM em 121/121), [19] rotulo. Acionamento provado ponta a ponta: da SE38, navegarMenu(s, "Sistema > Servicos > Reporting") chegou na SA38 ("ABAP: execucao do programa", campo RS38M-PROGRAMM), sem o script saber o tcode; caminho errado LANCA com a lista do que existe. CINCO armadilhas silenciosas medidas: (1) ha DOIS menus POMNI na tela e os dois tem um item "Sistema" — escopo pelo id, nunca pelo rotulo; (2) o botao Menu e TOGGLE, com o menu aberto o clique FECHA; (3) Escape NAO fecha o menu, e o ESCAPE do SAP e CANCELA A TRANSACAO (o proprio item "Cancelar" carrega lsdata[15]=ESCAPE); (4) a abertura do submenu nao e sincrona — esperar o filho, nunca um tempo; (5) o percurso e CASCATA, abrir um irmao fecha o anterior. E o indice menu[n] MUDA por tela (Sistema e menu[5] na SE38 e menu[4] na SA38): so o rotulo e estavel. Codigo em packages/adt-client/webgui.mjs § O MENU DA BARRA (filhoDiretoDeMenu, daBarraDeMenu, interpretarItemDeMenu, partirCaminhoDeMenu, acharItemDeMenu, itensDeMenu, abrirMenu, fecharMenu, navegarMenu), 7 testes puros com os lsdata REAIS (751 testes do pacote passam), receita em docs/receita-webgui.md § O MENU da barra. Medicao: sap-accelerate/work/POC_webgui_menu/medicoes/item26-menu.md. Ficou fora e virou item: 48 (item de menu desabilitado nunca foi visto), 49 (o menu pela via HTTP pura do ITS), 50 (a arvore do SAP Easy Access TV+MG).

- [x] 27. Provar que no ICF DESATIVADO responde 404 — a leitura do estado que faltou no item 11. Medido em 04/09/2026 (s4h 758/250) que /sap/bc/gui/sap/its/test, que EXISTE na ICFSERVICE como irmao do webgui, devolve a MESMA pagina de 9314 bytes que um path inventado — mas se ele esta em 404 por DESATIVACAO nao foi lido do sistema: a classe classrun que leria o estado do no nao chegou a rodar (o POST do ADT respondeu Service nicht erreichbar na janela da medicao). A ICFSERVICE nao serve de atalho: ICF_NOACT esta vazio nas 17474 linhas e nao existe tabela ICFACTIVE neste release. Achar a API ABAP que responde ativo/inativo por URL (candidatos nao medidos: cl_icf_tree, ICFINSTACT que tem PATH legivel e coluna INACTIVE) e cruzar com o 404 — se confirmar, o motivo da causa sem-no da sonda deixa de ser hipotese; se desmentir, o texto da lib muda.
> DESMENTIDO: o /sap/bc/gui/sap/its/test esta ATIVO (cl_icf_tree=>is_service_active e /iwfnd/cl_icf_access, 15/15 concordantes) e responde 404 assim mesmo — a causa e nao ter handler na ICFHANDLER (5 filhos, zero handler; o webgui irmao tem CL_HTTP_EXT_ITS_2), o mesmo que acontece com /sap, /sap/bc e /sap/public. O 404 tem tres causas e o HTTP nao separa nenhuma. O estado se le por CL_ICF_TREE=>IS_SERVICE_ACTIVE (estatico, url ou nodeguid, excecoes classicas) — nao por tabela: ICF_NOACT vazio em 17474 linhas, ICF_SERV_STAT com 0 linhas, ICFINSTACT so com as 241 ativacoes de instalacao. Contrafactual medido (URL inexistente da INVALID_URL; o public do vhost ZLHSGW_VOOS volta vazio) e a armadilha do homonimo entre virtual hosts documentada. Lib: interpretarSonda com o motivo novo, receita-webgui.md com duas secoes novas, teste puro ajustado (751 passando). Medicao: sap-accelerate/work/POC_icf_estado/medicoes/item27-estado-do-no-icf.md. A prova por desativacao virou o item 51 (bloqueado ate 05/09/2026).

- [x] 28. O POST do ADT sumiu por ~15 min no s4h e voltou sozinho — medir a causa. Em 04/09/2026, depois de uma varredura de ~120 GETs no ICF sem logoff, o create do classrun e o POST do datapreview passaram a responder 400 com HTML Service nicht erreichbar (o 404 do ICF em alemao) enquanto o GET stateless do /sap/bc/adt/core/discovery seguia 200; ~15 min depois o discovery respondia 200 de novo. Hipotese: sessoes de dialogo acumuladas pelos GETs (cada 200 do WebGUI abre uma) estourando o limite do usuario ou do sistema, com o no de escrita do ADT sendo o primeiro a recusar. Nao isolado. Parente do sintoma da fila 21 do jbv-adt-client (stateful 400 com stateless 200), e vale para QUALQUER varredura da lib — medir quantas sessoes o limite aguenta (SM04/AL08 por classrun) e se o logoff da sonda basta para nao chegar la.
> CAUSA ISOLADA por rampa no s4h 758/250 (04/09/2026), e o mecanismo era outro: o 400 Service nicht erreichbar NAO e o no do ADT fora do ar — e a SESSAO nascendo morta. Medido: (1) 1 GET no webgui = 1 sessao, 1 logoff = -1 sessao (10 GETs 4->14; 10 logoffs 14->4); (2) rdisp/max_alt_modes=16 DESMENTIDO como teto (28 sessoes minhas, tudo OK); (3) o teto real fica entre 145 e 154 sessoes do MESMO usuario — 144 passavam, 154 quebraram, com o GET stateless seguindo 200; (4) passado o teto o logon ainda responde 200 COM token mas o cookie vem SEM SAP_SESSIONID, e ai QUALQUER requisicao com esse cookie da 400 em QUALQUER path — inclusive /sap/public/ping, que na mesma janela dava 200 so com Basic (contra-prova: Basic 200 | Cookie 400 | Basic+Cookie 400, o cookie vence); (5) fechar depois NAO cura: no estado doente o proprio logoff da 400 e a sessao FICA (24/50 antes e depois), entao todo retry aprofunda; quem cura e o tempo (http/security_session_timeout=1800). Instrumento novo: TH_USER_LIST por SOAP RFC com USRLIST:[] da a SM04 sem passar pelo ADT. Lib: encerrarSessao parou de mentir (encerrada = status 200, antes era sempre true), sessaoNasceuMorta novo + aviso no fetchToken, ERROS_TRANSVERSAIS com a entrada do Service nicht erreichbar, receita-webgui e canal-soap-rfc atualizadas, 754 testes passando. Medicao: sap-accelerate/work/POC_sessoes_icf/medicoes/item28-sessoes-e-o-400-do-adt.md

- [x] 29. Contra-prova do TSTCC S_WEBGUI: o ITS recusa mesmo uma transacao com o flag desligado? A receita do par tran+WebGUI (receita-tran.md § O par tran + WebGUI) documenta que deployTransaction liga html_enabled por default (gui.html -> S_WEBGUI = 1) e que o driver imprime o que gravou — mas a contra-prova NUNCA foi feita: ninguem mediu o que o /sap/bc/gui/sap/its/webgui?~transaction=<TCODE> responde quando a transacao existe com S_WEBGUI vazio. Medir no s4h 758/250: deployTransaction de duas transacoes Y* iguais sobre o mesmo report (uma com gui:{html:true}, outra com gui:{html:false}), abrir as duas pelo canal e registrar a diferenca (erro do ITS, pagina de logon, tela normal?). Fecha um pre-requisito que hoje esta escrito como suposicao. Apagar as duas no fim.
> MEDIDO no s4h 758/250 em 04/09/2026 — o ITS NAO olha o TSTCC S_WEBGUI. Duas transacoes Y* iguais sobre RSPARAM ($TMP, mesma deployTransaction, so o gui.html diferente): YJBV_WGON (S_WEBGUI '1') e YJBV_WGOFF (S_WEBGUI '', confirmado por readTable em outra LUW) abriram IGUAIS pelo ~transaction — delta-update, titulo "Exibir parametro de perfil SAP", dynpro RSPARAM. Controle que da valor ao resultado: a transacao INEXISTENTE YJBV_WGNADA foi recusada no boot com HTTP 500 "Invalid transaction code YJBV_WGNADA / Start of transaction failed (rc=1)" — o canal sabe recusar, so nao recusa por causa do flag. Dois achados de canal no mesmo ciclo: (1) o GET da URL NAO decide — 200 + SAP_SESSIONID nos tres casos (~36 KB do shell); quem aceita/recusa a transacao e o POST de boot; (2) o t.gui.webgui impresso pelo driver (RPY_TRANSACTION_READ) bateu com a TSTCC em outra LUW nos dois casos — o html_enabled chega ao banco. As duas apagadas ao final (deleteTransaction subrc 0, readTransaction exists:false). A regra 3 do § par tran+WebGUI deixou de ser suposicao: receita-tran.md § A contraprova do TSTCC S_WEBGUI (tabela dos tres casos + o que fica aberto); comentario no ponto do default em tran.mjs. Fica aberto e virou o item 54: os outros dois caminhos de entrada (/n<TCODE> dentro da sessao ITS e o menu SAP Easy Access).

- [x] 31. No canal do NAVEGADOR o OK-code NAO leva o que foi digitado na dynpro — medido no s4h 758/250 em 04/09/2026 (item 13): preencher o campo da tabela na SE16 pelo gesto real (clique + insertText) e em seguida comandar(s, "ONLI") devolveu "Entrar nome de tabela", e o batch saiu SEM nenhum value/<SID> do campo (so focus/…, okcode/ses[0], action/304 position=4 e vkey/0). A causa provavel esta no fonte: o handler do GuiOKCodeField chama submitOkCode(), um caminho CURTO que publica o OK-code e nao passa pelo coletor que agrega os value/ dos campos sujos. Hoje a regra e "acionar (botao) leva valores, comandar (OK-code) nao" — o que basta para navegar, mas fecha a porta para "preenche e manda o fcode", que e como se dirige dynpro de verdade. A medir: (a) o valor vai se o campo perder o foco antes (blur/Change disparado) ou se o batch do okcode for precedido de um evento de campo; (b) se disparar o Enter de um campo VISIVEL com o okcd preenchido leva as duas coisas juntas; (c) se na via HTTP pura vale o mesmo (la o value/<SID> e explicito, entao provavelmente e limite so do navegador).
> RESOLVIDO ao contrario do que o item supunha: o OK-code LEVA o que foi digitado — nao havia limite de canal, faltava o valor virar EVENTO. No fonte, quem publica o valor e o Change do controle (addBatch({post:"value/"+SID})) e o submitOkCode enfileira na MESMA fila (v.add), entao o que estiver la sai junto; Input.insertText nao dispara o Change, o blur dispara (bissecao: change sintetico sem blur nao basta, blur sozinho basta). Medido no s4h 758/250 em 04/09/2026, sessao nova por cenario, batch lido do CDP, na tela de selecao da SE16 sobre a T000 com MTEXT=Neduca (1 das 5 linhas): preencher+comandar = 5 acertos (o limite antigo), preencher+blur+comandar = 1 acertos com value/ e okcode/ no MESMO post, mais os dois contrafactuais (okcode sem valor = 5; valor com Enter sem okcode = nao executa) e o controle positivo do botao (1 acertos). Alinea (c) idem na via HTTP pura, num POST de 113 ms — la o okcd e campo como outro qualquer e quem submete e o Enter. Alinea (b) medida e DESNECESSARIA: change sintetico no okcd nao e aceito pelo renderer. Virou codigo (commit f938f45): webgui.mjs JS_PUBLICAR_FOCO + comandar({publicarValores=true}), its.mjs comandar despachando a fila (era throw citando este item), receita-webgui.md com o § trocado e as DUAS armadilhas de medicao que a rodada pagou (hook no XHR nao ve todo o trafego; "ficou na mesma tela" nao e "nao executou"), testes puros nos dois modulos (756 passam). Achados viraram os itens 55 e 56. Medicao: sap-accelerate/work/POC_webgui_okcode_valores/medicoes/okcode-valores.md

- [x] 32. IDEIA (POC 4029823-J1B1N, item 10, 04/09/2026): where-used pelo ADT — POST /sap/bc/adt/repository/informationsystem/usageReferences?uri=<uri do objeto>, body usagereferences:usageReferenceRequest, Accept application/vnd.sap.adt.repository.usagereferences.result.v1+xml. Funcionou no SXD 816 para elementos de dados (status 200, refs com uri/parentUri); LIMITE medido: para um elemento de dados devolve quem DECLARA o tipo (LJ1BB2TOP, classes, pacotes), nao quem escreve no campo de uma tabela. Vale como funcao da lib (whereUsed(session, uri)) para classes/FMs/includes, com esse limite documentado. Script de prova: sap-accelerate/work/POC_4029823_j1b1n/scripts/medir-item10-onde.mjs.
> FEITO 04/09/2026: whereUsed(session, uri) + parseUsageReferences (puro) em packages/adt-client/search.mjs, commit e7f81b0 no adt-tools; 5 testes novos no parsers.test.mjs (suite: 766 passam). Parser casado com o XML REAL medido no SXD 816 (5 elementos de dados da POC 4029823-J1B1N item 10). Tres achados que corrigem a leitura do endpoint: (1) a arvore achata pai e filho — so isResult=true e uso (o script da POC contou 41 onde havia 15); (2) o resultado chega PELA METADE em silencio — no com canHaveChildren=true vem colapsado e o numberOfResults nao bate com o expandido (J_1BNFNUM_UTILITIES 12 x 5, 11 colapsados) — dai colapsados/completo no retorno; (3) nome/tipo/pacote so no filho adtObject. Limite documentado no README e no cabecalho: para elemento de dados o expandido sao DECLARACOES (TABL/DSF), nao escritas. Expandir no colapsado virou o item 57.
> medido: ACHADO na prova em SEGUNDO sistema (04/09/2026): o PREFIXO do namespace do where-used muda por sistema — SXD 816 manda usagereferences: (minusculo), s4h 758 manda usageReferences: (camelCase), mesmo endpoint e mesmo corpo. O parser do primeiro commit, preso ao prefixo do SXD, lia os 41.226 usos de MATNR no s4h como ZERO com status 200 e completo=true. Corrigido em 3523def: regexes pelo nome LOCAL, completo tambem falso quando ha usos anunciados e lista vazia, e teste de regressao com recorte real do s4h. Licao geral: provar em UM sistema so nao prova o parser.

- [ ] 33. IDEIA (POC 4029823-J1B1N, item 10, 04/09/2026): listar includes e FMs de um grupo de funcoes pelo ADT — GET /sap/bc/adt/functions/groups/<fg>/objectstructure devolve objectStructureElement com adtcore:type FUGR/I (includes) e FUGR/FF (FMs) e o href do source de cada um; o GET do grupo em si NAO lista includes e /includes sem nome da 400. Com isso da para grepar um grupo inteiro por um campo (J1BB: 203 includes; J1BB2: 315) — foi o que achou onde o Writer novo deriva UTRIB/IND_MOV. Vale como listarObjetosDoGrupo(session, fg) na lib + um grep de grupo. Script de prova: sap-accelerate/work/POC_4029823_j1b1n/scripts/medir-item10-j1bb.mjs.
> bloqueado: pergunta pendente ao Joris — nome da funcao (listarConteudoDoGrupoDeFuncoes | listarObjetosDoGrupo) e formato do retorno (pre-filtrado em includes/functionModules como whereUsed | cru como buscar). Desbloquear quando responder.

- [x] 34. Anomalia do ROT medida em 04/09/2026: ~40 s depois de o lancador SAPgui.exe ter INICIADO o pad (saplogon.exe 42432, sem pad antes), fecharSapGui viu GetObject(SAPGUI) com engine SEM filhos enquanto a sessao SAP Easy Access estava visivel; ~1 min depois sessoesAbertas listava a sessao normalmente (5,6 s). Hipoteses: registro no ROT atrasado quando o pad nasce do lancador; ou entrada SAPGUI do ROT de um saplogon transitorio (Start-Process saplogon com pad ja de pe). Reproduzir de proposito: fechar o pad, abrirSapGui, e amostrar sessoesAbertas a cada 5 s por 2 min anotando quando a sessao aparece.
> Reproduzido em 05/09/2026 (S4H 250, work/POC_rot_sapgui): NENHUMA das duas hipoteses. O que segura o GetObject(SAPGUI) e um dialogo modal do CLIENTE (SAP Logon, "Um script esta tentando acessar SAP GUI", OK/Cancelar) que aparece ~1,4 s depois da chamada e nao devolve o objeto ate alguem clicar OK (medido: 90 s sem resposta; com clique, sonda em 4,8 s e fecharSapGui em 1,4 s). E por CHAMADA: 3 sondas seguidas = 3 popups orfaos, porque o timeout do chamador mata o cscript e deixa o popup na tela. O atraso REAL do ROT existe mas dura ~3-5 s (o pad nascido do lancador ainda nao registrou a entrada SAPGUI: GetObject falha rapido, sem popup); a hipotese do saplogon transitorio fica sem suporte (so um pad nasceu e persistiu; o SAPgui.exe e o lancador e morre). O relato de "engine SEM filhos" veio da lib colapsar "sem entrada no ROT" e "engine com 0 conexoes" na mesma saida. Servidor inocente: user_scripting_force_notification=FALSE (classrun). Medicao: work/POC_rot_sapgui/medicoes/item34-rot.md; receita atualizada em jbv-adt-client/docs/receita-gui-scripting.md; achados viraram os itens 60, 61 e 62.
> errata: A medicao rodou contra a versao antiga da lib (node_modules) e a receita foi commitada primeiro no repo standalone jbv-adt-client (9209ed9). A canonica e adt-tools/packages/adt-client: secao portada para la e o paragrafo "Aberto (anomalia anotada)" substituido. O achado (popup do cliente segura o GetObject) nao depende da versao da lib.

- [x] 35. janelasSapGui devolve titulo/textos com MOJIBAKE pelo mesmo mal do tasklist (fila 17) — medido 04/09/2026: o PowerShell escreve na codepage OEM 850 e o Node le UTF-8; (Get-Process -Id N).MainWindowTitle chegou "Entrada do nome do usu?rio", e com [Console]::OutputEncoding=[Text.Encoding]::UTF8 antes chegou inteiro. Aplicar o OutputEncoding no comeco do PS_JANELAS, provar contra um cmd com titulo acentuado (script vivo.mjs da sessao do 17) e cobrir em gui.test.mjs.
> Eram DOIS eixos, nao um: [Console]::OutputEncoding=UTF8 (saida em OEM 850) E CharSet=CharSet.Unicode nos DllImport (leitura por GetWindowTextA, best-fit silencioso: ĄŻ→AZ, —→-, テスト→???). Medido 05/09/2026 num 2x2 contra janela de titulo conhecido; sem o OutputEncoding o eixo do CharSet nem se observa. PS_JANELAS virou psJanelas(processos) exportada; prova final rodou contra a lib. adt-tools c47cbec; teste em gui.test.mjs (771 verdes); receita-gui-scripting.md; medicao em sap-accelerate/work/POC_rot_sapgui/medicoes/item35-mojibake.md. Alvo de medicao: console NAO serve (janela pertence ao conhost) — Form WinForms num powershell.exe.

- [x] 36. clicar(s, { okcode: "btn[8]" }) na tela de selecao do RSPARAM NAO trocou a tela em 65 s — medido de passagem no item 19 (s4h 758/250, 04/09/2026, scripts/tela-pronta.mjs): a amostra ficou identica a da selecao o tempo todo (77 ct, 1 usr, 0 input). No item 9 o mesmo botao executou o report via acionar (esperarResposta). Diferenca a medir: clicar sem esperar vs acionar, ou o estado da sessao depois de um ir() que estourou o teto. Conferir o retorno de apontar (coberto/noPonto) antes de culpar o gesto.
> NAO reproduz: o clicar(okcode btn[8]) na selecao do RSPARAM executou o report em 10 de 10 sessoes (s4h 758/250, 05/09/2026), 1,0-1,5 s ate a lista ALV, com e sem esperarResposta, inclusive reproduzindo o Page.navigate cru + 65 s de laco e a SE38 navegada antes na mesma sessao. apontar devolve coberto:false em 10/10 (alvo M0:50::btn[8]-cnt em 1501,885, dentro do viewport) — o gesto nao e culpado. A tela do item 19 era OUTRA: 77 ct contra 85, faltando exatamente o menu suspenso (POMN 1 + POMNI 7) e com document.title vazio por 60 s — renderer que nao terminou de montar. Medicao em sap-accelerate/work/POC_webgui_btn8/medicoes/item36-btn8.md; a licao (clicar sem esperarResposta nao olha o resultado) foi para o doc de clicar() e o btn[8] do mapa OKCODES em webgui.mjs. Abertos viraram itens 64 (contra-prova do foco, inconclusiva) e 65 (porta 9222 fixa quebra sessoes em serie).

- [x] 37. Medir por que /n devolveu S000/SAPMSYST (titulo SAP) em vez do SAP Easy Access (SMEN). Medido no s4h 758/250 em 04/09/2026 (item 20, passo D5, its.mjs): numa sessao aberta por ~transaction=*SE16 DATABROWSE-TABLENAME=T000, depois de /nSE16, Enter, /nSE38, o comandar(/n) devolveu t-code S000, dynpro SAPMSYST, titulo SAP, 106 KB. No item 8 o mesmo /n numa sessao nascida no menu devolveu SAP Easy Access (SMEN, SAPLSMTR_NAVIGATION). Hipotese NAO isolada: a sessao que nasce por ~transaction nao tem o menu como tela inicial. Contra-prova: mesma sequencia numa sessao aberta sem ~transaction, no mesmo dia; e ver se /n depois de /nSMEN volta ao menu. Importa porque /n e a via documentada de voltar ao menu (receita-webgui.md § caixa de comando).
> Hipotese REFUTADA: nao e o ~transaction. Medido no s4h 758/250 em 05/09/2026 (11 bracos, um por sessao, via its.mjs): /n ENCERRA a transacao e cai na tela de fundo da sessao — SMEN so se o menu ja foi carregado alguma vez naquela sessao, senao S000/SAPMSYST 0040 (0 campos, 1 botao, 106 KB). Contra-prova que derruba a hipotese: sessao nascida NO menu dando /n estando no menu tambem cai no S000. O fundo se ADQUIRE: sessao nascida em *SE16, depois de /nSMEN, responde /n com o menu. /3 (F3) tem o mesmo limite; de tela interna so volta uma tela. Via para o menu: /nSMEN (318-372 ms, medido dos dois estados). Medicao: sap-accelerate/work/POC_webgui_n_menu/medicoes/item37-n-e-o-menu.md; receita-webgui.md § '/n NAO e ir ao menu' + JSDoc de comandar em its.mjs e webgui.mjs.

- [x] 38. FLP Designer: abrir o formulário 'Criar atribuição de destino' já persiste um target mapping vazio. Medido no SXD 816/100 em 04/09/2026 no catálogo YJBV_POC_4029823: quatro inspeções que só abriram o formulário e fecharam a sessão produziram quatro linhas vazias, tipo de navegação 'Outro'. A lib/receita WebGUI precisa tratar criação como mutação imediata: cancelar ou excluir o rascunho no finally, e oferecer uma primitiva transacional/cleanup para formulários que criam antes de Gravar. Evidência: sap-accelerate/work/POC_4029823_j1b1n/medicoes/flpd-tabela-mappings.png e scripts/flpd-inspecionar-mapping.mjs.
> Fechado. A lib WebGUI passou a tratar criacao como mutacao imediata: sessao.desfazer (pilha LIFO que o fechar executa COM O NAVEGADOR VIVO, avisando alto em stderr o que sobrou) e transacional(s, { abrir, corpo, descartar }) — arma o descarte no instante da criacao, dispara no finally salvo confirmar(), propaga o erro original do corpo, e RECUSA quem nao disser como desfazer. Medido no s4h 758/250 em 05/09/2026 sobre a SE16 (sap-accelerate/work/POC_4029823_j1b1n/medicoes/item38-desfazer.txt): os quatro casos. O FLP Designer NAO foi re-medido (o SXD so responde na rede do cliente). Codigo: adt-tools 3ba9a46 (webgui.mjs, webgui.test.mjs +9 testes, log.mjs aviso, docs/receita-webgui.md secao nova, README). O script que produziu o lixo (POC scripts/flpd-inspecionar-mapping.mjs) foi reescrito com transacional; o gesto de descarte usado e o da EXCLUSAO (o unico medido) e que ele funcione a partir do formulario ABERTO segue em aberto — se falhar, o fechar avisa. Sub-tema virou o item novo sobre o its.mjs.

- [x] 39. FLP Designer: selecionar item de ComboBox via UI5 exige fireSelectionChange além de setSelectedKey/fireChange. Medido no SXD 816/100 em 04/09/2026: targetTypeInput com key TR mostrava 'Transação', mas mantinha campos genéricos URL/ID; ao disparar fireSelectionChange, o formulário renderizou target_transactionInput e target_system_aliasInput. Criar helper selecionar() no canal navegador que use gesto real ou emule a sequência completa e espere a rerenderização.
> Fechado. Helper selecionar() no canal navegador, em modulo novo fiori.mjs (adt-client/fiori) — a mesma sessao do webgui.mjs, mas endereco por id de CONTROLE UI5 em vez de SID. Ele faz o GESTO REAL (abre a lista, clica no item), espera a rerenderizacao e devolve o que mudou: { gesto, chave, texto, mudou, apareceram, sumiram }; mudou:false e informacao (a chave e conferida e estoura se nao pegou). Disparo por API vira saida de emergencia (viaPrograma), avisando em stderr. MEDIDO no s4h 758/250 em 05/09/2026, UI5 1.114.0, em laboratorio que repete o formulario do Designer (o SXD so responde na rede do cliente): setSelectedKey sozinho nao dispara evento nenhum e ainda assim escreve chave E texto na tela (o sintoma); fireChange sozinho nao acorda o ComboBox (handler no selectionChange) e o sap.m.Select nao TEM selectionChange; num formulario por binding two-way o setSelectedKey sozinho ja repinta — nenhum disparo por API serve aos tres desenhos, o gesto real serve (dispara selectionChange E change nos tres). Aceitacao: 12/12 casos (chave, texto, id do DOM com -inner/-label, viaPrograma, e a selecao que nao muda campo). Medicao: sap-accelerate/work/POC_ui5_combobox/medicoes/item39-gestos.md (raw + scripts). Codigo: adt-tools 7b9023a (fiori.mjs, fiori.test.mjs +9 testes, docs/receita-fiori.md, README, package.json exports). O script da POC do FLPD (flpd-criar-mapping.mjs) passou a usar o helper, mas NAO foi re-medido no FLP Designer. Achado colateral virou o item 67 (sap-ui-core.js vem vazio na 2a requisicao).

- [x] 40. FLP Designer: clicar no item do template estático não adiciona o tile; o acionamento fica no descendente interativo. Medido no SXD 816/100 em 04/09/2026: LI X-SAP-UI2-CHIP:/UI2/STATIC_APPLAUNCHER não reagiu, enquanto o ícone AdminPage--universalCatalogView--X-SAP-UI2-CHIP:__UI2__STATIC_APPLAUNCHER-img adicionou o bloco. Melhorar clicar() para, quando o contêiner selecionado não tiver ação, localizar o menor descendente visível/clicável e reportar qual nó recebeu o gesto.
> clicar()/apontar() descem para o menor descendente acionável quando o alvo não declara ação, e reportam { desceu, recebeu, de, porQue, candidatos }. Reproduzido fora do SXD no s4h 758/250 (UI5 1.114.0, 05/09/2026): li inerte sem descer não dispara nada (o sintoma), com a descida aciona pelo iconeAdd; li ativo (sapMLIBActionable), item de ComboBox (role=option, item 39) e li sem descendente acionável NÃO são rebaixados. Critério lido do DOM: tag/role de comando, ct/lsdata/lsevents (mantém o WebGUI intacto) e cursor:pointer computado — o único que separa os dois li medidos. Código adt-tools e801f82; medição sap-accelerate/work/POC_ui5_clicar_descendente/medicoes/item40-descida.md. Aberto: contêiner com vários descendentes acionáveis → item 68.

- [x] 41. FLP Designer em HTTPS interno: o Chrome/CDP precisa ignorar o certificado não confiável antes da navegação. Medido no SXD 816/100 em 04/09/2026: /UI2/FLPD_CUST redireciona para https://awskartsxd01.acclab.com:44300/ e só abre após Security.setIgnoreCertificateErrors({ignore:true}). Avaliar opção cfg para certificados internos, com default seguro e receita explícita, em vez de cada script chamar s.cmd manualmente.
> Entregue: `cfg.certificado` (sistemas.json) com default SEGURO — ausente = validar normalmente; "sha256/<pino>" = aceitar SÓ aquele certificado (bandeira --ignore-certificate-errors-spki-list); true = sessão inteira sem validar, com aviso. `spkiDoHost(base)` lê o pino + subject/issuer/validade para conferir antes de pinar. `ir` passou a LER o errorText do Page.navigate e lança com a receita em ERR_CERT_*/ERR_SSL_* (antes: 60 s de espera contra chrome-error:// e a culpa na tela). `interpretarSonda` ganhou causa `certificado`. Medido em laboratório LOCAL 05/09/2026, sem VPN (POC_https_cert): Security.enable não é pré-requisito; o pino RESTRINGE (pino de outro cert → barrado); os 3 códigos de recusa do fetch. Ponta a ponta com abrirNavegador/ir: default lança, pino abre, true abre avisando. Commit adt-tools 85cb1ab; receita-webgui.md § HTTPS com certificado interno; medição sap-accelerate/work/POC_https_cert/medicoes/item41-cert.md. NÃO rodado contra o SXD (exige a rede do cliente): falta confirmar o pino real de awskartsxd01.acclab.com:44300 e que o FLP Designer abre com ele. O fetch/ADT em HTTPS com CA interna segue quebrado → fila 69.

- [x] 42. Enderecar botao POR JANELA na via HTTP: com popup aberto o tela.botoes/sidDoAlvo mistura wnd[0] e wnd[1]. Medido em 04/09/2026 sobre POC_webgui_okcode/medicoes/raw/d2-o.txt (item 23): o /o traz 17 botoes — 13 da barra da wnd[0], que CONTINUA no delta atras do modal, e 4 da barra propria da wnd[1]; btn[0] existe nas DUAS (wnd[1]/tbar[0]/btn[0] e wnd[0]/tbar[0]/btn[0]). O sidDoAlvo casa pelo fim do SID e pega o PRIMEIRO, e hoje acerta a wnd[1] so porque o bloco webguiPopups vem antes do cuaarea no delta — ordem de markup, nao regra. Entregar: escopo de janela (acionar(s, alvo, { janela: wnd[1] }) ou resolucao que prefira a janela ativa) e erro que mostre os candidatos das duas janelas quando houver ambiguidade.
> Entregue: o alvo da via HTTP tem JANELA. janelaAtiva(sids) = a GuiModalWindow de MAIOR indice declarada no delta (sem popup nenhuma janela se declara, entao wnd[0]); sidDoAlvo/preencher/acionar aceitam { janela } e, sem ele, resolvem na janela ATIVA — antes vencia o PRIMEIRO SID do markup. Erro novo mostra as duas janelas ("btn[15] nao esta em wnd[1] (a janela ativa) — esta em wnd[0]/tbar[0]/btn[15]; wnd[1] tem ...; peca { janela: wnd[0] }") e o mesmo okcode 2x na mesma janela pede o SID inteiro. sids/botoes/campos carregam a janela dona e recortam por ela; ativa(s) exposto. Corrigido de quebra: o catch do apelido engolia o erro do BOTAO e mandava procurar campo (acionar(s,"Executar") sem btn[8] dizia campo "Executar" nao esta na tela). Medido sobre os brutos de 04/09: d2-o.txt (btn[0] nas duas janelas) resolve wnd[1], e d2-ose16.txt (wnd[1]+wnd[2] empilhadas) passa a resolver wnd[2] — antes wnd[1], a de baixo. 800/800 testes do adt-client. Codigo: adt-tools packages/adt-client/{its.mjs,webgui.mjs,its.test.mjs} + docs/receita-webgui.md; medicao: sap-accelerate/work/POC_webgui_okcode/medicoes/item42-janela.md. Achado -> item 70 (popupDaTela ainda pega a modal de baixo).

- [ ] 43. Contra-prova da DERIVACAO do batch a partir do lsevents — o script ja esta escrito e nao rodou. work/POC_webgui_lsdata/scripts/derivar.mjs deriva do bruto, sem rede, action/4/wnd[0]/usr/radRS38M-FUNC_SSET (o Change do radio da SE38) e vkey/4/wnd[0]/usr/ctxtRS38M-PROGRAMM (o FieldHelpPress do campo), manda os dois pela via HTTP pura e contrasta o segundo com vkey/4/ses[0]. O item 24 fechou a composicao MEDIDA por familia, mas dentro da familia action/<n> so o action/3 foi postado: action/1, 4, 7, 8, 9, 25, 62, 74, 309, 810 e 901 estao no mapa e sao PRESUMIDOS. Se o radio marcar, o mapa do lsevents e um executor; se voltar X-Code, a composicao de action/<n> tem excecao como o vkey teve. Em 04/09/2026 o s4h nao respondeu: TimeoutError no GET da porta 8000 em 12s, com DNS resolvendo (ndc-s4hana -> 200.106.168.242).
> bloqueado: ate 05/09/2026 08:00: o s4h nao responde na porta 8000 (TCP timeout medido em 04/09/2026); sem sistema no ar a contra-prova nao roda

- [x] 44. Expor o lsevents no despejo de controles do its.mjs — o controlesDoHtml/controlesDoDelta le lsdata e IGNORA lsevents, entao pela via HTTP pura a tela nao entrega o mapa de DISPARO que o item 24 levantou: so a via do navegador (que le o atributo do DOM) o enxerga. Enquanto isso, um executor derivado do lsevents depende de um bruto coletado por Chrome. Medir/portar: acrescentar lsevents ao mesmo despejo (o atributo ja esta no HTML do delta-update, e o coletar.mjs prova que ele existe em 1049 de 1713 controles), com teste puro sobre um raw do POC_webgui_its_lib. Abre a porta para acionar(sessao, alvo, { evento }) — acionar pelo que a tela DECLARA, em vez do action/3 fixo do batchAcionar.
> Feito: controlesDoHtml/controlesDoDelta despejam lsevents (JSON parseado, null quando nao publica), e o JS_DESPEJO_CONTROLES do webgui.mjs ganhou o mesmo campo — as duas vias no mesmo formato. Medido 05/09/2026 sobre bruto ja em disco (sem rede): 334 de 1532 controles dos 4 raws do POC_webgui_its_lib publicam lsevents e o parser acha 334 de 334 (grep bate 1:1 em cada raw); na MESMA SE38, XML x DOM casado por id, o mapa evento->comando e IGUAL em 56 de 56 e diferente em 0; forma array[2] em 872 de 872 pares. Achado: comando que JA VEM ENDERECADO com o SID de OUTRO controle (ActivateHelp do ct=MB publica action/1/wnd[0]/sbar) — virou o item 71, que e o acionar(sessao, alvo, { evento }). Commit adt-tools b575904; medicao em sap-accelerate/work/POC_webgui_its_lib/medicoes/item44-lsevents-http.md; script cruzar-lsevents.mjs na mesma POC; receita-webgui.md atualizada.

- [x] 45. Exportar o ALV do WebGUI por ARQUIVO — a via que o item 25 deixou por medir. O lerGrid ja resolve a LEITURA (fragments=0,<total-1> traz a tabela inteira num POST), entao isto nao e para extrair dado: e para saber se o canal alcanca o gesto de exportacao do proprio ALV, que o usuario final usa. Duas pistas medidas em 04/09/2026 (s4h 758/250, POC_webgui_grid/medicoes/item25-grid.md): o lsevents do grid publica CopyToClipboardRequest: [{},{"15":true}], e o ALV tem o caminho de menu Exportar -> arquivo local. Os dois desembocam numa via de SAIDA que o canal nao tem (item 13) — medir se ha alguma, e o que acontece quando se dispara o CopyToClipboardRequest pela via HTTP pura.
> MEDIDO no s4h 758/250 (05/09/2026): o canal TEM via de SAIDA — o ITSDoc, um dialogo fora do batch/json em .../bc/gui/sap/its/webgui/<n>/data/<id>~<verbo>. O gesto e o botao File local... (wnd[0]/tbar[1]/btn[45], CTRL_SHIFT_F9) da propria barra do ALV; o radio de formato se marca com action/4 e quem confirma e vkey/0/ses[0]. O laco e: ler arrITSDocParams do delta -> POSTar o verbo (query?RetQuery= / filesavedialog / get / clipboardexport) -> devolver o controle com okcode =OK + vkey/0 + state/ur. Baixados: nao-convertido 1,08 MB, tabuladores 182 KB (TSV), html 6,76 MB em 2 partes, clipboard 1,07 MB. Cruzado com o lerGrid na mesma sessao: 1617 de 1617 nomes iguais, 0 diferentes, 68,3x menos bytes. Sobre a 2a pergunta: o CopyToClipboardRequest NAO tem via HTTP e nao e falta de descoberta — os 17 eventos do grid publicam 0 comandos no lsevents, o renderer marca submit=false (monta a string do DOM e chama navigator.clipboard) e o mapa RGACTIONS do GuiGridView (33 acoes) nao tem nenhuma de copiar/exportar. Virou codigo: itsdocDoDelta, pedidoDoItsdoc, OK_ITSDOC, FORMATOS e exportarLista no its.mjs (+3 testes puros com script-call real). Medicao: sap-accelerate/work/POC_webgui_export/medicoes/item45-exportar.md; receita-webgui.md § Exportar a lista por ARQUIVO. Achados novos: fila 72 (ITSDoc de ENTRADA) e 73 (formato Planilha eletronica).

- [x] 46. O lerGrid do lado do NAVEGADOR (webgui.mjs) — o par do lerGrid que o item 25 poe no its.mjs. Medido em 04/09/2026 (s4h 758/250, POC_webgui_grid fases A-C) que pelo DOM o alcance e OUTRO: a lista do RSPARAM traz 166 linhas no DOM (grid#C102#1,1 ate #166,5), nao as 27 da janela — so 27 <tr> tem altura, o resto esta la com o texto e altura zero (scrolling client, clientCellThreshold 10000). Entao um lerGrid de navegador le o BLOCO de graca, sem rede. Passar do bloco e que exige gesto: medido que clique sintetico na celula e PageDown NAO moveram a janela nem geraram uma requisicao, e quem moveu foi a RODA do mouse (Input.dispatchMouseEvent type mouseWheel, 12 rodadas levaram a janela de 0..26 a 120..146). Decidir se o webgui.mjs ganha lerGrid do bloco, e se rolar por roda vale a pena tendo a via HTTP.
> DECIDIDO por medicao (s4h 758/250, 05/09/2026, POC_webgui_grid fases I-L). (1) SIM ao lerGrid do bloco: virou lib em webgui.mjs (jsBlocoDoGrid, linhasDoBloco, lerGrid) — 166 linhas / 830 celulas em 19 ms na pagina e ZERO requisicao, contra 1,28 MB e 246 ms da via HTTP; as 830 celulas batem campo a campo com o its.lerGrid. Existe porque e a UNICA leitura de celula que a sessao do navegador tem (a sessao HTTP e outra, nao ve filtro/drill-down). parcial:true quando bloco.ate < total. (2) NAO a rolar por roda para ler: e o MESMO action/710 ao MESMO preco (8,97 KB gzip por 28 linhas nos dois canais, fase K), em fatias de 28 e a ~2,9 s por rodada — ~222 rodadas / ~11 min contra 2,3 s do its.lerGrid. Achado: o bloco CRESCE e nao desliza (1..166 -> 1..362 em 30 rodadas, sem buraco). Medicao: sap-accelerate/work/POC_webgui_grid/medicoes/item46-webgui-grid.md. Abriu os itens 74 (o 710 de dentro do navegador) e 75 (posicionar o ALV em linha distante).

- [x] 47. Grid do WebGUI: ESCREVER em celula, ordenar, filtrar e selecionar linha — o que o item 25 nao tocou. O RSPARAM e editable:false, entao escrita em celula nao tem medicao nenhuma. O lsevents do grid (medido em 04/09/2026, POC_webgui_grid/medicoes/raw/a-dom.json) publica CellSelect, BlockSelect, ClientRowSelect, ClientColumnSelect, ColumnResize, CellDoubleClick [{},{"2":true}], CellExpand/CellCollapse, CellContextMenu e Hotkey — e nenhum foi exercitado. O lsdata do grid ja entrega o estado da selecao (selectedRows, selectedCells, selectedBlock, currentCellRow/Column, selectionMode). Achar um ALV editavel no s4h e medir o ciclo escrever->gravar->conferir em outra LUW.
> Escrita em celula do ALV do WebGUI medida ATE O BANCO. O gesto: clicar troca o <span ct=CBS> por um <input> de mesmo id; o BLUR enfileira action/622 (row_index + column_id pelo NOME + value) e quem manda e o round-trip seguinte. MODO DE FALHA SILENCIOSO medido por contra-prova pareada: sem publicar saem 0 action/622, o ABAP responde "GRAVOU subrc=0" e a tabela em OUTRA LUW fica inalterada — a mensagem de sucesso nao e prova. Corrigido um bug calado do item 46 (celula em edicao lia "" porque o lsdata[21] vira string e o innerText de <input> e vazio). Como nenhum ALV editavel do sistema grava em banco (fase B, pelo fonte), o ciclo com LUW saiu de laboratorio proprio: ZJBV_ALV47 + ZJBV_ALV47_EDIT em $TMP (ALV em docking container sobre a tela de selecao — dispensa dynpro), que FICAM no s4h para os itens 76-78. Lib: escreverCelula, escolherGrid, indiceDaColuna, lerGrid().editando (adt-tools ce2ee99, 54 testes verdes). Medicao: sap-accelerate/work/POC_webgui_grid_edit/medicoes/item47-escrever-celula.md. Abriu 76 (selecionar), 77 (ordenar/filtrar), 78 (inserir/apagar linha), 79 (colar em lote) e 80 (custo escondido do comandar).

- [x] 48. Item de menu DESABILITADO no WebGUI — nunca foi visto, e a leitura de item cinza segue nao medida. Achado do item 26 (s4h 758/250, 04/09/2026): nos 121 itens de menu varridos da SE38, aria-disabled veio "false" em 20 e AUSENTE nos outros 101 — nenhum "true". Por isso interpretarItemDeMenu devolve habilitado: null para "a tela nao disse", e null NAO e habilitado. Falta achar uma tela com item de menu cinza (candidatos: Programa > Gerar sem programa carregado; menu numa lista ALV; item que exige objeto selecionado), medir o que o DOM carrega (aria-disabled? classe? lsdata?) e so entao decidir se habilitado sai do ARIA ou de outro lugar. Enquanto nao medir, quem ler habilitado===false esta lendo um valor que nunca foi produzido.
> MEDIDO (s4h 758/250, 05/09/2026, 279 itens de 5 telas): a habilitacao do item de menu sai do lsdata[5] — `false` = desabilitado, AUSENTE = habilitado (o lsdata so transporta o que difere do default, como `4` e `6`). Achados 7 itens cinza (SU01 Usuario>Gravar/Renomear, Easy Access Processar>Criar ligacao no desktop e Suplementos>Atribuir usuarios, SE16 Legenda de cores, SM37 Selecao de job simples, SU01 Log de distribuicao); a SE38 nao tem nenhum — era azar de amostra. O aria-disabled="false" que enganou o item 26 e o REALCE do popup (47/47 batem com a classe urMnuRowOn), nao habilitacao. O cinza NAO esta na cor: cor computada identica no habilitado e no desabilitado; a marca visual e so a classe urMnuRowDsbl. Contra-prova no mesmo popup: clicar no cinza deixa o menu ABERTO e o carimbo igual; o irmao habilitado fecha o menu e muda a tela. Lib: interpretarItemDeMenu le lsdata[5] com o ARIA de confirmacao e NAO devolve mais null; navegarMenu LANCA ao topar com item desabilitado (provado ponta a ponta). 54 testes verdes. Medicao: sap-accelerate/work/POC_webgui_menu/medicoes/item48-menu-cinza.md; receita: packages/adt-client/docs/receita-webgui.md § O item DESABILITADO. Abriu o item 81 (botao de toolbar desabilitado nunca medido; `desabilitado: !!el.disabled` do JS_DESPEJO_CONTROLES e sempre false no ITS).

- [x] 49. O MENU pela via HTTP pura do ITS — o comando nao esta derivado. Achado do item 26 (s4h 758/250, 04/09/2026): o POMNI NAO publica lsevents (null em 121/121); quem publica o Select e o POMN pai — {"1":"action/4","2":true} no submenu e {"JScript":"sap.g4h.doWguMenuSelect(oCfg)"} na raiz. Ou seja: pela via sem navegador (its.mjs) o menu ainda nao e acionavel, e navegarMenu so existe no CDP. action/4 e um dos comandos que a receita lista como AINDA NAO POSTADOS (mesma familia do item 43). Medir: o action/4 leva o SID do item de menu? abrir o menu pela via HTTP muda a tela (o menu vem no delta-update)? Se levar, o caminho de menu vira navegacao sem Chrome.
> PROVADO no s4h 758/250 (05/09/2026): action/4 LEVA o SID do item de menu e navega — action/4/wnd[0]/mbar/menu[5]/menu[3]/menu[0] levou SE38 (SAPLWBABAP) a SA38 (SAPMS38M) em 76 ms, um POST, sem Chrome. A 2a pergunta estava mal posta: NAO ha menu a abrir nesta via — o boot da SE38 ja devolve os 146 POMNI de wnd[0]/mbar em 3 niveis (o id e o SID e o caminho); quem materializa sob demanda e o renderer, o delta-update transporta tudo. Contra-provas: action/3 no MESMO SID volta -101 not supported (a familia e a do 4); action/4 em no COM submenu e aceito e inocuo (146 menus antes e depois) — abrir e gesto de UI, nao de protocolo; action/3/wnd[0]/tbar[0]/[0] (clicar cua2sapmenu_btn) volta -102 control not found. A guarda do item 48 (lsdata[5]===false) vale igual e custa ZERO rede: 11 dos 146 ja vieram no delta. Na lib: its.mjs ganhou itensDeMenuDoDelta, itensDeMenu(s,{sob}), acharCaminhoDeMenu e navegarMenu — reusando interpretarItemDeMenu/partirCaminhoDeMenu/acharItemDeMenu/filhoDiretoDeMenu/daBarraDeMenu do webgui.mjs (um modelo para as duas vias). E2E: descobrir 27 ms e zero POST, navegar 91 ms e um POST, guarda do cinza e caminho inexistente lancando com a lista do que ha. action/4 sai da lista dos nao-postados (item 43). Medicao: sap-accelerate/work/POC_webgui_menu/medicoes/item49-menu-http.md; receita-webgui.md ganhou o paragrafo "O mesmo menu pela via HTTP pura". Abriu 82 (a arvore esta no DOM do CDP invisivel?) e 83 (folha que abre popup).

- [x] 50. A arvore do SAP Easy Access (ct=TV + MG) — o menu de USUARIO, nao tocado. Achado do item 9 e reconfirmado no 26 (s4h 758/250, 04/09/2026): MG (role=group, 15 controles, id tree#C105#n#1#mg, texto "Favoritos" / "Menu SAP" / "SAP Fiori Launchpad") e TV (role=button, 30 controles, id tree#C105#n#1#1#i) formam a arvore de navegacao do SAP Easy Access — outro caminho de menu, separado da barra que o item 26 mediu. O TV publica DoubleClick: {"0":"GuiTree","1":"action/74","13":2} e o lsdata traz [0] rotulo, [2], [7] INHERIT, [14], [15], [17] {ctmenu, focusable}. Medir o vocabulario, como se EXPANDE um no e provar um acionamento (chegar numa transacao pela arvore de favoritos/menu SAP). Vale porque e o caminho que o usuario final descreve, e o unico que enxerga os FAVORITOS.
> A arvore do SMEN se enderece pela CHAVE, no CONTAINER — e o lsevents dela MENTE. Medido no s4h 758/250 (05/09/2026, POC_webgui_arvore/medicoes/item50-arvore.md): o action/74 que o TV publica devolve -102 control not found (o id tree#... e do renderer, o servidor nao o conhece) e action/74 no SID do container devolve -101. Capturado do Chrome, o gesto real e action/2/<SID do container> com content type=OnNodeDoubleClick&node_key=<K>; a chave sai do nodeindexes do lsdata do STCS ([chave, categoria, indiceDoPai]), e o indice do id tree#C105#<n>#1#1#i e a posicao nessa lista. EXPANDIR e o MESMO gesto (num no com filhos o duplo clique abre: 15->22 nos) ou o action/8 (CellExpand do container) com type=node; type=OnNodeExpand nao existe (-132). ⚠ o indice n e POSICIONAL — expandir reindexa; so a chave e estavel. ⚠ o acionamento pode levar 15,5 s (transacao fria) e estourou o teto de 30 s do postar: TETO_ARVORE = 120 s. PROVA: Menu SAP > Escritorio > Agenda > Proprio -> SSC1 em 2561 ms (2 expansoes + 1 acionamento) e Favoritos > Producao -> CO01 em 386 ms, um POST. Entregue na lib: arvore/arvoreDosBrutos/containerDaArvore/indiceDoNo/acharNoDaArvore/batchExpandirNo/batchAcionarNo/expandirNo/acionarNo/navegarArvore no its.mjs, 5 testes puros novos (820 passam), receita § A ARVORE do SAP Easy Access. Abriu #84 (flag de tem-filhos), #85 (colapsar) e #86 (a arvore pelo navegador).

- [ ] 51. Provar por MUTACAO CONTROLADA que no ICF desativado responde 404 — a parte que o item 27 nao pode medir sem alterar o sistema. Medido em 04/09/2026 (s4h 758/250) que o 404 tem tres causas e o HTTP nao separa nenhuma: no ausente (is_service_active levanta INVALID_URL), no ATIVO sem handler na ICFHANDLER (o /sap/bc/gui/sap/its/test, com 5 filhos e zero handler — e tambem /sap, /sap/bc, /sap/public) e desativado, que segue SEM prova porque no s4h nenhum no inativo e enderecavel pelo host padrao (o unico e o /ZLHSGW_VOOS/sap/public, de outro virtual host). Ciclo a rodar: GET no /sap/bc/gui/sap/its/ewm_mobgui (hoje 200, 1370 B, WebGUI do EWM mobile — ninguem usa no laboratorio) + is_service_active, depois CL_ICF_TREE=>DEACTIVATE_NODE, medir os dois de novo, ACTIVATE_NODE e provar a restauracao pelos MESMOS dois. E a unica alteracao de configuracao do s4h de toda a POC — adiada pelo Joris em 04/09/2026 para 05/09/2026. Contexto e medicoes: sap-accelerate/work/POC_icf_estado/medicoes/item27-estado-do-no-icf.md
> bloqueado: ate 05/09/2026 08:00: mutação de configuração do s4h adiada pelo Joris em 04/09/2026

- [x] 52. Parar o retry quando a sessao NASCEU MORTA — o aviso do item 28 e so log. O fetchToken ja detecta (sessaoNasceuMorta: logon 200 com token mas cookie sem SAP_SESSIONID), mas quem faz laco segue insistindo: deployAndRun tenta 5 vezes com 3s, e no estado doente CADA tentativa soma uma sessao que o logoff nao remove (medido 04/09/2026: logoff da 400 e a contagem nao cai). Fazer o laco abortar com erro nomeado em vez de gastar as 5 tentativas, e varrer os outros lacos de retry da lib pelo mesmo criterio.
> Guarda na RAIZ, nao em cada laco: fetchToken grava o veredito na sessao (session.nasceuMorta), call RECUSA a requisicao antes do fetch (a requisicao que nao sai e uma sessao a menos somada ao teto) e abrirLogon lanca em vez de devolver sessao que parece boa. Erro nomeado SessaoNasceuMorta (code SESSAO_NASCEU_MORTA, ehSessaoMorta(e)), exportado pelo barrel, com mensagem que diz o que NAO adianta (insistir, logoff) e o que resta (esperar http/security_session_timeout, SM04/TH_USER_LIST). deployAndRun: para na 1a onde gastava 5x3s, e o load antigo da classe (motivo real do laco) segue com as 5 — os dois com teste (classrun.test.mjs, novo). Quem decide e o veredito do LOGON, nunca o formato do cookie: sessao do cache do connect passa direto (teste de regressao). Varredura: todo laco que passa por call fica coberto na raiz e nenhum captura; probe/canais capturam de proposito e agora relatam o motivo nomeado; webgui/fiori/gui sao CDP e GUI Scripting, nao sessao HTTP. O estado doente NAO foi reproduzido no S4H de proposito (deixaria o laboratorio inutilizavel por 1800s) — coberto por ICM de mentira; a contra-prova real foi a sessao saudavel, que segue passando. 826 testes verdes. adt-tools 32dd221; medicao em sap-accelerate/work/POC_sessao_morta/medicoes/item52-guarda-sessao-morta.md. Abriu 87 (connect cacheia sessao morta) e 88 (sonda do WebGUI nao nomeia o teto).

- [ ] 53. Bissectar o teto de sessoes HTTP e descobrir se e POR USUARIO ou GLOBAL. O item 28 mediu a faixa (144 passavam, 154 nao) mas nao o numero, e mediu com um usuario so — no momento da quebra havia 179 sessoes no sistema contra rdisp/tm_max_no=1000, entao o teto global declarado NAO e o que se atinge. Descobrir que recurso esgota (candidatos nao medidos: sessoes de seguranca HTTP da SICF_SESSIONS, entradas por usuario, contexto do ICM) e se um segundo usuario continua entrando com o primeiro no teto. Custo: cada rodada envenena o sistema por ~30 min (o s4h e compartilhado — combinar janela com o Joris).
> medido: M8 do item 28 estreita a pergunta: o canal voltou 26 min depois da quebra (19:52:30 quebrou, 19:52:41 as 150 sessoes fecharam, 20:18:07 o cookie voltou com SAP_SESSIONID) e voltou com 30 sessoes minhas na TH_USER_LIST — MAIS do que durante os 15 min em que esteve quebrado. Logo a contagem da TH_USER_LIST NAO e o recurso que esgota: o logoff limpa a lista e nao encurta a cura. Procurar o recurso em outro lugar (sessoes de seguranca HTTP da SICF_SESSIONS, contexto do ICM).
> bloqueado: ENTREGUE SEM ENVENENAR (05/09/2026, s4h 758/250) a pergunta "que recurso esgota": é a sessão de segurança HTTP NÃO USADA por usuário — linha em SECURITY_CONTEXT com TIMEOUT_CHECK=2, com EOL FIXO de start+1800 s gravado na criação, que o logoff NÃO remove. É esse EOL que explica a cura por tempo do M8 do item 28. Fonte medida: toda requisição STATELESS autenticada cria exatamente 1 (20 SOAP = +21; 20 GETs em /sap/public/ping = +1, só a própria sonda); um GET no WebGUI, ao contrário, cria sessão USADA (+10 usadas por 10 GETs), que o logoff derruba. Refutados por medida os outros dois candidatos do item: contexto do ICM (GET_CONTEXT_CACHE_STATISTICS = max 2500 / used 15) e SEC_CONTEXT_COPY (9->19->9, limpa 100%). Teto: DTEL SECURITY_SESSION_LIMIT_UNUSED diz "per User", configurável em ICF_SESSION_CNTL (VAZIA aqui), default DECLARADO 100 em CL_HTTP_SECURITY_SESSION=>CO_SESSION_LIMIT_UNUSED_DFLT — a constante não é usada em ABAP, quem aplica é o kernel. E a cura instantânea existe: ABORT_SECURITY_SESSION filtrado por timeout_check=2 fez 42 sessões virarem 2, 40 de 40 sem erro. Medição: sap-accelerate/work/POC_sessoes_icf/medicoes/item53-o-recurso-que-esgota.md (scripts i53-a..n). Nada foi derrubado: medido sempre abaixo de 45 sessões, contra os 144 que o item 28 provou seguros. PERGUNTA AO JORIS, para o que sobra (o teto EFETIVO e o per-usuário-x-global): as duas metades que faltam exigem chegar ao teto, e para isso preciso de (a) uma janela combinada no s4h e (b) um SEGUNDO login no s4h — o .env só tem SAP_S4H_USER, e sem um segundo usuário a pergunta "por usuário ou global" não tem como ser medida, só inferida do DDIC. Antes da janela vale fazer o item 89 (curar por canal stateless): com ele a rodada custa segundos em vez dos 30 min, e o risco de derrubar o sistema compartilhado cai para o do próprio usuário.

- [x] 54. Onde MAIS o TSTCC S_WEBGUI pesaria? A contraprova do item 29 (receita-tran.md § A contraprova do TSTCC S_WEBGUI) mediu SO o caminho da URL ~transaction: com S_WEBGUI vazio a transacao abriu igual. Faltam os outros dois caminhos de entrada do WebGUI, no mesmo par de transacoes descartaveis (YJBV_WGON/YJBV_WGOFF sobre RSPARAM, s4h 758/250): (1) /n<TCODE> por comandar() DENTRO de uma sessao ITS ja aberta; (2) o menu SAP Easy Access do WebGUI (a transacao aparece/roda pela arvore?). Se nenhum dos tres olha o flag, o gui.html vira so fidelidade a SE93 e a receita pode dizer isso sem ressalva. Apagar as duas no fim.
> Medido no s4h 758/250 em 05/09/2026 — os TRES caminhos de entrada do WebGUI abrem a transacao com TSTCC S_WEBGUI vazio, e os tres sabem recusar (cada um com sua mensagem, em momento diferente): (1) URL ~transaction (item 29, HTTP 500 no controle); (2) /n<TCODE> por comandar() numa sessao ja aberta — 119 ms com o flag, 103 ms sem, e o controle inexistente fica no SMEN com statusbar "Transaction … does not exist"; (3) o menu SAP Easy Access — a transacao $TMP NAO aparece na arvore (igual nos dois, nao distingue), entao mediu-se pelo FAVORITO: Favorites > Insert Transaction -> popup -> enter -> acionarNo, 92 ms com o flag e 108 ms sem, e o controle e recusado JA na insercao (arvore 15 -> 15). Veredito: o gui.html do deployTransaction e fidelidade a SE93, nao pre-requisito do canal — a receita perdeu a ressalva. Dois achados de tabela: o action/41 (a selecao isolada, aberto do item 50) funciona e e ELE que habilita o menu que age sobre o no; e o SID do container da arvore nao sobrevive ao /nSMEN na mesma sessao (-103 not available; sessao nova apaga em 165 ms) -> itens 91-93. Limpeza confirmada: favoritos de volta a 15 nos sem YJBV, as duas transacoes apagadas (subrc 0, TSTC/TADIR/TSTCC ausentes). Medicao: sap-accelerate/work/POC_webgui_tstcc/medicoes/item54-tstcc-caminhos.md; receitas receita-tran.md e receita-webgui.md atualizadas.

- [ ] 30. A transacao YJBV4029823 pode ter ficado de lixo no SXD 816/100. Criada em 03/09/2026 pela POC 4029823 (scripts/criar-tran.mjs, report YJBV_POC_R_4029823_J1B1N, C:UsersJORISB~1.VELAppDataLocalTemp) e a POC nao tem passo de limpeza — a spec so decidiu nao limpar as NFs criadas, nada sobre a TRAN. Conferir por readTransaction(cfgSXD, YJBV4029823) e, se existir, apagar por deleteTransaction. Exige acesso ao SXD (a POC rodou com VPN); enquanto nao houver, o item fica aberto. Objeto de repositorio no sistema de um cliente nao pode ficar largado — e a propria receita-tran.md § O par tran + WebGUI diz isso.
> estado: Tudo pronto para o delete. Adiado DUAS vezes pelo Joris ('deixa pra depois' em 04/09/2026 e 'joga para o final da fila' em 04/09/2026) — por isso saiu de 'em andamento' e foi para o fim da fila; retomar so quando ele mandar. Estado: TRAN YJBV4029823 CONFIRMADA no SXD 100 (TSTC pgmna=YJBV_POC_R_4029823_J1B1N dypno=1000 cinfo=8; TSTCT PT; TADIR TRAN $TMP autor JVELOSO); ha acesso ao SXD. Script pronto e versionado fora do git (work/ e ignorado) em work/POC_4029823_j1b1n/scripts/limpar-tran.mjs: deleteTransaction + readTransaction em OUTRA LUW como contraprova. Proximo passo: perguntar de novo ao Joris e, com o sim, rodar node work/POC_4029823_j1b1n/scripts/limpar-tran.mjs e conferir exists=false. O delete nao toca o report YJBV_POC_R_4029823_J1B1N nem as NFs 73-91.
> bloqueado: Posso apagar a TRAN YJBV4029823 do SXD 816/100? (3a vez que pergunto — adiada em 04/09 duas vezes; nao rodo o delete sem o seu sim explicito.) MEDIDO HOJE 05/09/2026, so leitura: a TRAN EXISTE. O que se perde, exato — TSTC: PGMNA=YJBV_POC_R_4029823_J1B1N, DYPNO=1000, CINFO=8; TSTCT (P): "POC 4029823 - copia NF pela J1B1N"; TADIR: TRAN/$TMP/autor JVELOSO. TSTCP: nao existe (null). O delete NAO toca o report YJBV_POC_R_4029823_J1B1N nem as NFs 73-91. Custo de errar e BAIXO: $TMP nao e transportavel (nenhuma request envolvida) e o par criar-tran.mjs recria em segundos — o "irreversivel" aqui e barato de desfazer. Conferido tambem que a divergencia entre a lib do node_modules do sap-accelerate e a canonica packages/adt-client e SO um comentario (o achado do item 29): deleteTransaction e funcionalmente identico, entao o script pronto roda igual pelas duas. Com o seu sim: node work/POC_4029823_j1b1n/scripts/limpar-tran.mjs (deleteTransaction + readTransaction em OUTRA LUW como contraprova, esperado exists=false). Contra-argumento a favor de apagar, para voce pesar: a receita-tran.md § regra 5 diz que no sistema do cliente apagar NAO e opcional — a transacao e objeto de repositorio e aparece na SE93 de todo mundo.

- [x] 55. A mesma armadilha do item 31 pode estar nos OUTROS gestos do NAVEGADOR — medir quais levam os valores da dynpro. Provado em 04/09/2026 que o valor so viaja depois que o campo perde o foco (o Change do controle e quem faz addBatch({post:"value/"+SID}), e Input.insertText nao o dispara); o comandar ja publica o foco sozinho, mas tecla(s, "F8"), clicar em alvo que NAO seja botao, navegarMenu e abrirMenu nao foram medidos com campo preenchido. O gesto do botao (acionar) esta medido que leva (controle positivo G4: 1 acertos) — porque o clique tira o foco. Rodar o MESMO ciclo da POC_webgui_okcode_valores (SE16 sobre a T000, MTEXT=Neduca -> 1 acerto; sem filtro -> 5) para cada gesto, e publicar o foco em quem perder. Vale porque um gesto que perde valor em silencio nao da erro: a tela executa e devolve a tabela inteira.
> NENHUM gesto perde o valor — a armadilha do item 31 e SO do comandar. Medido no s4h 758/250 em 05/09/2026 (sap-accelerate/work/POC_webgui_gestos_valores/, fases A-D, medicoes/gestos-valores.md), mesmo ciclo do item 31 (SE16/T000, MTEXT=Neduca -> 1 acerto; sem filtro -> 5) com a linha de base repetida NA RODADA (comandar cru sem valor = 5, preencher+comandar cru = 5, preencher+acionar(btn[8]) = 1). Deram TODOS 1 acerto: tecla(F8), clicar em outro campo, clicar em rotulo <L> (descer:false), clique cru por coordenada em area inerte (foco vai ao BODY), abrirMenu, abrirMenu+fecharMenu e navegarMenu(Programa > Executar). Nos gestos que nao executam sozinhos o executor foi o comandar CRU (publicarValores:false), que a base mede perdendo — logo o acerto veio do gesto. Bissecao com listener no campo (fase D): sao DOIS mecanismos — o gesto de MOUSE tira o foco e o blur publica (sai POST isolado [{post:value/...,logic:ignore}]); o gesto de TECLA NAO blura e leva assim mesmo, porque o vkey sai endereçado ao CONTROLE (vkey/8/wnd[0]) com o value/ junto, ao contrario do vkey/0/ses[0] do submitOkCode, que e da SESSAO. O change NAO discrimina (apareceu nos 3, inclusive no que perdeu). Razao de fundo: comandar e o unico gesto do canal que NAO e nativo — despacha KeyboardEvent sintetico noutro elemento (ToolbarOkCode, 0x0), entao o campo nem perde foco nem recebe evento. Nada a corrigir no comportamento; entrou a regra medida em webgui.mjs (§ do comandar) e em docs/receita-webgui.md (§ Os OUTROS gestos nao tinham a armadilha). Achado colateral -> item 94 (fecharMenu devolve false e gasta 15,5 s apos abrirMenu).

- [x] 56. Por onde sai o trafego do WebGUI que o hook no XMLHttpRequest NAO ve. Medido em 04/09/2026 (POC_webgui_okcode_valores, fase B): um cenario mandou T000 ao servidor e a tela avancou, com ZERO value/ capturado por um hook em XMLHttpRequest.prototype.send instalado por Page.addScriptToEvaluateOnNewDocument (que roda ANTES dos scripts da pagina) — o mesmo trafego apareceu inteiro no CDP (Network.requestWillBeSent com postData). Nao sei a causa e nao vou palpitar: candidatos a medir sao fetch, sendBeacon e uma referencia ao send guardada antes do hook. Importa porque hook em XHR e a instrumentacao obvia deste canal e ela produziu uma leitura FALSA que quase virou conclusao — quero saber se o CDP e obrigatorio ou se o hook pode ser consertado.
> A PREMISSA DO ITEM ERA FALSA: o hook no XMLHttpRequest NAO e cego e nao ha canal invisivel. Medido no s4h 758/250 em 05/09/2026, tres fases. Fase J: hook AMPLO (xhr.send, fetch, sendBeacon, WebSocket.send, form.submit, evento submit, beforeunload) por Page.addScriptToEvaluateOnNewDocument, lido em TODOS os contextos de execucao vivos, contra o CDP no mesmo cenario — leituras IDENTICAS nos dois casos, e no cenario que "vazava" o CDP TAMBEM viu zero value/ (1 POST: focus + action/304 + vkey/0 + state/ur, sem value/ e sem okcode/). Tudo type=XHR, initiator=script, pilha em webgui_min.js:147/162; bate com o fonte (7 XMLHttpRequest, ZERO fetch(, ZERO sendBeacon, ZERO .submit(); o batch sai por um XHR singleton criado num IIFE). A causa real sao duas, medidas: (1) CONTAMINACAO DE ESTADO — a SE16 reabre com a ultima tabela do usuario ja no campo mesmo em sessao nova do ITS, entao a tela avancou para T000 sem nada sair no fio (fase K, teste diferencial: mandei TSTC de verdade, digitei T000 sem publicar, e o contrafactual K3 "nao digita nada, so Enter" chegou a MESMA tela com zero value/); (2) A LEI DO value/ — o renderer publica value/<SID> quando o valor ATUAL difere do que o servidor mandou naquela dynpro; e comparacao com o ESTADO, nao "houve digitacao" (fase L, cinco cenarios com previsao declarada antes, cinco bateram: mesmo valor + Enter nao publica; valor diferente publica; digitar ZZZZ e VOLTAR ao original nao publica; o blur do comandar({publicarValores:true}) obedece a mesma lei — e nao publicar com valor igual esta CERTO, o servidor ja tem). Resposta a pergunta do item: o CDP NAO e obrigatorio para ver este canal, e o hook nao precisa de conserto — mas o CDP segue preferido por ser externo a pagina (sobrevive a troca de documento, que mata o window.__ do hook) e por dar initiator.stack; se usar hook, leia-o em TODOS os contextos (Runtime.evaluate sem contextId le so o default do frame de topo). Corrigido onde a afirmacao falsa estava escrita: receita-webgui.md (armadilha 1 riscada + novo § com a lei do value/ e a contaminacao) e a medicao do item 31. A licao virou edicao da skill investigacao (dois bullets em Disciplina transversal: "cenario em sequencia nao e cenario independente" e "instrumento e inocente ate prova em contrario"). Achado novo virou o item 95. Medicao: sap-accelerate/work/POC_webgui_okcode_valores/medicoes/item56-hook-xhr.md

- [x] 57. IDEIA (achado do item 32, 04/09/2026): EXPANDIR no where-used o no colapsado. Medido no SXD 816: no com canHaveChildren="true" (FM, include, classe, pacote) vem sem os usos de dentro, e por isso o numberOfResults anunciado nao bate com o expandido (J_1BNFNUM_UTILITIES: 12 anunciados x 5 expandidos, 11 colapsados). A lib ja SINALIZA (colapsados/completo em whereUsed), mas nao expande — expandir e outra chamada, formato NAO MEDIDO (provavel POST usageReferences com a uri do no, ou o mesmo endpoint com affectedObjects preenchido). Fechar isso e o que torna o where-used de FM/include util de verdade: hoje os usos em CODIGO sao justamente os que chegam colapsados.
> FECHADO 05/09/2026 (s4h 250, POC_whereused_expandir): expandir e OUTRO endpoint — POST /sap/bc/adt/repository/informationsystem/usageSnippets, anunciado no discovery ao lado do usageReferences. As duas suposicoes do item estavam erradas (nao e a uri do no, nem affectedObjects). A chave e o <objectIdentifier> de DENTRO do no colapsado, e vai LITERAL: sem a contrabarra o servidor responde 200 com lista VAZIA (contraprova nos dois sentidos). Um identifier rende N snippets (14 anunciados x 22 ocorrencias no J_1BNFNUM_UTILITIES) — numberOfResults conta objetos-uso, nao linhas. Nem todo colapsado e buraco: ou tem identifier (expansivel) ou e container cujos filhos vieram na mesma arvore (zero excecoes em 4 objetos, ate 13.710 nos) — o alarme de parcial passou a olhar os expansiveis. Custo por CHAMADA: 10 ids 35s, 200 48s, 500 142s, 2000 falha; dai lote=200 e maxExpandir=500. Na lib: whereUsed(s, uri, { expandir: true }) devolve usos com arquivo/linha/coluna/trecho, expandirUsos + parseUsageSnippets, refs[].id, expansiveis/containers no retorno. J_1B_NF_OBJECT_CHECK ia de 0 usos localizados para 6, completo:true. Commit adt-tools 2e7c124; medicao sap-accelerate/work/POC_whereused_expandir/medicoes/item57-expandir.md.

- [x] 58. Medir comandar() com o SPOP MODAL aberto na via HTTP — a forma exata da trava do item 13. Em 05/09/2026 (item 23, passo D) o /nSE38 PASSOU com o popup "Sessões ABAP" (/o) aberto: cuatitle virou Editor ABAP, tcode SE38, popup sumiu — enquanto no navegador (item 13) o okcd da wnd[0] parou de postar assim que getPopupCount() foi a 1. Mas o /o tem barra propria (tbar[0] na wnd[1]); o SPOP (Sim/Nao, sem btn[n]) e um modal mais duro e NAO foi testado com comandar. Medir: abrir SE16, /nend (SPOP), comandar(/nSE38) — passa ou trava? Se passar, a via HTTP nao tem a trava do navegador e isso e vantagem do canal; se travar, o comandar precisa fechar o popup antes.
> MEDIDO no s4h 758/250 em 05/09/2026: a via HTTP NAO tem a trava do navegador, nem com o modal duro. Com o SPOP de /nend aberto (11 SIDs na wnd[1], ZERO btn[n] — so usr/btnSPOP-OPTION1|2 — e o okcd da wnd[0] ainda enderecavel), comandar(/nSE38) voltou delta/pegou:true em 170 ms, cuatitle "Editor ABAP: 1a tela", tcode SE38, popup sumido; reproduzido SE38->SE16 em 86 ms. O SPOP era o de LOGOFF e a sessao continuou aberta (s.aberta=true, mais um state/ur sem acao confirmando o estado estavel): o modal foi DESCARTADO, nao respondido "Sim". Logo o comandar NAO precisa fechar o popup antes — e vantagem medida deste canal sobre o navegador (item 13). Nao medido: SPOP com campo obrigatorio, ou popup de erro que a dynpro reponha. Medicao: sap-accelerate/work/POC_webgui_popup/medicoes/spop-comandar.md (script spop-comandar.mjs, brutos i58-*). Registrado na lib: its.mjs (doc do comandar) e docs/receita-webgui.md. Achado lateral virou item novo na fila.

- [x] 59. O pegou:true e FALSO POSITIVO quando a acao nao muda a tela com popup aberto. Medido em 05/09/2026 (item 23, passo H): vkey(s, 12) (F12/Cancelar) com o SPOP "Efetuar logoff" aberto voltou pegou:true, forma delta, 226 KB — e o popup continuou la (wnd[1], sidsWnd1 11, mesmo cuatitle). A lib diz que pegou e nada aconteceu. Decidir a regra: o pegou deveria comparar a tela ANTES x DEPOIS (dynpro/popup/sids) em vez de so olhar a forma da resposta, ou expor um sinal separado (mudou). Vale pelo menos avisar quando o delta volta com o mesmo popup aberto.
> DECIDIDO E MEDIDO: pegou fica sendo o veredito do PROTOCOLO (a forma da resposta) e ganha um PAR — mudou, o veredito da TELA, por carimbo ANTES x DEPOIS. carimboDosSids = <tcode>/<dynpro>/<d-num> <janela ativa> "<cuatitle>" #sha1(SIDs com lsdata), SEM a barra de mensagem; mudouDaTela mapeia a forma (delta inteiro -> o carimbo decide; boot e delta parcial do ALV -> null; multipart -> false; logoff -> true). Toda resposta do postar passa a trazer mudou, mensagem e carimbo, e sai AVISO ALTO em stderr quando pegou && !mudou && o mesmo popup continua aberto. A barra de mensagem fica FORA do carimbo por medicao: nos brutos do item 23 (SPOP x F12) os 198 SIDs sao identicos e a UNICA diferenca em toda a tela e wnd[0]/sbar_msg — a mensagem e o comentario do ABAP sobre a acao, nao a tela (com ela dentro, o falso positivo voltaria com outra roupa). E ela e o ouro que a lib nao mostrava: o SAP ja dizia "Nao se pode selecionar codigo de funcao" — com messageType OK, tipo de SUCESSO num texto de recusa (messageType NAO e veredito). Medido no s4h 758/250 em 05/09/2026 (POC_webgui_mudou/medicoes/item59-mudou.md): 5/5 pares offline; e contra o sistema A boot mudou=null, B state/ur puro mudou=false SEM aviso (controle, nada de falso alarme), C /nend mudou=true, D+E vkey(s,12) com SPOP pegou=true mudou=FALSE com aviso e mensagem (o caso do item, reproduzido 2x), F botao Nao pelo SID mudou=true, G /nSE16 mudou=true, M action/3 num GuiLabel multipart pegou=false mudou=false com o carimbo da sessao INTACTO. Carimbo estavel: 223 SIDs byte a byte iguais em 2 POSTs na mesma tela, e o hash da SE38 limpa igual entre sessoes e dias. acionarNo e navegarMenu largaram o mudou ad-hoc (so titulo+dynpro) e usam o geral. its.test.mjs +3 testes (44), suite 835 verde. Abriu #99 (navegarArvore { acionar: false } mente no mudou) e #100 (a via do navegador nao tem o par).

- [x] 60. Desligar o pedago do cliente: onde mora a opcao "Um script esta tentando acessar SAP GUI"? Medido em 05/09/2026 (item 34, S4H 250): o servidor tem sapgui/user_scripting_force_notification=FALSE, mas o pad abre um dialogo modal #32770 a CADA GetObject(SAPGUI) e o canal so responde depois do OK. Nao esta em HKCU...SAP Frontend ServerScripting (so WindowPlacement) nem na chave Security do HKLM (UserScripting=1, SecurityLevel=1, DefaultAction=0); varredura por Warn|Notif|Attach sob HKCUSoftwareSAP e HKLM WOW6432Node nao achou nada. Procurar no SapGuiSettings/opcoes do GUI 8.00 e provar por medicao (sonda responde sem popup).
> ACHADO E PROVADO: a opcao e HKCUSoftwareSAPSAPGUI FrontSAP Frontend ServerSecurityWarnOnAttach (REG_DWORD 0) — a checkbox "Notify when a script attaches to SAP GUI" das opcoes do GUI. O item 34 procurou na chave errada: e ...Security, nao ...Scripting, e o valor NAO EXISTE por padrao (ausente = avisar), o que explica as varreduras vazias. Quem entregou o nome foi o PRODUTO, offline e sem tocar em nada: varredura de strings dos 224 .dll/.exe/.ocx do SAPgui em ASCII e UTF-16LE — saplogon.exe/saplgpad.exe/SapGuiServer.exe carregam WarnOnAttach colado a CSapGuiAuto::GetScriptingEngine e ao caminho SoftwareSAPSAPGUI FrontSAP Frontend ServerSecurity (o ponto exato do pedagio), e o XML do SAP Settings Migration Tool embutido no sapsettings.ocx declara UserScripting/WarnOnAttach/WarnOnConnection como REG_DWORD sob essa chave (pagina CScriptingPage, controle SCRP_NTFY_ATTCH_SCRT_CHK). MEDIDO no pad saplogon 13060 (o MESMO do item 34), 05/09/2026: valor ausente = timeout de 25 s com popup "SAP Logon" aos 1,33 s; WarnOnAttach=0 no MESMO pad, sem reiniciar = estado 3 em 1,08 s sem popup; tres sondas seguidas em 0,76/0,85/0,87 s (o pedagio era por chamada — agora nenhuma paga); apagando o valor o popup VOLTA (timeout 20 s, aviso aos 1,29 s) e regravando 0 sai de novo — causalidade nos dois sentidos. WarnOnAttach SOZINHO basta: WarnOnConnection removido e o pedagio seguiu desligado. A maquina ficou com a chave HKCU criada (WarnOnAttach=0, WarnOnConnection=0); reverter = reg delete da chave; o HKLM nao foi tocado. Nao medido: precedencia HKCU x HKLM (o HKLM daqui nao tem WarnOnAttach). Doc: packages/adt-client/docs/receita-gui-scripting.md § Como desligar o pedagio. Medicao: sap-accelerate/work/POC_rot_sapgui/medicoes/item60-warnonattach.md (scripts item60-*.mjs, brutos raw/item60-*.json). A tecnica "perguntar ao binario" virou linha na skill investigacao.

- [x] 61. sessoesAbertas e fecharSapGui colapsam dois estados diferentes em "sem conexao": GetObject(SAPGUI) FALHOU (nao ha entrada no ROT) e engine COM entrada mas 0 conexoes. Foi essa confusao que gerou o diagnostico errado do item 34 ("engine sem filhos" com a sessao visivel). Separar os estados (0=getobject falhou, 1=sem GetScriptingEngine, 2=Children.Count lancou, 3=engine com N) e, alem disso, detectar o dialogo #32770 do pad para falhar dizendo "o cliente esta pedindo autorizacao" em vez de pendurar ate o timeout e deixar popup orfao na tela. Sonda pronta em sap-accelerate/work/POC_rot_sapgui/scripts/comum.mjs.
> correcao: O enunciado foi escrito contra a versao ANTIGA da lib (node_modules do sap-accelerate = github jbv-adt-client). A canonica (adt-tools/packages/adt-client) ja separa os vazios por diagnosticarRot + lerJanelas. O que falta e um estado proprio para o popup de autorizacao (#32770 titulo SAP Logon, Static contendo "script"), avaliado ANTES do estado sessao: hoje, com o popup de pe e o SAP Easy Access visivel, lerJanelas devolve sessao e diagnosticarRot conclui logon-pendente ("e a TELA DE LOGON") — errado, a sessao esta logada e falta o OK. Ver receita-gui-scripting.md § O pedagio do cliente.
> Popup do pedagio virou estado proprio: ehPopupAutorizacao (#32770 titulo "SAP Logon" sem versao, Static com script/Skript como reforco), lerJanelas responde autorizacao ANTES de sessao, diagnosticarRot responde pede-autorizacao depois so de com-sessao (vence o Err do GetObject e a conexao sem usuario no ROT) com o WarnOnAttach=0 na explicacao; sessoesAbertas passou a ler janelas sempre que ha processo. adt-tools 110ceda; receita-gui-scripting.md SS O pedagio do cliente + tabela de estados; 2 testes novos (74 no gui.test.mjs, 837 na suite). Falhar cedo/orfao ficou no item novo.

- [x] 62. Por que o popup de scripting destrava sozinho as vezes? Medido em 05/09/2026 (item 34, S4H 250): sem ninguem clicar, 2 chamadas destravaram sozinhas (34,7 s e 19,7 s) e 5 penduraram ate o timeout do chamador (90 s; 12 s x3; 15 s). E o pad ANTIGO (saplogon 41700, aberto no dia anterior) respondia em ~3 s sem popup nenhum, inclusive sem conexao, enquanto o pad NOVO (13060) pedia autorizacao ate depois de a conexao ser fechada. Medir o que muda entre um pad e outro (quem ja deu OK, quanto dura a permissao).
> pista: O item 60 (05/09/2026) da o interruptor para atacar isto barato: HKCU...SAP Frontend ServerSecurityWarnOnAttach liga e desliga o aviso na chamada seguinte, sem reiniciar o pad. Com ele da para contra-provar a variacao POR PAD — abrir dois pads de idades diferentes e alternar o valor — em vez de esperar a anomalia aparecer sozinha.
> O popup NAO destrava sozinho: 12 chamadas, 12 dialogos, 0 destraves com ninguem na maquina (pad sem conexao, conexao nascendo e sessao de pe). O OK nao deixa memoria — a chamada seguinte 2 s depois pede de novo, igual em +30 s e +120 s; nem por pad, nem por processo, nem por tempo. Idade do pad nao e variavel: o mesmo saplogon 13060 que pedia as 10 h pede identico 12 h depois. O que restou de pe, com contra-prova positiva: um SendKeys {ENTER} (sem tocar no botao) fechou o dialogo e a chamada respondeu em 7,2 s — o #32770 rouba o foco e o Enter aciona o default. Em 05/09 de manha havia uma pessoa na maquina e NAO havia vigia de janelas; o "pad antigo nao pedia autorizacao" era inferencia pelo tempo de resposta (3,0 s), nao observacao do dialogo. De quebra: o texto do pedagio colhido por sonda (Static "Um script esta tentando acessar SAP GUI.", &OK/&Cancelar) e o popup medido em 0,48-0,73 s. Medicao: sap-accelerate/work/POC_rot_sapgui/medicoes/item62-pedagio-memoria.md; receita atualizada em packages/adt-client/docs/receita-gui-scripting.md; abriu o item 102 (a discordancia do SXD de 04/09).

- [x] 63. janelasSapGui() com o encoding corrigido (item 35) ainda nao foi confirmado de ponta a ponta contra uma janela REAL do SAP GUI com texto acentuado — o pad nao estava aberto em 05/09/2026. O caminho de codigo e o mesmo provado contra o Form WinForms, mas falta a confirmacao: abrir o pad no S4H/250 (idioma PT), provocar o dialogo de senha e a caixa de mensagem (sapshcut -conn sem -sysname), e conferir titulo e textos[] (ex.: "Nem todos os dados estao disponiveis", "Entrar o nome do usuario e a senha") byte a byte contra o que a tela mostra.
> adiado: 2026-09-05 22:32 — a sessão falhou (claude-code exited with code 1:
> CONFIRMADO de ponta a ponta contra janelas REAIS do SAP GUI (S4H/250, PT): caixa "SAP GUI" e dialogo "Ligacao SAP GUI - logon (S4H, 250, PT, )" lidos pela lib com titulo e textos[] byte a byte iguais ao que a tela desenha (conferido contra foto PrintWindow) — acentos em UTF-8 (c3 a3, c3 ad, c3 a7, c3 a1), sem U+FFFD, sem best-fit, sem truncamento. A janela real revelou um TERCEIRO eixo que o item 35 nao podia ver: o Static do SAP GUI e multilinha (as 3 linhas da mensagem vem de UM Static com 

- [x] 64. A justificativa do Emulation.setFocusEmulationEnabled no abrirNavegador nunca foi CONTRA-PROVADA: o comentario diz que o Unified Renderer so reage a evento em pagina focada, mas a chamada tem .catch(() => {}) — se falhar, o clique se perde em silencio. Medido em 05/09/2026 (item 36, s4h 758/250) que mandar setFocusEmulationEnabled { enabled: false } NAO tira o foco: document.hasFocus() continuou true e o clique funcionou. Achar como produzir a pagina SEM foco (outra aba em Page.bringToFront? Target novo? Input.dispatchMouseEvent em alvo desfocado?) e medir se o clique deixa de postar — ou apagar a justificativa do comentario.
> adiado: 2026-09-05 22:32 — a sessão falhou (claude-code exited with code 1:
> CONTRA-PROVADA e FALSA. Medido s4h 758/250 05/09/2026 (POC_webgui_foco/medicoes/item64-foco.md, bruto raw/item64-foco.json): a pagina so perde o foco com uma SEGUNDA aba trazida a frente (PUT /json/new + Page.bringToFront nela) — desligar a emulacao sozinho nao tira. Com hasFocus: false e visibilityState: hidden o clique POSTOU 4/4 na mesma tela do item 36 (SA38 -> RSPARAM -> btn[8], 1251/1526/1561 ms; o 9878 ms da 1a rodada nao reproduziu). O Unified Renderer NAO exige pagina focada. A chamada FICA — o par c x d isola o efeito dela (com emulacao a aba de tras segue visible/focada, sem ela cai para hidden) — e o .catch(() => {}) esta certo porque ela nao e pre-requisito. Comentario corrigido em packages/adt-client/webgui.mjs (adt-tools ff7188c).

- [x] 65. abrirNavegador usa a porta 9222 FIXA e o Chrome anterior ainda a segura: medido em 05/09/2026 (item 36) que 5 sessoes em serie quebram na 5a com "webgui: CDP nao respondeu na porta 9222 em 30000 ms" — o fechar() so espera 300 ms depois do kill. Quebra qualquer POC que abra uma sessao por rodada. Medir: sondar porta livre a partir de 9222, ou esperar a porta soltar antes de subir. O fechar() tambem deixa o perfil temporario no disco quando o Chrome ainda segura (rmSync dentro de catch vazio).
> adiado: 2026-09-05 22:33 — a sessão falhou (claude-code exited with code 1:
> RESOLVIDO — e o achado foi pior que o item: com a 9222 fixa e um Chrome anterior ainda VIVO, o Chrome novo falha o bind e morre, mas o GET /json/list responde DO CHROME ANTIGO e a sessao nova se anexa a pagina VELHA, calada (window.__marca escrita em A lida por B; anexouNaSessaoAntiga: true). O erro "CDP nao respondeu na porta 9222" era o caso BENIGNO. Custo no disco: 85 pastas jbv-webgui-* no %TEMP%, 7,1 GB, 10 chrome.exe orfaos — limpos na sessao. Correcao: porta 0 (efemera) por default, com a porta REAL lida de <perfil>/DevToolsActivePort (668 ms; dois Chromes simultaneos ganham portas distintas e so veem o proprio target), e fechar() com Browser.close (derruba o grupo 10->0) + kill de fallback + rmSync em ate 10 tentativas, avisando o caminho do que nao sair. Porta explicita ocupada vira erro nomeado, nao anexo. Provas: 6 sessoes em serie 6/6; 4 sessoes REAIS no s4h 758/250 na SA38/RSPARAM 4/4; vitest webgui 54/54. Commit adt-tools c7c7e37; medicao sap-accelerate/work/POC_webgui_porta/medicoes/item65-porta.md. Achado novo (cmd() pendura para sempre quando o ws do CDP fecha) virou o item 104.

- [x] 66. A via HTTP pura do WebGUI (its.mjs) NAO tem pilha de desfazer: transacional() exige sessao.desfazer, que so a sessao do abrirNavegador (webgui.mjs) tem, e o fechar(sessao) do its.mjs so manda /nex + logoff ICF. Mesma exposicao do item 38 (criar e mutacao imediata) sem a mesma rede: quem dirigir uma dynpro que cria ao entrar pela via HTTP nao tem como armar o descarte. Medir se a pilha sobe para uma camada comum (as duas vias ja compartilham montarTela) ou se cada sessao tem a sua. Ver docs/receita-webgui.md secao "Criar e mutacao IMEDIATA".
> adiado: 2026-09-05 22:33 — a sessão falhou (claude-code exited with code 1:
> A pilha SOBE para camada comum: criarPilhaDeDesfazer e transacional sao PUROS (so compoem callbacks), entao o codigo e um so no webgui.mjs e o its.mjs os importa e reexporta (mesmo arranjo do montarTela); a INSTANCIA e por sessao. O que e por via e o MOMENTO: no navegador antes do Browser.close, na via HTTP antes do /nex (descartar e um POST, precisa da sessao do ITS viva). Modo de falha novo: sessao do ITS morta -> o fechar NAO gasta a pilha, avisa alto com o rotulo e devolve pendentes. Medido no s4h 758/250 (POC_webgui_its_lib/medicoes/item66-desfazer-http.md): descarte pendente voltou delta em 81 ms antes do logoff; 3 testes novos, 840/840. adt-tools c1e51da; receita-webgui.md secao "As duas vias tem a MESMA pilha". Achados viraram itens 105 e 106.

- [x] 67. ICM devolve sap-ui-core.js VAZIO na requisicao repetida — medir a causa e a defesa. Medido no s4h 758/250 em 05/09/2026 (POC_ui5_combobox): o 1o GET de /sap/public/bc/ui5_ui5/resources/sap-ui-core.js trouxe 774.764 bytes; os seguintes voltaram 200 com Content-Length 0, e a pagina UI5 ficou sem window.sap (o script carrega vazio, onload dispara, nenhum erro). Nao e cache do navegador: o fetch do Node tambem recebe 0. Uma query qualquer na URL (?jbv=<ts>) restaura. sap-ui-core-nojQuery.js e os library-preload.js do mesmo diretorio nunca vieram vazios. Descobrir se e o Static Cache do ICM (Sap-Isc-Etag), se atinge outros recursos e se o canal navegador deve carimbar a URL sozinho ao dirigir app UI5.
> adiado: 2026-09-05 22:33 — a sessão falhou (claude-code exited with code 1:
> E o HTTP server cache do ICM, e a chave inclui o ENCODING: as entradas sao <path>&&&GZ=<enc>&<mandante>&<hash da query>& (GZ=0 identity, GZ=1 gzip, GZ=B br). Medido no s4h 758/250 em 05/09/2026: sap-ui-core.js vinha 200 com 0 bytes SO em gzip; identity e br da mesma URL vinham completos (774.788 bytes), e nenhum dos outros 14 recursos medidos falhava (inclusive library-preload.js de 3 MB) — nao e tamanho, nao e o handler, nao e o navegador. A entrada envenenada e INVISIVEL na listagem (nenhuma entrada GZ=1 com o hash da query vazia, nenhuma com dsize=0 em 9.952) e cache-control/pragma no-cache sao ignorados pelo ICM; a resposta boa carrega sap-isc-etag, a vazia nao. CONTRA-PROVA: apos ICM_CACHE_INVALIDATE_ONE por classrun (subrc=0) o MESMO GET gzip voltou com 774.788 bytes. Defesa decidida: o canal navegador NAO carimba a URL — cada ?ts= distinto cria entrada nova de ~213 KB por 7 dias num cache que estava com 9.952 de 10.000 entradas; a defesa e medir e curar. Entregue: packages/adt-client/icm.mjs (medirRecurso, lerCacheEstatico, invalidarCacheEstatico, curarRecursoVazio) + icm.test.mjs (5 testes) + docs/receita-icm-cache-estatico.md; suite 845 testes verde. Medicao: sap-accelerate/work/POC_icm_static_cache/medicoes/item67-icm-cache-vazio.md. Gatilho NAO reproduzido — virou item novo.

- [x] 68. clicar(): contêiner com VÁRIOS descendentes acionáveis — hoje o rebaixamento escolhe o de menor caixa, o que é chute. Medido no s4h 758/250 em 05/09/2026 (POC_ui5_clicar_descendente): com um só descendente acerta sempre; com "adicionar" e "detalhe" na mesma linha do catálogo nada garante qual é o certo. Hoje o retorno LISTA os candidatos. Decidir se cabe endereçar por rótulo/ícone (title, aria-label, sap-icon) e medir numa lista com dois gestos.
> adiado: 2026-09-05 22:34 — a sessão falhou (claude-code exited with code 1:
> DECIDIDO por medição: endereçar por rótulo CABE (aria-label -> title -> texto -> value, sem caixa/acento) e endereçar por sap-icon NÃO — o nome do ícone não chega ao DOM, só o caractere em data-sap-ui-icon-content. Medido no s4h 758/250 05/09/2026 (UI5 1.114.0): numa linha com botão "Adicionar" (4012 px2) + ícone "Detalhe" (224 px2) o menor-por-caixa dispara SEMPRE o Detalhe — não é "não pegou", é o gesto ERRADO calado; com dois ícones iguais ganha a ordem do DOM. Entregue clicar/apontar({ dentro: <rótulo> }): acertou nas 4 rodadas, e sem casamento ÚNICO (inexistente ou ambíguo) levanta erro com a lista e dispara zero ações. Padrão inalterado (menor caixa, item 40 intacto), mas agora avisa que escolheu por TAMANHO e lista os rótulos; apontar devolve gestos (acionáveis independentes — a pilha de 4 nós do sap.m.Button colapsa em 1). Commit adt-tools 346ef1d (webgui.mjs, webgui.test.mjs +4 testes, docs/receita-webgui.md). Medição: sap-accelerate/work/POC_ui5_clicar_descendente/medicoes/item68-dois-gestos.md. Achados novos viraram itens 109 (rótulo é traduzido — medir via neutra pela IconPool) e 110 (suíte só passa do diretório do pacote).

- [x] 69. HTTPS com CA interna: o `fetch` do Node recusa antes do HTTP, e a opção `certificado` (do Chrome) NÃO o cobre. Medido em 05/09/2026 (POC_https_cert): auto-assinado → DEPTH_ZERO_SELF_SIGNED_CERT, CA interna desconhecida → UNABLE_TO_VERIFY_LEAF_SIGNATURE, nome que não bate → ERR_TLS_CERT_ALTNAME_INVALID. Hoje só o veredito é honesto (interpretarSonda devolve causa `certificado`); o canal ADT inteiro (sap-connection, its, adt-client) continua sem passar. Avaliar aceitar a CA por sistema sem NODE_TLS_REJECT_UNAUTHORIZED global — undici Agent com { connect: { ca } } por chamada, ou --use-system-ca do Node 24.
> adiado: 2026-09-05 22:34 — a sessão falhou (claude-code exited with code 1:
> ENTREGUE: o canal ADT passa em HTTPS de CA interna, e SO por ela. Campo `ca` no sistemas.json (arquivo .pem, "sistema" = store do Windows, PEM colado, ou lista), aplicado por tls.setDefaultCACertificates em resolverAlvo e de novo em sessaoAtual (processo novo nasce sem confianca). Commit adt-tools f4b49ff: packages/adt-client/ca.mjs + ca.test.mjs (6 testes; suite 855/855 verde antes da doc), config/session/webgui/sistemas.exemplo/README/receita-webgui. AS DUAS CANDIDATAS DO ITEM CAIRAM/MUDARAM: undici por chamada esta fora (pacote ausente, ERR_MODULE_NOT_FOUND, e o Node nao expoe o Agent interno — seria dependencia nova), e --use-system-ca nao e alternativa e sim complemento (so vale com a CA no store do SO; a fonte "sistema" faz isso por API, 278 CAs contra 144 do bundle). A via que resolve e tls.setDefaultCACertificates, em runtime, sem bandeira nem env. CONTRA-PROVA que define o desenho: com a CA declarada, uma raiz de controle NUNCA declarada continua barrada nos dois canais, enquanto NODE_TLS_REJECT_UNAUTHORIZED=0 aceita as duas — por isso a lib nao tem "aceita qualquer certificado" no canal do Node. PONTA A PONTA com o codigo do connect contra ICM de mentira com certificado de verdade: fetchToken devolveu token+cookie e sondarWebgui deu ok na CA interna, e barrou a raiz estranha. Dois gotchas medidos e documentados: declarar ANTES do primeiro fetch daquele host (keep-alive por origem — a conexao ja recusada nao se refaz) e setDefaultCACertificates SUBSTITUI a lista (o modulo acumula o que declarou). Nome que nao bate (ERR_TLS_CERT_ALTNAME_INVALID) nenhuma CA resolve, e a sonda passou a dizer isso. De quebra: a sessao em cache agora carrega certificado e ca — sem isso o processo seguinte perdia o pino do Chrome. Medicao: sap-accelerate/work/POC_https_cert/medicoes/item69-ca-fetch.md (scripts medir-ca-no-fetch.mjs e medir-canal-adt-com-ca.mjs). ABERTO no item 111 (bloqueado sem VPN): confirmar contra o SXD real, com o .pem da CA da acclab.com.

- [x] 70. popupDaTela escolhe a modal ERRADA quando ha duas empilhadas: ele faz find(Type===GuiModalWindow) e pega a PRIMEIRA do markup. Medido em 05/09/2026 sobre POC_webgui_okcode/medicoes/raw/d2-ose16.txt (o /o e, sobre ele, o popup da SE16): o delta declara wnd[1] E wnd[2], as duas GuiModalWindow, e popupDaTela devolve a wnd[1] ("Sessoes ABAP", a de BAIXO) com os botoes dela; tela.popup e tela.aviso apontam para a janela errada, e quem le o popup le o de tras. Mesmo defeito de ordem-de-markup que o item 42 corrigiu no enderecamento (a lib ja tem janelaAtiva(sids) em its.mjs). Entregar: popupDaTela pela janela ATIVA (a modal de maior indice), e decidir o que fazer com as de baixo — expor pilha ou so a de cima.
> adiado: 2026-09-05 22:35 — a sessão falhou (claude-code exited with code 1:
> Entregue: popupsDaTela (a PILHA de modais, de baixo para cima) + popupDaTela = o TOPO, pela mesma regra da janelaAtiva (maior wnd[n], extraida no helper indiceDaJanela). Cada modal traz atras com os SIDs das de baixo; tela.aviso passou a dizer "popup wnd[2] aberto (sobre wnd[1] - e a de cima que responde)". DECISAO sobre as de baixo: expor a pilha por FUNCAO propria (popupsDaTela) e nao um campo novo na tela - quem responde ao popup quer a de cima, e o atras ja avisa que ha outra atras sem inchar o modelo. Contra-prova sobre os brutos medidos (POC_webgui_okcode/medicoes/raw): d2-ose16.txt agora da wnd[2] "Informacao" com os botoes Avancar/Ajuda e atras [wnd[1]] (antes: wnd[1] "Sessoes ABAP"); d2-o.txt e d3-nend.txt seguem wnd[1] com atras []; c4-nse38.txt segue null. Teste novo com o markup COPIADO do bruto das duas modais (its.test.mjs, 48 testes; suite 856 verde). Doc: docs/receita-webgui.md secao "Modais EMPILHADAS: o popup e a de CIMA". Commit adt-tools 5d7a034. NAO tocado: o aviso ainda repete a afirmacao falsa sobre a wnd[0]/usr esvaziar - e o item 98.

- [x] 71. acionar(sessao, alvo, { evento }) — acionar pelo que a tela DECLARA, compondo o POST a partir do lsevents, em vez do action/3 fixo do batchAcionar. A porta que o item 44 abriu: o despejo do its.mjs ja entrega o mapa evento->comando (medido 05/09/2026, POC_webgui_its_lib/medicoes/item44-lsevents-http.md — 334 de 334 atributos lidos, e igual ao do DOM em 56 de 56 controles da mesma SE38). Falta a COMPOSICAO, que e por FAMILIA e nao por concatenacao: action/<n> e value levam o SID do controle (action/3/<SID>, value/<SID>+content); okcode/ses[0] e vkey/<n>/ses[0] ja vem auto-enderecados; vkey/<n> SEM sufixo nao se posta com o SID do campo (o alvo do teclado e a sessao, e o campo entra pelo focus anterior); e — achado do item 44 — existe comando que JA VEM ENDERECADO com o SID de OUTRO controle: o ActivateHelp do ct=MB publica action/1/wnd[0]/sbar, e nao o SID do proprio elemento (wnd[0]/sbar_msg). Entregar a funcao pura (bruto + nome do evento -> passos do batch) com teste sobre os raws, e so depois postar de verdade; a contra-prova de execucao e o item 43.
> adiado: 2026-09-05 22:35 — a sessão falhou (claude-code exited with code 1:
> batchDoEvento(controle, evento, { valor }) PURO no its.mjs + acionar(s, alvo, { evento }) e eventosDoAlvo. Composicao por FAMILIA: 709 de 709 pares postaveis dos 5 raws compoem, sem familia desconhecida; 51 (7,2%) nao sao "comando + / + SID" (47 vkey/<n> sem sufixo -> focus + /ses[0]; 4 action/1/wnd[0]/sbar ja enderecados a OUTRO SID). Dois achados: o SID mora ANINHADO no lsdata (indice varia por ct; 387 de 392 com exatamente um par, nenhum com dois) e SID repetido existe (POMNI x POMN do menu, nunca dois com lsevents) — dai o controleDoSid desempatar por quem declara disparo, o que reproduz o action/4/<SID> do item 49. NADA foi postado: contra-prova de execucao e o item 43. Commit adt-tools b5a2be3; medicao sap-accelerate/work/POC_webgui_its_lib/medicoes/item71-compor-lsevents.md; script scripts/compor-lsevents.mjs; 862 testes passam.

- [x] 72. O ITSDoc de ENTRADA: subir ARQUIVO para o SAP pela via HTTP pura do WebGUI — o par exato da exportacao medida no item 45. Lido no renderer (lsgui/js/webgui_min.js, funcao Import do invoke_itsdoc) e NAO medido: quando a dynpro pede ITSDocMethod=Import, o cliente POSTa <URL>post com um FormData de campo LOCALFILE1 (Blob application/octet-stream), e depois devolve o controle com o OK_ITSDOC (okcode =OK + vkey/0 + state/ur). Medir num gesto real de upload (ex.: importacao de arquivo local de alguma transacao) se o Node consegue entregar o arquivo assim, e o que o servidor faz com FileSaveDialog/FileOpenDialog antes. A peca pura pedidoDoItsdoc do its.mjs ja tem o lugar do verbo.
> adiado: 2026-09-05 22:36 — a sessão falhou (claude-code exited with code 1:
> MEDIDO no s4h 758/250 (06/09/2026): o canal HTTP puro SOBE arquivo — 57 B de texto e 256 KB de binario aleatorio num POST so, sha256 IDENTICO na ida e na volta (subiu por CG3Z, voltou por CG3Y). O gesto: CG3Z, campo ctxtRCGFILETR-FTFRONT com um nome VIRTUAL (Z:\x.bin, que nao existe em disco nenhum), txtRCGFILETR-FTAPPL com o destino real, botao Carregar (wnd[1]/tbar[0]/btn[14], Press: action/3) — e o servidor vai DIRETO ao ITSDocMethod=Import, sem FileOpenDialog nenhum, porque o nome ja esta na dynpro. O POST do Import e o unico do ITSDoc que LEVA dado, e vai DIFERENTE: UpDownSendRequest(url, null, "X", ...), e o "X" DESLIGA o Content-type x-www-form-urlencoded e o X-Requested-With — quem fala e o multipart do FormData (LOCALFILE1, Blob application/octet-stream); com Content-type a mao o boundary quebra. ACHADO QUE CORRIGE O ITEM 45: o Query tem QUATRO sub-verbos com respostas diferentes — CD (o diretorio corrente), FL (o TAMANHO em bytes), FE (o arquivo existe? 1/0), DE (o diretorio existe? 1/0); a lib respondia o caminho a todos, e a CG3Y (que pergunta FE) recebendo Z:\ deu DUMP. Terceiro achado: o FileOpenDialog (F4 no campo, vkey/4) manda os parametros no CORPO (FileEncoding=&count=1&FileName0=...) com a URL terminada em ?, ao contrario do FileSaveDialog que poe tudo na URL — e responde-lo PREENCHE o campo da dynpro, ou seja da para ESCOLHER o arquivo sem digitar. Virou codigo: pedidoDoItsdoc devolve { caminho, corpo, conteudo, envia } e cobre Import/FileOpenDialog/os quatro Query (caminho: null = nao POSTe, so devolva o controle); updown ganhou as duas formas de POST; atenderItsdoc(sessao, resposta, { dado, ... }) e o laco do renderer nas DUAS direcoes, extraido de dentro do exportarLista (que passou a viver dele, sem regressao: 182015 B, 1 parte, 3 voltas, 1621 linhas na mesma rodada). 864/864 verdes. adt-tools 2bd0aae; medicao em sap-accelerate/work/POC_webgui_import/medicoes/item72-import.md; receita-webgui.md secao SUBIR arquivo para o SAP. FICOU NO SISTEMA: /usr/sap/trans/item72_upload.txt (57 B) e /usr/sap/trans/item72_upload.bin (128 KB) no s4h — arquivos NOVOS, nada de terceiros sobrescrito — e um dump na ST22 da contra-prova do Query FE. Achados novos: itens 112 (o TETO do Import — o maximum file size do renderer e o 413 do UpDownSendRequest) e 113 (os metodos do ITSDoc que nao sao arquivo: Execute, Delete, DirectoryListFiles, GetTempPath).

- [x] 73. O formato PLANILHA ELETRONICA da exportacao do ALV nao passa pelo ITSDoc — medir onde desemboca. Medido em 05/09/2026 (item 45, s4h 758/250): dos 6 radios do popup Gravar lista em file..., cinco caem no ITSDoc (Export ou ClipboardExport) e o radio 2 (Planilha eletronica) abre OUTRO popup, Export As, com os campos GS_EXPORT-FILE_NAME, GS_EXPORT-FORMAT e GS_EXPORT-DESTINATION e o botao Exportar para... . Nao foi seguido. Vale porque e o caminho que o usuario final usa para XLSX, e porque o mesmo popup aparece no btn[43] (Planilha eletronica..., CTRL_SHIFT_F7) da barra do ALV.
> adiado: 2026-09-05 22:36 — a sessão falhou (claude-code exited with code 1:
> DESEMBOCA no mesmo ITSDoc, uma etapa depois: o Avançar do popup de 6 radios abre a modal **Export As** (`SAPLSALV_GUI_CUL_EXPORT_AS`) e quem dispara é o "Exportar para..." dela (`wnd[1]/tbar[0]/btn[20]`, SHIFT_F8), não o Enter. Laço de 3 voltas: `FileSaveDialog` → `Export` → **`Execute`** (método novo; NÃO tem POST — no renderer é showBlob, e responder `cancel` conclui igual mas mente). O `btn[43]` abre o MESMO popup e o MESMO ITSDoc — mas as duas vias NÃO dão o mesmo arquivo: `btn[45]`→radio planilha traz a LISTA em células (1620 × 32, 206 650 B) e o `btn[43]` traz o GRID (1618 × 5, 88 061 B), com os 1617 parâmetros batendo 1617/1617. Na lib: `exportarPlanilha`, `exportAsDoPopup`, `pedidoDoItsdoc` do `Execute`, e `exportarLista({formato:"planilha"})` deixou de devolver 0 B em silêncio (adt-tools e465c3e; 866/866 testes). Medição: sap-accelerate/work/POC_webgui_planilha/medicoes/item73-planilha.md

- [x] 74. O action/710 de dentro da sessao do NAVEGADOR — dar ao webgui.mjs o alcance TOTAL do ALV sem segunda sessao. Medido em 05/09/2026 (item 46, POC_webgui_grid fases I-L) que o lerGrid do navegador so alcanca o BLOCO (166 de 1617) e que rolar por roda para completar custa ~222 rodadas / ~11 min. Mas a pagina JA TEM o action (o token da sessao no webguiform0) e o cookie: um fetch no contexto dela para batch/json com o mesmo batchFragmento do its.mjs daria a tabela inteira NA SESSAO DO NAVEGADOR (que enxerga filtro, drill-down e linha selecionada — coisa que a sessao HTTP paralela nao ve). Nao medido: se um POST fora do framework dessincroniza o Unified Renderer (o moin/contador de sequencia) e mata a tela.
> adiado: 2026-09-05 22:36 — a sessão falhou (claude-code exited with code 1:
> webgui.lerGridInteiro na lib: o action/710 postado de DENTRO da pagina (action + moin + cookie HttpOnly que o fetch same-origin manda), 1617/1617 linhas em 4 pedidos e 2,4 s, 8.085 de 8.085 celulas iguais as do its.lerGrid, e com o ALV ordenado na tela a leitura sai na ordem NOVA (a sessao paralela devolve a original). O risco do item RESPONDIDO: com o header moin o POST e INOCUO (positiva de controle antes e depois: o framework pediu sozinho 166->194 e continuou pedindo 194->222; btn[3] trouxe tela viva) e o moin NAO e contador de sequencia (o POST do proprio framework leva o mesmo window.moin). ⚠ SEM o moin e HTTP 500 e a sessao MORRE calada (roda ainda posta, bloco nao cresce, btn[3] traz tela vazia) — a funcao estoura antes de postar. adt-tools 2b6e3bd; medicao em sap-accelerate/work/POC_webgui_fragmento/medicoes/item74-fragmento.md

- [x] 75. Posicionar o ALV do NAVEGADOR numa linha distante — o gesto para dar drill-down em linha fora do bloco. Medido em 05/09/2026 (item 46, fase J) que a roda do mouse move a janela 10 linhas por rodada a ~2,9 s cada e que firstVisibleRow do lsdata fica 0 o tempo todo (o servidor nao acompanha a rolagem dentro do bloco). Falta medir se arrastar o _vscroll (ct=SCB, lsdata com posicao/max/janela/total) posiciona direto, e se ha um action de VerticalScroll (o 61 aparece no batch da roda com logic:ignore) que sirva de gesto. Isto e NAVEGACAO, nao leitura — para ler a tabela inteira a via e o its.lerGrid.
> adiado: 2026-09-05 22:36 — a sessão falhou (claude-code exited with code 1:
> Arrastar o thumb do <cid>_vscroll POSICIONA direto: um gesto, ~1 s, 1 requisicao de ~9 KB — contra ~90 rodadas de roda (~4 min) para a mesma linha 900. O action/61 (VerticalScroll) nao e gesto a parte: o framework o publica sozinho ao soltar (position 0-based, logic:ignore) junto do action/710 do fragmento, o mesmo do lerGridInteiro. Drill-down PROVADO: depois de posicionar em 1234, o duplo clique mandou action/53 row_index=1234 (1-based) e abriu o popup do rec/empty_stxx_as_white_list, a linha certa. Duas armadilhas medidas: o lsdata do scrollbar e o firstVisibleRow NAO acompanham a rolagem (ficam 1 e 0 com a janela em 1600) — quem diz onde a janela esta e o iidx das <tr> com altura > 0; e arrasto de menos de 4 px nao move nada (3 tentativas, 46 s perdidos), o ajuste fino sai pela roda. Virou posicionarGrid(sessao, alvo, linha) + as puras estadoDoScrollbar/miraDoScrollbar/jsJanelaDoGrid/naJanela no webgui.mjs (6 testes), receita-webgui.md secao posicionarGrid, README. E2E: 6/6 alvos dentro, 0 gesto quando ja visivel. Medicao: sap-accelerate/work/POC_webgui_grid/medicoes/item75-posicionar.md. Achado que virou item 118: duplo clique nao e gesto da lib.

- [x] 76. Grid do WebGUI: SELECIONAR linha e celula pelo navegador. Medido no item 47 (05/09/2026, POC_webgui_grid_edit/medicoes/raw/e-publicar.json) que o clique numa celula ja dispara sozinho action/50 (top_left/bottom_right/reference row+column = bloco selecionado) e action/53 (row_index+column_index = celula corrente) — a selecao COMECA ai e ninguem a exercitou de proposito. O lsdata do grid entrega selectedRows, selectedCells, selectedBlock, currentCellRow/Column e selectionMode; o lsevents publica CellSelect, BlockSelect, ClientRowSelect, ClientColumnSelect. Medir: selecionar UMA linha (a caixa da coluna 0, SAPTABLECSSELECTIONCELL), selecionar N linhas, ler a selecao de volta pelo lsdata, e provar que o ABAP a enxerga (o laboratorio ZJBV_ALV47_EDIT ja esta no s4h e pode ganhar um fcode que despeja a selecao).
> adiado: 2026-09-05 22:37 — a sessão falhou (claude-code exited with code 1:
> MEDIDO E PROVADO. A caixa de selecao da linha e grid#<cid>#<n>,0 (sem o #if da celula de dado) e vive no <tr> da faixa CONGELADA (-mrss-cont-left-Row-<n-1>), nao no do dado — por isso a varredura inicial nao a achou apesar do hasSelectionColumn: true. O gesto e 100% CLIENTE: 5 gestos, ZERO requisicao, nem depois do delayedChangedSelectionTimeout de 1500 ms. Clique simples SUBSTITUI, ctrl ACRESCENTA, shift faz FAIXA. O ABAP a enxerga no round-trip seguinte (action/47), provado com contra-prova pelo fcode FC02 do laboratorio ZJBV_ALV47_EDIT: nenhuma selecao -> rows=0; linha 2 -> rows=1:0000000002; 1 e 3 -> rows=2; faixa 1..3 -> rows=3. O action/47 COMPACTA faixa contigua (";1-3;"). Clicar numa CELULA nao seleciona linha (sai action/50+53, get_selected_rows vazio). ARMADILHA: o selectedRows do lsdata esta sempre UM ROUND-TRIP ATRAS — com 1 e 3 pintadas ele dizia ";2;" — igual ao scrollbar do item 75; quem sabe a verdade e a classe da caixa (urSTRowSelIcon). Virou lib: selecionarLinhas, lerSelecao (com defasado), interpretarSelectedRows, idDaCaixa, jsSelecaoDoGrid, MOD, e clique({ modificadores }). Compoe com posicionarGrid (RSPARAM: linha 900 em 1211 ms + selecao em 368 ms). 74 testes verdes no webgui, 882 no pacote. Medicao: sap-accelerate/work/POC_webgui_grid_sel/medicoes/item76-selecionar.md · adt-tools e03f20a

- [x] 77. Grid do WebGUI: ORDENAR e FILTRAR o ALV pelo navegador. Continua aberto desde o item 25. A barra do ALV tem os botoes (ordenar cresc/decr, filtrar) e o menu de contexto da celula publica CellContextMenu no lsevents; o ColumnResize e o ClientColumnSelect sugerem que o cabecalho e acionavel. Medir: qual gesto ordena (clique no cabecalho? botao da tbar[1]?), o que sai na rede, se o bloco do DOM e REORDENADO ou se o servidor manda outro fragmento (muda o significado do _linha do lerGrid!), e como o filtro se aplica e se le de volta. Laboratorio: ZJBV_ALV47_EDIT (3 linhas) e a lista do RSPARAM (1617).
> adiado: 2026-09-05 22:37 — a sessão falhou (claude-code exited with code 1:
> MEDIDO e ENTREGUE (s4h 758/250, 05-06/09/2026, POC_webgui_grid_ord fases A-L, laboratorio ZJBV_ALV47_EDIT com o fcode FC03 novo que despeja lines(gt_tab), o indice do get_selected_rows e gt_tab[1]). O gesto: ordenar e filtrar sao o MESMO gesto em duas metades — marcar a coluna no <th> grid#<cid>#0,<c> (ZERO POST, puro cliente) e acionar o botao da barra do ALV; a marca viaja como action/46 columns=;2; junto do round-trip seguinte (irmao do action/47 rows= do item 76) e MORRE se ele vier sem ela, por isso ordenarGrid/filtrarGrid marcam a coluna eles mesmos. O botao e enderecavel pelo SID ("<sid do grid>/tbar/btn&SORT_ASC", ou dbtn& para MB_FILTER); o id do DOM (C102_toolbar_btn15) e POSICIONAL e nao sobrevive a outra tela nem a dois ALVs. ⚠ Sem coluna marcada, SORT_ASC nao ordena: abre o dialogo "Ordenacao" (SAPLSALV_CUL_...) — e ele NAO e wnd[1], o lerTela segue dizendo janela.principal: true e o clique seguinte cai ATRAS do modal e sai calado (cegou a fase B inteira); JS_MODAL_DO_ALV o enxerga por [ct^="PW"] visivel e o ordenarGrid estoura com o titulo. As DUAS respostas do item: (1) o DOM e reordenado no lugar (mesmos <tr>) e ordenar REORDENA A TABELA INTERNA DO ABAP — com NOME desc, gt_tab[1]-nome virou tres e o get_selected_rows da linha 1 respondeu 0000000001=tres; (2) filtrar NAO toca a outtab — com 1 linha na tela (_linha 1) o ABAP respondeu 0000000002=E2E-776551, n=3, tab1 intacta: o framework TRADUZ o indice visual para o da outtab. Isso FECHA o significado do _linha do lerGrid: "a n-esima linha VISIVEL agora", nunca uma identidade — guardado antes de ordenar/filtrar aponta para outro dado, e sob filtro nem e o numero que o ABAP usa; quem precisa de identidade guarda a CHAVE. O filtro: MB_FILTER com coluna marcada abre "Determinar valores para criterios filtro", esse SIM wnd[1] de verdade (ctxt%%DYN001-LOW/HIGH, OK em wnd[1]/tbar[0]/btn[0], ids do DOM posicionais achados por jsPorSid). Dois modos de falha medidos: o campo e ctxt e CONVERTE PARA MAIUSCULAS (filtrar "tres" minusculo, que existe, deu 0 linhas SEM ERRO NENHUM; total: 0 e resposta, nao excecao), e o dialogo REABRE COM O FILTRO ANTERIOR — limpar um intervalo escrevendo so o LOW deixava o HIGH de pe (QTD 1..100 "limpo" continuava em 2 linhas, fase K caso 8); corrigido escrevendo o HIGH sempre que existe, inclusive vazio, e REVALIDADO contra o sistema na fase L (limpa para 3 linhas; e 1..2 seguido de { de: 815 } da 1 linha). O estado se le no NOME do PNG do cabecalho, composicional head<ordem>o<filtro>.png (headaoo/headdoo/headoof/headaof/headdof) — dai saem duas regras: ordenar por outra coluna SUBSTITUI o criterio mas PRESERVA o filtro da coluna anterior, e limpar o filtro NAO derruba a ordenacao. Lib (packages/adt-client/webgui.mjs): marcarColuna, ordenarGrid, filtrarGrid, lerColunas + os puros estadoDoCabecalho, idDoCabecalho, jsCabecalhoDoGrid, jsBotaoDaBarra, JS_FCODES_DA_BARRA, JS_MODAL_DO_ALV, jsPorSid. 5 testes novos com os icones e SIDs REAIS do laboratorio; 79 no webgui, 887 no pacote, todos verdes. Commit 8756876 no adt-tools; receita em packages/adt-client/docs/receita-webgui.md § "Ordenar e filtrar o ALV" (e o bullet de em-aberto do § ALV atualizado). Medicao: sap-accelerate/work/POC_webgui_grid_ord/medicoes/item77-ordenar-filtrar.md. Ficou fora e virou item: 121 (o dialogo "Ordenacao", ordenacao por varias colunas) e 122 (o CellContextMenu, a outra porta para os fcodes).

- [x] 78. Grid do WebGUI: INSERIR e APAGAR linha no ALV editavel. Medido no item 47 que o lsevents da celula editavel publica DeleteItem [{},{"3":true}], e o cabecalho do BCALV_EDIT_01 diz que o demo aceita linha nova ("you may change values and enter new lines"). Nao medido: qual gesto cria a linha (digitar na ultima? botao da toolbar do ALV?), qual action a apaga, e o que acontece com o _linha absoluto das demais. Laboratorio: ZJBV_ALV47_EDIT (o MODIFY dele nao apaga — apagar linha no ALV exigiria um DELETE no report, entao o item provavelmente comeca alterando o laboratorio).
> adiado: 2026-09-05 22:37 — a sessão falhou (claude-code exited with code 1:
> MEDIDO no s4h 758/250 (06/09/2026): quem cria e apaga linha nao e o DeleteItem do lsevents da celula — e a BARRA do ALV editavel, com LOCAL&APPEND ("Anexar linha"), LOCAL&INSERT_ROW, LOCAL&DELETE_ROW e LOCAL&COPY_ROW, endereçaveis pelo SID e ausentes num ALV somente leitura (BCALV_GRID_DEMO nao tem nenhum dos quatro). APPEND vai para o FIM sempre; INSERT_ROW entra ANTES da linha selecionada (ou, sem selecao, da CORRENTE); DELETE_ROW apaga TODAS as selecionadas de uma vez e, sem selecao, apaga a CORRENTE — que qualquer clique move; o _linha das demais RENUMERA na hora. ⚠ O gesto FAZ round-trip (action/3) e mesmo assim NAO GRAVA: contra-prova pareada, mesma sequencia, sem o FC01 a tabela ficou inalterada em outra LUW; com ele, a linha nova entrou e a apagada saiu. E MODIFY FROM TABLE nunca apaga — o FC01 do laboratorio ZJBV_ALV47_EDIT virou DELETE FROM + MODIFY (sincronizacao) para que "apagou" fosse falseavel; FC02/FC03 intactos, fonte anterior em raw/d-fonte-antes.abap. A linha nova nasce com os campos INICIAIS: sem preencher o ID numc(3) ela chegou ao banco como 000. Virou codigo: inserirLinha(s, alvo, { antesDe?, valores? }), apagarLinhas(s, alvo, [n]) e FCODES_DE_LINHA no webgui.mjs, os dois conferindo o total depois do gesto (80 testes verdes). Commit adt-tools b7c83b1; medicao em sap-accelerate/work/POC_webgui_grid_linha/medicoes/item78-inserir-apagar-linha.md; receita-webgui.md § Inserir e apagar LINHA no ALV. Achados novos: fila 123 (duplicar linha por LOCAL&COPY_ROW) e 124 (a caixa que nao pinta depois de um round-trip — intermitente, hipotese de timing testada e caida).

- [x] 79. Grid do WebGUI: escrita em LOTE por ClipboardTablePaste (action/25). Medido no item 47 que a celula editavel publica "ClipboardTablePaste":[{},{"0":"GuiTextField","1":"action/25","2":true,"3":true}] — e este e um dos raros lsevents de shell que TRAZ o comando (indice 1). Hoje escreverCelula custa um clique + digitacao + blur por celula, e todas viajam num round-trip so; colar um bloco poderia custar um gesto so. Medir: o formato do content do action/25 (TSV? qual separador?), se ele respeita a celula corrente como ancora, e se o ABAP recebe igual ao action/622.
> adiado: 2026-09-05 22:38 — a sessão falhou (claude-code exited with code 1:
> FECHADO — e o enunciado estava errado no ponto central: NAO existe action/25 nenhum. O lsevents da celula declara ClipboardTablePaste -> action/25 e MENTE (como o DoubleClick -> action/74 da arvore, item 74). Medido no s4h 758/250 em 06/09/2026 no laboratorio ZJBV_ALV47_EDIT (fases A-D, POC_webgui_grid_paste): colar posta, num POST so, focus/<SID> + action/50 (area) + action/53 row_index=<r>&column_index=<c> (a celula CORRENTE) + UM action/770 c0=<v>&c1=<v>..&curColIdx=<coluna ancora 1-based>&curRowIdx=<0-based RELATIVO a ancora> por linha colada + state/ur. Formato: TAB separa coluna, quebra separa linha, e LF e CRLF dao o MESMO batch (o TSV do Excel serve direto), e o content vai URL-encoded (a;b -> a%3Bb). ANCORA: sim, respeita a celula corrente — e SO ela: disparar o paste no span da celula 3,NOME sem clicar nela antes colou na (1,1), a corrente do ALV (curColIdx=1), truncando REP79 em REP na chave. ABAP: recebe igual ao action/622 — ITEM79-A/201, ITEM79-B/202, ITEM79-C/203 colados num gesto, FC01, lidos em outra LUW por dataPreview, identicos; e contra-prova pareada mostra que colar faz round-trip e mesmo assim NAO grava. CUSTO: as mesmas 6 celulas custam 9748 ms e 0 requisicao por escreverCelula (tudo pendente) contra 2578 ms e 1 requisicao por colarBloco — e o POST do paste leva junto o que estava pendente do escreverCelula. Tres silencios medidos: texto sem TAB e sem quebra nao vira colagem nenhuma (0 requisicao, nada muda), coluna que estoura a direita e descartada sem aviso (as duas viraram recusa ANTES do gesto), e linha que estoura embaixo o ALV ANEXA (action/771 pasteOption=Append) com a linha nova vazia fora do bloco (vira anexadas no retorno). A tela depois do paste tambem nao e prova: ABC colado na coluna numerica ficou na tela, o FC01 disse GRAVOU subrc=0 e o banco manteve o valor antigo. A via do gesto e o ClipboardEvent sintetico com DataTransfer; o paste nativo do CDP (Input.dispatchKeyEvent commands:[paste]) da o MESMO batch mas passa pelo clipboard do SISTEMA, que e do usuario. Entregue: tsvDoBloco + jsColarNoGrid + colarBloco em packages/adt-client/webgui.mjs (3 testes puros novos; suite 891/891), receita § Colar um BLOCO no ALV, README e nota cruzada no escreverCelula — commit adt-tools 5eaefec. Medicao: sap-accelerate/work/POC_webgui_grid_paste/medicoes/item79-colar-bloco.md. Achado novo virou o item 125 (postar o action/770 pela via HTTP do its.mjs, sem Chrome).

- [x] 80. webgui.comandar: o custo escondido quando a tela NAO muda. Medido no item 47 (05/09/2026, POC_webgui_grid_edit/medicoes/raw/h-contraprova.json) que o mesmo comandar(FC01) levou 4,2 s numa corrida e 42,8 s na outra — a diferenca e o esperarMudanca ir ate o teto quando o round-trip repinta o grid sem mudar o carimbo (JS_CARIMBO = title + n de elementos + ...). O e2e da mesma sessao deu mudou:true em 1,6 s, entao e intermitente. Medir o que muda no carimbo num repaint de grid e decidir se o carimbo precisa de mais um sinal (a mensagem da statusbar? o lsdata do grid?) — mudou:false hoje pode ser "nao pegou" OU "pegou e a tela ficou igual", e a lib nao distingue.
> adiado: 2026-09-05 22:38 — a sessão falhou (claude-code exited with code 1:
> MEDIDO e RESOLVIDO no s4h 758/250 (06/09/2026) — nao e intermitencia, e cegueira do carimbo. O JS_CARIMBO le os 300 PRIMEIROS chars do innerText, que sao o CABECALHO (titulo/menu/botoes); nem o grid nem a statusbar (char ~1000) chegam la. Em 5 gestos com round-trip COMPLETO, o carimbo ficou IGUAL em 3 (FC01 repetido 183 ms, FCZZ 186 ms, FC03 374 ms) e nos 2 que mudou foi pela contagem de elementos, por acaso. Dentro do DOM nao ha sinal generico e honesto (o lsdata do grid tem version 1→2→3→4 por round-trip, mas so existe onde ha grid; o painel Tempo E2E depende do tema). O sinal e o ROUND-TRIP de rede: novos ehTelemetria/roundTrips (puros, testados — o gesto dispara um 2o POST de FESR que nao pode contar) e esperarTroca (round-trip OU carimbo, o que vier primeiro, assentando sempre antes de julgar: a resposta chega ~200 ms antes do repaint). comandar passou a devolver respondeu ao lado de mudou: respondeu:false = nenhuma conversa com o ABAP; respondeu:true+mudou:false = houve conversa e a tela ficou igual. ⚠ respondeu:true NAO e o mesmo que pegou — o FCZZ tambem faz round-trip; efeito quem prova e a mensagem/o dado. PROVA (fase L, teto de 40 s de proposito): os 5 gestos fecharam entre 1476 e 1678 ms — 4 deles pagariam o teto inteiro antes — e a escrita persistiu no banco. Medicao: sap-accelerate/work/POC_webgui_grid_edit/medicoes/item80-carimbo-repaint.md (bruto j-carimbo/k-oque-muda/l-comandar-novo.json). Codigo+receita: adt-tools packages/adt-client/webgui.mjs, webgui.test.mjs, docs/receita-webgui.md. Propagacao aos outros gestos (acionar/ordenar/filtrar/inserir/apagar) → item 126.

- [x] 81. BOTAO desabilitado no WebGUI nunca foi medido — e o campo que existe hoje mente. JS_DESPEJO_CONTROLES le `desabilitado: !!el.disabled` (a propriedade DOM nativa), que em <span>/<div> do ITS e sempre false; `botoes()` nao le habilitacao nenhuma. Achado do item 48 (05/09/2026): no item de MENU a habilitacao nao esta no ARIA nem na cor — esta no `lsdata[5]`, omitido quando e o default. Hipotese (NAO medida): o botao cinza da toolbar tem indice equivalente no lsdata. Medir: achar tela com botao de toolbar cinza (candidatos: SE16 sem tabela, SU01 sem usuario, botao Voltar na tela inicial), despejar TODOS os atributos + lsdata do botao cinza e de um irmao habilitado, e so entao decidir de onde sai habilitado em `botoes()`/`lerTela`. Receita: docs/receita-webgui.md § do item DESABILITADO. Medicao irma: sap-accelerate/work/POC_webgui_menu/medicoes/item48-menu-cinza.md
> adiado: 2026-09-05 22:38 — a sessão falhou (claude-code exited with code 1:
> MEDIDO: o botao desabilitado e o lsdata[5] === false — o MESMO indice do item de menu (item 48). Provado com irmaos construidos de proposito (report ZJBV_BTN81: BT_ON x BT_OFF com SCREEN-INPUT = 0, mesma tela, uma variavel so): os lsdata diferem em UM indice, e com o 5 vem aria-disabled=true, tabindex=-1, lsButton--disabled, opacity 0.4, cursor default. Ausente e habilitado. A hipotese do item estava certa — mas achar um botao cinza custou 27 estados: a BARRA nao acinzenta (o EXCLUDING do PF-STATUS REMOVE o botao), 390 botoes de 18 telas sem um cinza; quem fica cinza e o pushbutton de dynpro. Tres armadilhas medidas: (a) el.disabled e false em 390/390 (o botao e <div>; no campo quem vale e readOnly), (b) o lsdata[5] do CAMPO e o VALOR, nao flag — a regra e por papel, (c) o lsevents e IDENTICO nos dois, entao a via HTTP tambem precisa do lsdata[5]. Clique no cinza gera ZERO POST. SCREEN-ACTIVE = 0 tira o botao do DOM (escondido != desabilitado). Lib: habilitadoDoBotao() nova, ariaDesabilitado no despejo, habilitado em interpretarControle/botoes(), e o lerTela passou a trazer o pushbutton de dynpro (sem ele o campo seria inalcancavel). 86 testes verdes + prova E2E. Receita: docs/receita-webgui.md §§ do BOTAO desabilitado e da BARRA que nao acinzenta. Medicao: sap-accelerate/work/POC_webgui_botao/medicoes/item81-botao-cinza.md. Abriu o item 127 (acionar pushbutton pelo id + guarda).

- [x] 82. A arvore de menu INTEIRA tambem esta no DOM da via CDP, invisivel? Achado do item 49 (s4h 758/250, 05/09/2026): pela via HTTP o boot da SE38 devolve os 146 POMNI de wnd[0]/mbar em tres niveis, SEM abrir menu nenhum — o item 26 tinha medido que no DOM o menu so nasce ao clicar em cua2sapmenu_btn (o clique criou 25 elementos, 11 POMNI). Mas o navegador recebe o MESMO delta-update. Medir: os POMNI existem no DOM antes do clique (querySelectorAll sem o filtro de visivel do JS_ITENS_DE_MENU)? o renderer os descarta ou so os esconde? Se estiverem la, o navegarMenu do webgui.mjs troca a cascata de cliques (um por nivel, cada um com espera nao-sincrona) por UMA leitura + um clique na folha.
> adiado: 2026-09-05 22:39 — a sessão falhou (claude-code exited with code 1:
> MEDIDO no s4h 758/250 (06/09/2026): elemento de POMNI da barra NAO existe no DOM antes do clique — querySelectorAll SEM o filtro de visivel devolve 0 de wnd[0]/mbar (os 7 que aparecem sao o menu de informacao do sistema). O item 26 estava certo. MAS a arvore INTEIRA esta no DOM, como TEXTO: o boot escreve a marcacao de cada popup num <xmp> inerte (class=lsPopupMenu__metaData, 28 na SE38), e ali estao os 146 POMNI de wnd[0]/mbar em QUATRO niveis com os 11 cinzas — os mesmos do delta da via HTTP (item 49). O renderer nao descarta nem esconde: guarda a marcacao e INFLA sob demanda, um popup por clique (o botao inflou os 7 do nivel 0; Sistema inflou so os 12 filhos DELE; a folha de nivel 2 continuou inexistente). Confirmado em 6 telas (SMEN 99, SE38 146, SE16 83, SE11 122, SM30 78, SU01 106 itens; 7-35 ms; 0 POMNI vivos em todas). Logo a premissa do item vale para LER e nao para ACIONAR: clique e em elemento, e a folha so nasce depois do pai. NA LIB: webgui.mjs ganhou JS_ARVORE_DE_MENU e arvoreDeMenu(sessao) (DOMParser sobre o texto dos <xmp>, documento solto, zero clique); acharCaminhoDeMenu desceu do its.mjs para o webgui.mjs (reexportado la) — o percurso por rotulo virou um codigo so para as duas vias; navegarMenu resolve o caminho INTEIRO na arvore antes de tocar na tela e so entao desce a cascata clicando por id (o clicar ja espera o alvo aparecer, e essa espera substituiu a leitura de itensDeMenu entre os passos). MEDIDO contra o HEAD, mesmo roteiro na SE38: descobrir Sistema>Servicos 758 ms e 28 elementos materializados -> 8 ms e ZERO clique; guarda de rotulo inexistente 395 ms com o menu aberto -> 6 ms com o DOM intocado; guarda do item cinza 13559 ms e 51 elementos -> 6 ms; navegar ate Reporting 2584 -> 2335 ms (a cascata do acionamento e irredutivel). A guarda do cinza agora vale para o caminho inteiro e some o efeito colateral de deixar o menu aberto depois do erro. Medicao: sap-accelerate/work/POC_webgui_menu/medicoes/item82-arvore-no-xmp.md; receita-webgui.md atualizada. Abriu 128 (os 39 POMNI de wnd[0]/usr/mnu), 129 (inflar o popup do <xmp> para matar a cascata) e 130 (its.test.mjs:511 falha no HEAD).

- [x] 83. Folha de menu que abre POPUP: o navegarMenu da via HTTP nao distingue "abriu modal" de "trocou de tela". Achado do item 49 (05/09/2026): o mudou do its.navegarMenu compara titulo e dynpro antes/depois, e o popup (wnd[1]) vem no MESMO delta-update sem trocar a dynpro — logo uma folha que abre modal (ex. "Encerrar", "Configuracoes...") volta mudou:false, indistinguivel da folha inocua do action/4 em no com submenu. Medir contra folhas que abrem SPOP e devolver o popup no resultado (o popupDaTela ja existe). Mesma familia do item 59 (pegou:true falso positivo com popup aberto).
> adiado: 2026-09-05 22:39 — a sessão falhou (claude-code exited with code 1:
> Medido no s4h 758/250 em 06/09/2026. A premissa estava VENCIDA: o carimbo do item 59 ja inclui a janelaAtiva, entao as tres folhas que abrem SPOP (Sistema>Status..., Ajuda>Configuracoes..., Utilitarios>Configuracoes...) voltam mudou:true — o veredito ANTIGO (titulo+dynpro) e que dava false. O defeito REAL que sobrou: as tres, mais a folha que troca de tela (Servicos>Reporting), sao TODAS forma:delta/pegou:true/mudou:true — o mudou diz QUE mudou, nunca O QUE. Corrigido: its.navegarMenu devolve popup (o objeto, com titulo e botoes), a leitura vira !r.mudou ? nada : r.popup ? popup : tela; novo popupDaSessao(s) so paga o parse (8-17 ms num delta de 300 KB) quando os SIDs ja dizem que ha modal, e foi reusado nos 3 pontos do exportarLista; lerResposta.popup (booleano do CORPO, colidindo de nome com o objeto do lerTela — r.popup?.sid dava undefined em silencio) renomeado para temPopup; postar passa a devolver janela. Contra-prova que fixou o desenho: com a modal na frente o action/4 volta multipart/pegou:false e temPopup:false COM a modal ainda aberta — por isso o popup sai da TELA, nao do corpo. Prova E2E 4/4. Medicao: sap-accelerate/work/POC_webgui_menu/medicoes/item83-menu-popup.md; receita-webgui.md secao "A folha de menu que abre POPUP". Abertos viraram itens 131 (guarda que falta: modal aberta engole o action/4 em silencio), 132 (webgui.navegarMenu do navegador tem o mesmo buraco) e 133 (teste ja quebrado no HEAD, tela.botoes com popup).

- [x] 84. A arvore nao diz quem TEM FILHOS — achado do item 50 (s4h 758/250, 05/09/2026). O navegarArvore descobre expandindo, e um no FOLHA gasta um POST inocuo (action/8) so para se revelar folha. Medir se o nodeindexes (o 2o campo, categoria: 0 na raiz Menu SAP, 1 nos nos de menu, 2 na raiz Favoritos, 3 nos favoritos) ou o lsdata do MG/L carrega essa flag. Se carregar, o percurso poupa um POST por folha e o significado da categoria fica medido de quebra.
> adiado: 2026-09-05 22:39 — a sessão falhou (claude-code exited with code 1:
> MEDIDO (s4h 758/250, 06/09/2026): a flag existe e NAO esta onde o item supunha. O lsdata do MG, do L e dos dois TV e byte a byte igual em folha e pasta, e a categoria do nodeindexes tampouco separa (folha e pasta do menu sao as duas 1). A flag mora no lsdata[5] de um <td subct="HIC"> — celula SEM ct, invisivel para o despejo por [ct]: INDENT = folha, COLLAPSED/EXPANDED = pasta. Contra-prova por POST: 30/31 (INDENT -> folha 10/10; o unico erro e uma pasta DECLARADA que abre vazia, e errar para esse lado custa o POST que ja se pagava). Na lib: expansaoDoHtml (puro), arvore(s).nos[].expansao/temFilhos, e expandirNo numa folha NAO posta ({ pulou: true }). E2E contando POSTs: descobrir uma folha caiu de 1 POST/~180 ms para 0 POST/22 ms, com o acionamento intacto (SSC1). De raspao (metade da fila 85): action/8 e TOGGLE — no no aberto ele COLAPSA. Commit adt-tools 12f35a6; medicao em sap-accelerate/work/POC_webgui_arvore/medicoes/item84-flag-filhos.md

- [x] 85. COLAPSAR um no da arvore — nao medido (item 50). O container (STCS do SMEN) publica CellCollapse: action/9 no lsevents, e o irmao dele (CellExpand: action/8) foi medido e FUNCIONA com content type=node&node_key=<K>. Postar o 9 pelo mesmo molde e conferir que os filhos somem do nodeindexes. Vale porque uma arvore aberta demais reindexa tudo e engorda o delta.
> adiado: 2026-09-05 22:39 — a sessão falhou (claude-code exited with code 1:
> em andamento: metade MEDIDA no item 84 (06/09/2026): o action/8 (CellExpand) e TOGGLE — postado num no ja EXPANDED ele COLAPSA (Menu SAP: 11 filhos -> 2 nos, 80 ms; raw f0-action8-toggle.json). Nao precisa de action/9. Falta: expor colapsar na lib (colapsarNo? ou expandirNo com o toggle documentado) e medir se o action/9 do lsevents do container tambem existe.
> MEDIDO e ENTREGUE (s4h 758/250, SMEN, 06/09/2026). O action/9 (CellCollapse) EXISTE — mesmo content type=node&node_key=<K>, 70-84 ms — e o item 84 estava certo no fato e errado na conclusao: o que importa e a ASSIMETRIA dos irmaos. 6 POSTs numa sessao sobre Favoritos: action/9 em EXPANDED COLAPSA (15->13 nos, 84 ms); action/9 repetido, em COLLAPSED virgem e em FOLHA e ACEITO e nao faz nada (IDEMPOTENTE); action/8 em COLLAPSED expande e em EXPANDED COLAPSA (TOGGLE, reconfirmado). Ou seja, quem repetisse o action/8 para garantir aberto FECHARIA o no. Na lib: batchColapsarNo/colapsarNo (nao posta em no ja fechado) e expandirNo agora PULA em no ja EXPANDED — correcao, nao economia. E2E pela lib: colapsarNo(Root) levou 22->4 nos em 83 ms e o delta do POST seguinte de 304.231 B para 214.393 B (-29,5%, ~5 KB por no visivel), reabrir restaurou os 22 nos com Escritorio ainda aberto (o servidor guarda a expansao de dentro), colapsar/expandir repetidos = 0 POST. +2 testes puros (63). Commit adt-tools d0d7d8c; medicao em sap-accelerate/work/POC_webgui_arvore/medicoes/item85-colapsar.md (scripts colapsar.mjs e e2e-colapsar.mjs). Abriu #134 (a suite ja estava vermelha no HEAD) e #135 (action/901 RequestData e action/62 do container).

- [x] 86. A arvore do SAP Easy Access pela via do NAVEGADOR (CDP) — nao portada (item 50). Os ids do DOM sao os mesmos (tree#C105#<n>#1#1#i) e o lsdata do STCS traz o nodeindexes igual, mas o gesto de la e o duplo clique sintetico (Input.dispatchMouseEvent com clickCount 1 e 2), que a POC deu na mao. ⚠ Medido no item 50: o clique SIMPLES nao gera POST nenhum (o action/41 fica enfileirado no renderer e so sai no proximo gesto que posta) e clicar no icone (L) NAO expande. Portar arvore/expandirNo/acionarNo/navegarArvore para o webgui.mjs, ou decidir que esta via nao precisa deles.
> adiado: 2026-09-05 22:40 — a sessão falhou (claude-code exited with code 1:
> PORTADO: arvore/expandirNo/colapsarNo/acionarNo/navegarArvore agora existem no webgui.mjs com a MESMA assinatura da via HTTP; as puras (indiceDoNo, containerDaArvore, arvoreDosBrutos, acharNoDaArvore, TETO_ARVORE) migraram para o webgui.mjs e o its.mjs as importa/reexporta. A leitura do DOM da a mesma arvore de graca (~15 ms) — inclusive a flag temFilhos, que exige despejar [subct=HIC] alem de [ct]. O gesto e UM so e e TOGGLE (duplo clique: expande o fechado, fecha o aberto, aciona a folha em 54 s), entao as guardas de estado viraram SEGURANCA. LIMITE MEDIDO: a RAIZ nao fecha por gesto (Root e Favo postam o action/2 e nada muda; nivel 1 fecha em 253 ms) — para isso so a via HTTP (action/9); fila 136. Correcao ao item 50: o ICONE expande com duplo clique (o que nao expande e o clique simples). A espera fecha no round-trip (item 80): gesto inocuo caiu de 30 s para 1,8 s. Medicao: sap-accelerate/work/POC_webgui_arvore/medicoes/item86-arvore-navegador.md; 7 testes novos no webgui.test.mjs; receita-webgui.md atualizada.

- [x] 87. O connect grava no cache uma sessao que NASCEU MORTA — achado do item 52. session.mjs:94 faz newSession+fetchToken e so exige token para gravar .sessao.json; no teto de sessoes o logon responde 200 com token, o cookie vem sem SAP_SESSIONID e o CLI grava um cookie inutil: todo comando seguinte da 400 Service nicht erreichbar e porQueFalhou nem e consultado. Aplicar o mesmo criterio do item 52 (a sessao ja vem marcada com nasceuMorta) e falhar no connect com a mensagem que diz esperar o security_session_timeout, em vez de cachear.
> adiado: 2026-09-05 22:40 — a sessão falhou (claude-code exited with code 1:
> conectar() agora lanca erroSessaoMorta ANTES do gravarSessao — o criterio do item 52 chegou ao connect: no teto de sessoes o logon vem com token e cookie sem SAP_SESSIONID, e esse cookie inutil nao vai mais para o .sessao.json. session.test.mjs (novo, 4 testes) cobre a decisao de disco e protege o .sessao.json real de quem roda a suite; sem a correcao 3 dos 4 falham. Catalogo (tipos/_esquema.mjs -> docs/tipos.md) atualizado. Commit adt-tools 6fa494a. Achados registrados: item 138 (adt-git tem copia atrasada da lib, com os bugs 52 e 87 ainda dentro) e item 139 (its.test.mjs:516 falha desde fe4f2c8 — regressao pre-existente, nao tocada aqui).

- [x] 88. A sonda do WebGUI/ITS nao NOMEIA o teto de sessoes — achado do item 52. interpretarSonda (webgui.mjs:307) devolve ok:false causa:inesperado motivo "200 sem cookie de sessao e sem pagina de logon" exatamente no estado em que a sessao nasce morta, e its.abrir lanca com esse texto — que manda procurar SICF. Medir se 200-sem-SAP_SESSIONID no no do webgui tem outra causa alem do teto antes de nomear; se nao tiver, dar a causa propria e o mesmo texto acionavel do erro SessaoNasceuMorta.
> adiado: 2026-09-05 22:40 — a sessão falhou (claude-code exited with code 1:
> MEDIDO E DESMENTIDO: 200-sem-SAP_SESSIONID no no do webgui TEM outra causa alem do teto, e ela e o SUCESSO — um 2o GET dentro da sessao ja aberta devolve a mesma assinatura HTTP (200, sem SAP_SESSIONID no Set-Cookie, sem logon) com os MESMOS 36 487 bytes de tela. Logo NAO foi nomeado como teto. O que separa tela de formulario e o shell webguiform0 (ausente nos 23 KB do logon): nova causa sem-sessao-nova, com as duas leituras no motivo e o mesmo texto acionavel do SessaoNasceuMorta (SAIDA_SESSAO_MORTA extraida em sap-connection.mjs) para quem nao mandou cookie; inesperado fica so para o 200 que nao e nem logon nem tela. its.abrir para de abrir toda recusa com "canal WebGUI indisponivel" (prefixoDaRecusa) — era essa frase que mandava procurar o no na SICF. Varredura completa (credencial errada, sem auth, senha errada, mandante 999, idioma ZZ, transacao inexistente, sem sap-client) na medicao. Commit adt-tools 1b926e5; medicao sap-accelerate/work/POC_webgui_sonda_causa/medicoes/item88-arvore-de-causas-da-sonda.md. ABERTO -> fila 142 (o no SEM cookie no estado doente, custa ~30 min de laboratorio). Achados novos -> fila 140 (o logoff da sonda nao fecha: urlWebgui le cfg.idioma, a lib usa cfg.lang, +1 sessao por sonda, medido) e 141 (teste vermelho pre-existente em its.test.mjs:516).

- [x] 89. Caminho STATELESS para curar sessão de segurança envenenada — wrapper FM RFC sobre CL_HTTP_SECURITY_SESSION_ADMIN=>ABORT_SECURITY_SESSION filtrado por userid = sy-uname E timeout_check = 2 (CO_SESSION_UNUSED). Medido no item 53 que o ABORT cura instantaneamente (42 sessões -> 2, 40 de 40 sem erro), mas só foi provado por classrun, que é ADT, que é STATEFUL — justo o canal que morre no estado doente. Com um FM RFC-enabled o SOAP (único canal que sobrevive, M6 do item 28) alcança a cura, e aí a bisseção do teto (item 53) deixa de custar os 30 min de espera e vira uma rodada de segundos. Este item é o que DESTRAVA o 53.
> adiado: 2026-09-05 22:41 — a sessão falhou (claude-code exited with code 1:
> ENTREGUE (06/09/2026, s4h 758/250): a cura de sessao de seguranca envenenada ANDA por canal STATELESS — 15 de 15 abortadas, 0 erros, 422 ms, contra os 30 min de espera do http/security_session_timeout. O item 53 deixa de custar janela combinada. Codigo: buildSecuritySessionCureSource(name) em adt-client.mjs (irmao do buildBdcWrapperSource) + curarSessoesDeSeguranca(cfg,{fm,dryRun,pouparCorrente}) e o parser puro parseCuraSessoes(xml) em rfc-soap.mjs; commit 71cf69b no adt-tools; 3 testes novos contra o XML REAL. DOIS achados que a medicao por classrun do item 53 NAO podia dar: (1) a regra 3 daquele item (o filtro nao mata "nem a propria") era ARTEFATO DO CANAL — por SOAP a propria requisicao E uma sessao nao usada e entra na lista de alvos, dai GET_CURRENT_SESSION_CONTEXT no wrapper (achado da public section) e o parametro pouparCorrente; (2) abortar o proprio contexto e INOFENSIVO (HTTP 200, resposta completa, 287 ms, requisicao seguinte normal), entao o default e nao poupar e a limpeza fica 100%. Aritmetica fecha na unidade em todos os passos, e o dry-run e o controle negativo. Contraprova em outro canal/LUW: classrun ok=true depois da cura (o ADT nao caiu) e a contagem pela CLASSE bate com a pela TABELA. REGRA QUE SAI DAQUI, e vale para a rodada do 53: o wrapper e um EXTINTOR — deployFunctionModule vai por ADT (stateful), entao no estado doente o FM NAO PODE SER CRIADO; ele tem que existir ANTES da quebra. Por isso FUGR YJBV_POC_FG_SEC + FM YJBV_POC_FM_SECCURA (FMODE=R) FICAM no s4h 250 ($TMP), de proposito; apagada so a classe de contraprova YJBV_POC_CL_SECCONTA (deleted:true, ausente da TADIR em outra LUW). ABERTO, e e o que o 53 mede PRIMEIRO quando o canal quebrar: o SOAP autenticado consegue CRIAR sessao de seguranca com o teto ja estourado? (o M6 do item 28 so mediu Basic SEM cookie). Se nao conseguir, a espera de 30 min volta. Medicao: sap-accelerate/work/POC_sessoes_icf/medicoes/item89-cura-stateless.md (scripts i89-a..h); receita: packages/adt-client/docs/receita-fm-rfc-wrapper.md secao "O segundo wrapper". Achado colateral -> item 143 (its.test.mjs vermelho, PRE-EXISTENTE, confirmado por stash).

- [x] 90. A lib vaza UMA sessão de segurança HTTP por requisição stateless — medido no item 53: 20 chamadas SOAP RFC = +21 contextos SECURITY_CONTEXT com TIMEOUT_CHECK=2, exatamente 1,00 por requisição, que o logoff NÃO remove e que só morre no EOL fixo de start+1800 s. Contra o teto por usuário (default declarado 100 em CL_HTTP_SECURITY_SESSION=>CO_SESSION_LIMIT_UNUSED_DFLT, ICF_SESSION_CNTL vazia neste sistema), um laço de ~100 leituras SOAP em 30 min derruba o canal stateful do usuário. Decidir o remédio na lib (reusar sessão stateful onde hoje há laço stateless, e/ou limpar as próprias não usadas ao fim) e documentar em packages/adt-client/docs/canal-soap-rfc.md. Medição: sap-accelerate/work/POC_sessoes_icf/medicoes/item53-o-recurso-que-esgota.md
> adiado: 2026-09-05 22:41 — a sessão falhou (claude-code exited with code 1:
> REMÉDIO: reuso da sessão de segurança no canal SOAP (cookie SAP_SESSIONID guardado e reenviado), default em callFunction/ping/readTable/callBapi — transparente nos ~150 call sites. Medido no S4H 758/250 em 06/09/2026: 10 chamadas SEM cookie = +10 sessões não usadas em 1285 ms; COM cookie = +0 e 737 ms (1,00 -> 0,00 por chamada, 43% mais rápido), com controle negativo reusarSessao:false no mesmo processo. NÃO torna o canal stateful (2ª chamada sem Set-Cookie, nenhum sap-contextid, SM04 delta 0): cada POST segue sua LUW e a regra de escrita não muda. Cookie morto é inofensivo (Basic re-autentica; 200 com lixo/vazio/outro SID), por isso sem retry. Somam-se encerrarSessaoSoap (melhor esforço) e esquecerSessaoSoap; absorverSetCookie é puro, com 4 testes. Documentado em packages/adt-client/docs/canal-soap-rfc.md, seção "A sessão de segurança: por que a lib manda o cookie de volta". Commit adt-tools 36a8e14. Achados viraram os itens 144 (logoff do ICF responde 500 neste sistema desde 04/09), 145 (auditar os outros canais HTTP da lib) e 146 (its.test.mjs:516 quebrado, pré-existente). Medição: sap-accelerate/work/POC_sessoes_icf/medicoes/item90-o-remedio-reuso-de-sessao.md

- [x] 91. Por que o SID do container da arvore MORRE apos o /nSMEN na mesma sessao? Medido no item 54 (s4h 758/250, 05/09/2026): depois de entrar numa transacao pelo favorito e voltar com comandar(s, '/nSMEN'), o action/41 no SID que arvore(s) devolve vem multipart e o Favorites > Delete responde -103 failed to fire action: not available; em sessao NOVA os mesmos dois POSTs apagam em 165 ms. E o SID que mudou (tree#C105 -> outro) ou o delta do /nSMEN que vem incompleto? Se for o primeiro, arvore()/acionarNo() tem um erro silencioso depois de qualquer ida-e-volta ao menu. Comparar o SID do container antes e depois, no bruto (POC_webgui_tstcc/medicoes/raw/c2-YJBV_WGON.json).
> adiado: 2026-09-05 22:41 — a sessão falhou (claude-code exited with code 1:
> NENHUMA das duas hipoteses. O SID do container NAO muda (wnd[0]/usr/cntlIMAGE_CONTAINER/shellcont/shell/shellcont[0]/shell, id tree#C105, identico antes e depois) e o delta do /nSMEN NAO vem incompleto (242 KB, aParams, 1 GuiTree, nodeindexes 16, 30 TV, as mesmas 15 chaves, parcial false) — e mesmo assim action/41, /8 e /2 respondem multipart -103 failed to fire action: not available. O que morre e o BINDING de acao do controle no ITS: a resposta da volta pinta a arvore mas ainda nao a registra como acionavel. Sondas: /nSMEN de SMEN para SMEN nao mata (nao e o okcode, e ter SAIDO); tres action/41 recusados seguidos continuam -103 (o POST recusado nao aquece); um postar(s,[ESTADO]) sozinho (~78 ms) CURA; enter tambem cura; o resto da tela obedece normalmente durante o -103; o id tree#C105 no lugar do SID da -102 (o SID e o endereco certo). arvore() nao tem erro de leitura — quem paga e o gesto no primeiro tiro. Medicao: sap-accelerate/work/POC_webgui_sid_smen/medicoes/item91-sid-arvore-smen.md (scripts a-sid-smen.mjs, b-ressuscitar.mjs, c-quantos-posts.mjs). Achado gravado no comentario medido da secao da arvore em packages/adt-client/its.mjs; a nota 2 do item54 foi corrigida. Conserto na fila: item 147; anomalia -102: item 148.

- [x] 92. Os FAVORITOS do SAP Easy Access viram gesto da lib? O ciclo inteiro esta medido por HTTP (item 54): navegarMenu('Favorites > Insert Transaction') -> popup wnd[1] de 1 campo -> preencher+enter -> o no novo -> acionarNo; e apagar e action/41 (a selecao) + Favorites > Delete. Hoje isso e receita (receita-webgui.md § action/41 e os FAVORITOS), nao API. So promover se doer uma terceira vez — a pergunta a responder e se algum uso REAL precisa de favorito, ou se ele so serviu de caminho de medicao.
> adiado: 2026-09-05 22:41 — a sessão falhou (claude-code exited with code 1:
> NAO PROMOVER — decidido em 06/09/2026, sem medicao nova (a pergunta era de historico de uso, e o historico esta no FS). O gesto de ESCREVER favorito apareceu UMA vez: a fase C2 do item 54. A C3 e a limpeza do favorito que a C2 criou e o item 91 investiga o -103 que essa limpeza produziu — uma dor e os seus ecos, nao tres dores; a regra de tres nao foi atingida. E o uso era instrumental: a transacao $TMP recem-criada nao esta em menu de area nenhum (fase C1: zero YJBV nos 15 nos), entao Favoritos era o unico ramo da arvore que ela alcancava — caminho de medicao, nao requisito. Contra o que a lib ja entrega (numeros do MESMO item 54): entrar numa transacao pelo favorito custa 129-208 ms de insercao + 92-108 ms de acionamento = 221-316 ms, escreve estado persistente do usuario e desfazer exige sessao nova, contra 103-119 ms de comandar(/n<TCODE>) sem efeito colateral e alcancando transacao fora de qualquer menu; saber se o tcode existe ja e readTransaction ou o proprio /n, com a mesma mensagem; e LER/ACIONAR favorito que ja existe e API desde o item 50 (arvore, acharNoDaArvore, navegarArvore([Favoritos, ...]), acionarNo). O que faltava de API era so ESCREVER favorito — o unico gesto que ninguem pediu, e o que cobra em outra moeda: favorito e estado persistente do usuario no sistema do cliente, uma API que os cria deixa lixo no SAP de quem roda a lib. GATILHO DE REABERTURA gravado na receita: um uso real fora de medicao que precise ESCREVER favorito (reproduzir ambiente de usuario final, testar autorizacao de menu) — hipotese, nao dor, nao apareceu em nenhuma sessao. Quando aparecer, a receita ja e a implementacao. Receita: packages/adt-client/docs/receita-webgui.md secao "Por que isto e RECEITA e nao API (item 92)" + o indice de gestos marcando o action/41 como receita POR DECISAO. Commit adt-tools 3ed7da6.

- [x] 93. readTransaction nao le a TSTCC — o flag que a propria lib GRAVA nao volta no assert. tran.mjs le TSTC/TSTCT/TSTCP/TADIR por readTable; para conferir o S_WEBGUI/S_WIN32 em outra LUW foi preciso readTable(cfg, 'TSTCC') a mao (itens 29 e 54). Acrescentar TSTCC ao readTransaction fecha o ciclo do gui do deployTransaction (que hoje so tem a palavra do driver, na mesma LUW). Custo: mais um readTable no Promise.all que ja existe.
> adiado: 2026-09-05 22:42 — a sessão falhou (claude-code exited with code 1:
> Medido 06/09/2026, S4H 758/250 (POC_tran_tstcc). readTransaction agora lê TSTCC no Promise.all que já existia; deployTransaction devolve guiBanco/guiDivergencias e leva a TSTCC para dentro do ok; deleteTransaction exige TSTCC ausente. Valores medidos: S_WEBGUI é 1 (não X), S_WIN32/S_PLATIN são X, e a linha existe mesmo com tudo desligado. Prova de que o assert morde: pedir { win: true } numa transação existente (ALREADY_EXIST, a FM não altera) dá ok:false com a divergência — antes dava ok:true. Em existed só valem os flags explícitos. Puro e testado: GUI_TSTCC + conferirGui (tran.test.mjs). adt-tools 41fdf3c; receita-tran.md § o gui volta no assert; medição em sap-accelerate/work/POC_tran_tstcc/medicoes/item93-tstcc.md. Aberto: S_SERVICE/S_PERVAS não têm parâmetro na FM.

- [x] 94. fecharMenu NAO fecha o menu depois de um abrirMenu — e gasta 15,5 s em silencio. Medido no s4h 758/250 em 05/09/2026 (item 55, cenario T4b da POC_webgui_gestos_valores): apos abrirMenu(s) com sucesso (6 itens de nivel 0), fecharMenu(s) devolveu false e levou 15487 ms — as 3 tentativas do toggle, cada uma esperando 4 s pelo itensDeMenu esvaziar, sem nunca esvaziar. O comandar seguinte funcionou assim mesmo, entao nao trava nada — mas quem chamar fecharMenu explicitamente paga 15 s e recebe um false que ninguem le. Investigar se (a) o clique no cua2sapmenu_btn com o menu ABERTO nao chega ao toggle (o popup do menu pode estar por cima do botao — apontar/elementFromPoint diria), ou (b) o menu fecha mas os POMNI ficam no DOM (display:none), e ai o assert do itensDeMenu e que esta errado. O JS_ITENS_DE_MENU nao filtra por visibilidade.
> adiado: 2026-09-05 22:42 — a sessão falhou (claude-code exited with code 1:
> Era a hipotese (b), e o mecanismo e mais especifico: ao FECHAR, o renderer nao apaga nem esconde o popup — empurra para rect.y = -100000 com display:block e visibility:visible intactos, e os <tr> dos itens seguem medindo 289x32. O filtro do JS_ITENS_DE_MENU (offsetWidth || offsetHeight) lia "6 itens visiveis" com o menu ja fechado no PRIMEIRO clique (o aria-expanded do cua2sapmenu_btn ja era false) — dai os 15,5 s e o false. Hipotese (a) DESCARTADA: com o menu aberto, apontar no cua2sapmenu_btn devolve coberto:false / noPonto:cua2sapmenu_btn; o popup nasce ABAIXO da barra. Achado colateral medido: clicar em area vazia NAO fecha o menu (6 itens antes, 6 depois) — o toggle e o unico gesto de fechamento deste canal. Correcao: o filtro testa o RETANGULO contra o viewport. Depois dela, fecharMenu = true em 21 ms (era false em 15487 ms), abrir->fechar->abrir coerente (18 ms / 330 ms / 6 itens), navegarMenu segue chegando na SA38 e o T4b da POC de gestos leva o valor com fechar custando 14 ms. Contra-prova do true rapido: 20 amostras a cada 150 ms mostram o menu ABERTO e estavel apos abrirMenu — o toggle e DOM puro, sem rede. adt-tools 02c85d5 (webgui.mjs + receita-webgui.md, 6a armadilha). Medicao: sap-accelerate/work/POC_webgui_fecharmenu/medicoes/item94-fecharmenu.md

- [x] 95. Abrir uma transacao NAO da tela limpa: os campos voltam preenchidos com o valor da ultima execucao do usuario. Medido em 05/09/2026 (POC_webgui_okcode_valores, fases K e L): a SE16 aberta por URL em sessao NOVA do ITS devolveu ctxtDATABROWSE-TABLENAME ja com a tabela do cenario anterior (K1 mandou TSTC, K2 abriu com TSTC, K3 abriu com T000), e /nex + URL nova nao limpam. E parametro SET/GET da memoria do usuario, nao cache do navegador — mas isso e HIPOTESE, nao foi medido contra a memoria. Importa para roteiro de automacao: um passo que so preenche parte dos campos herda o resto da ultima vez, e o valor herdado NAO aparece no fio (o renderer so publica value/ quando difere do que o servidor mandou), entao o roteiro fica com um valor que ninguem escreveu e que a instrumentacao nao mostra. Medir: em quais telas isso vale, se ha gesto de limpar (okcode? parametro na URL?) e se a lib deve expor "abrir limpo".
> adiado: 2026-09-05 22:42 — a sessão falhou (claude-code exited with code 1:
> A hipotese estava meio errada: e SET/GET parameter da SESSAO EXTERNA, nao da memoria do usuario — a USR05 nao muda (RFC_READ_TABLE, outro canal) e navegador novo abre limpo. Herda em 6/6 telas (SE16, SE11, SE38, SE37, SE24, SM30) e CRUZA transacoes (TADIR digitado na SE16 apareceu na SE11). Gesto que limpa: parametro VAZIO na URL (*SE16 DATABROWSE-TABLENAME=), que ZERA a memoria — /n e sap-sessioncmd=open NAO limpam. Virou codigo: limpar em expressaoTransacao/urlWebgui/abrirTransacao/its.abrir + renovarSessao(s), receita-webgui.md § Entrar na tela LIMPA, testes puros (adt-tools 6cc8403). Medicao: sap-accelerate/work/POC_webgui_tela_limpa/medicoes/tela-limpa.md

- [x] 96. IDEIA (achado do item 57, 05/09/2026): o snippet do usageSnippets traz no atributo matches um access<Modo> — nas 45 ocorrencias medidas no s4h 250 so apareceram accessRead e accessUnknown. SE accessWrite existir, e ele que responde a pergunta que o where-used de elemento de dados nunca respondeu: QUEM GRAVA neste campo (hoje a lib diz explicitamente que nao responde, e manda para o canal de fonte/debug). Medir: where-used expandido de um campo com escrita conhecida (ex.: um campo de tabela Z gravado por um programa Z do proprio laboratorio, onde a escrita e certa) e ver o que vem no matches. Se accessWrite existir, expor filtro escritas/leituras em whereUsed e corrigir o aviso do README e do search.mjs.
> adiado: 2026-09-05 22:43 — a sessão falhou (claude-code exited with code 1:
> MEDIDO (s4h 250, 06/09/2026, laboratorio Z com escrita certa, criado e apagado pela POC): accessWrite EXISTE, e sempre com gradeComponent. Mas a hipotese do item cai pela metade em tres pontos, todos medidos: (1) o matches e uma LISTA por TOKEN e o parser lia o primeiro accessw+ da string, que na linha de escrita e o token da CLASSE (accessUnknown) e nao o do atributo (accessWrite) - a lib ESCONDIA a escrita; (2) o acesso e sobre a VARIAVEL, nao sobre o BANCO: INSERT ... FROM saiu accessRead e UPDATE ... SET saiu accessUnknown, entao o filtro leitura chega a devolver o INSERT; (3) no where-used do DATA ELEMENT nao ha o que filtrar - vieram so as duas DECLARACOES, ambas accessUnknown, porque ali quem e referenciado e o TIPO e nao o valor; o aviso do README continua valendo para o DE, por outro motivo. Entregue: parseMatches (puro), campo acessos com o trecho de cada offset, acesso/grade do token decisivo, whereUsed({ expandir: true, acesso: escrita|leitura }) com usosAntesDoFiltro, filtrarPorAcesso puro, 3 testes com o XML real medido, comentarios do search.mjs e aviso do README corrigidos. adt-tools 3a56bd5; medicoes em sap-accelerate/work/POC_whereused_escrita/medicoes/item96-conclusao.md

- [x] 97. IDEIA (achado do item 57, 05/09/2026): o discovery do ADT anuncia /sap/bc/adt/repository/informationsystem/whereused — um endpoint SEPARADO do usageReferences e do usageSnippets, na mesma colecao, com templateLinks VAZIO (sem template de URI). Nunca foi chamado por esta lib e nao se sabe o que ele faz nem que corpo aceita. Medir por sonda (GET, POST vazio, OPTIONS) no s4h 250 e ler a mensagem de erro do servidor, como foi feito no 57 para descobrir o formato do usageSnippets. Vale para saber se ha um where-used mais barato ou com escopo configuravel que o usageReferences.
> adiado: 2026-09-05 22:43 — a sessão falhou (claude-code exited with code 1:
> MEDIDO no s4h 250 em 06/09/2026 (POC_whereused_endpoint): o /whereused NAO e uma variante do usageReferences — e o where-used do RIS CLASSICO (o do SE84/SE11), e tem SIM escopo configuravel. O contrato saiu do servidor, nao de chute: OPTIONS 400, GET 405 em todo Accept (so POST), e POST responde 500 com.sap.adt.ris "Kein Service fur ID gefunden" IDENTICO com corpo vazio e com corpo cheio — o handler decide antes de ler o corpo, logo falta um QUERY PARAM. Parei de sondar e li o ABAP: CL_RIS_ADT_RES_APP roteia para CL_RIS_ADT_RES_WHERE_USED, cujo post e um CASE sobre RIS_REQUEST_TYPE com tres acoes (WHERE_USED, WHERE_USED_LAZY, CODE_LINES); o corpo e application/xml PURO e SEM namespace, com o esquema exato da ST RIS_ST_ADT_REQUEST (<ris_request> com object_type/scope_object/payload). E nao se adivinha codigo nenhum: /fullnamemapping?RIS_REQUEST_TYPE=MAP_URI_TO_RIS_REQUEST&uri= traduz URI ADT em ris_data_request, e /metadata?RIS_REQUEST_TYPE=WUL_TYPES_COMPLETE lista os 32 escopos validos do objeto. ARMADILHA que me custou tres medicoes: <object_type> e o tipo do RESULTADO (onde procurar), nao o do alvo — trocar com o <payload> responde 200 com ZERO BYTE, sem erro e sem contador, e lista vazia fica indistinguivel de "nao ha usos" (a contraprova que separou canal ruim de erro meu foi MATNR: 41.226 usos no outro canal nao podem virar 0). CUSTO, mesmo DTEL e mesma sessao: usageReferences 1 chamada 913ms 29 refs com containers e 12 nos que ainda pedem o usageSnippets; /whereused 10 chamadas 4884ms 18 usos JA resolvidos (enclosing+object+uri), sem colapsado — os dois CONCORDAM nos objetos reais. Ou seja: mais barato so na pergunta estreita (que CLASSES usam este DE: 54ms x 913ms, 17x) e ~5x mais caro para varrer tudo. WHERE_USED_LAZY engana no nome: traz MAIS (34,5MB x 7,6MB no T000) e e o unico que le scope_object/full_name. Registrado como doc no fim do search.mjs (nao implementado — a decisao e do item 159). Nao medido e dito: corpo do CODE_LINES (400), efeito do scope_object (inconclusivo), alvo fora de DTEL/TABL. Abriu 156 (CODE_LINES), 157 (scope_object), 158 (call sem timeout — o fetch morre aos 300s do undici, ja mordeu no item 57), 159 (implementar) e 160 (suite vermelha preexistente). Medicao: sap-accelerate/work/POC_whereused_endpoint/medicoes/item97-whereused.md

- [x] 98. ACHADO do item 58 (05/09/2026): a lib AFIRMA algo falso sobre popup — corrigir no CODIGO. O comentario de its.mjs (secao dos blocos do delta, ~linha 350) e o campo aviso de montarTela dizem "com POPUP aberto a wnd[0]/usr NAO vem no delta". E falso: reprocessando os brutos com telaDoDelta, SE16 com SPOP aberto da 34 campos (i58-b-nend.txt) e SE16 com /o da 34 (b-o.txt); as leituras de 0 do item 21 vieram todas do SMEN — e o SMEN SEM popup nenhum tambem da 0 (c5-n.txt), porque o menu nao tem campos de dynpro. O que de fato esvazia e o bloco steploop0; os campos vem noutro bloco do mesmo delta. Fazer: (a) reescrever o comentario de its.mjs; (b) decidir o destino do aviso — hoje so dispara quando campos.length===0, entao ele mente justamente na tela que nao tem campos (remover, ou trocar por um aviso do que foi medido de verdade); (c) teste puro sobre os brutos fixando a regra. A receita ja foi corrigida no item 58.
> adiado: 2026-09-05 22:43 — a sessão falhou (claude-code exited with code 1:
> Medido reprocessando os brutos com telaDoDelta (POC_webgui_popup e POC_webgui_okcode, medicoes/raw/*): o popup NAO tira a wnd[0]/usr do delta — SE16 com SPOP (i58-b-nend.txt) e com /o (b-o.txt) dao 34 campos e os mesmos 85 SIDs do sem-popup (c2-t000.txt); SE38 com /nend (f0-nend.txt) da 1 campo como o c4-nse38.txt. Os 0 do item 21 eram do SMEN, que da 0 com e sem popup (c5-n.txt) porque o menu nao tem dynpro. O steploop0 nao esvazia: e sempre a mesma casca de 119 bytes nos 12 brutos; a dynpro mora no bloco userpanel. Feito: (a) comentario dos blocos do delta reescrito; (b) o aviso virou de ENDEREÇAMENTO (campos/botoes sao a wnd[0] atras do modal, o conteudo da modal esta em popup) e agora dispara sempre que ha popup; (c) teste puro novo sobre a estrutura de blocos do f0-nend.txt. Receita-webgui atualizada. Suite verde: 934 testes. Commit adt-tools 95c18f3.

- [x] 99. navegarArvore com { acionar: false } devolve mudou: false mesmo tendo EXPANDIDO no (o expandirNo dispara POST e muda a tela). Achado em 05/09/2026 ao dar significado preciso ao mudou (item 59): agora que mudou e o carimbo ANTES x DEPOIS, esse false e mentira quando expandidos.length > 0. Decidir: propagar o mudou do ultimo expandirNo, ou renomear o campo nesse ramo (ele responde acionou, nao mudou).
> adiado: 2026-09-05 22:44 — a sessão falhou (claude-code exited with code 1:
> PROPAGADO, nao renomeado: o ramo { acionar: false } agrega o mudou das EXPANSOES (agregarMudou, puro em webgui.mjs, reexportado pelo its.mjs) — true se alguma mexeu, null se inconclusiva, false se nada postou. Nao e o do ULTIMO expandirNo: a expansao que muda a tela costuma ser a do MEIO do caminho. Junto: expandidos so recebe a expansao que POSTOU (o ultimo passo empurrava a chave sem olhar o pulou). Nas duas vias + teste que falha com o false fixo; 935 testes passam. Doc: packages/adt-client/docs/receita-webgui.md § O mudou do navegarArvore depende do RAMO. Commit 99bca45.

- [x] 100. A via do NAVEGADOR (webgui.mjs) nao tem o par do item 59: acionar/comandar devolvem mudou (carimbo de DOM) mas NAO devolvem a mensagem da statusbar nem avisam quando o popup continua aberto depois de uma acao que nao mudou nada. Na via HTTP a mensagem do ABAP era a explicacao do falso positivo (OK: Nao se pode selecionar codigo de funcao). Medir se o carimbo de DOM do webgui.mjs inclui a barra de mensagem — se incluir, ele tem o falso NEGATIVO simetrico (mensagem nova conta como tela mudou).
> adiado: 2026-09-05 22:44 — a sessão falhou (claude-code exited with code 1:
> MEDIDO e ENTREGUE no s4h 758/250 em 06/09/2026. A resposta: o carimbo do DOM inclui a barra de mensagem POR ACIDENTE, e pelas DUAS portas — na MESMA tela SE38 a mensagem NASCER conta como tela mudou (nEl 630->632, falso positivo) e o TEXTO dela trocar NAO conta (msgbar no char 317, o carimbo le 300, falso negativo: a lib diz mudou:false e nunca mostra o ERROR: O programa nao existe). E o char 317 e desta tela: depois de um /n a tela encurta, a msgbar cai no char 58 e passa a contar — o mesmo evento conta ou nao pelo comprimento do cabecalho. O par do item 59 aqui e PIOR que na via HTTP: com o SPOP do /nend aberto, F12 devolve respondeu:false (a tecla nao sai do navegador, trava do item 13), popup ainda em wnd[1], e mesmo assim mudou:true — o gesto liga/desliga itens de menu dentro dos 300 chars e o carimbo OSCILA entre dois valores, 2/2. Contra-prova: em repouso o carimbo e estavel (4 leituras/7,5 s = 1 valor, com e sem popup), logo a oscilacao e do gesto. ENTREGUE: comandar/acionar/clicar devolvem { mensagem, janela } (JS_DESPEJO_DA_ACAO + estadoDaAcaoDe + fecharGesto, o par do mensagemDosSids + janelaAtiva do its.mjs) e sai AVISO alto quando o mesmo popup continua na frente e a acao nao conseguiu nada — condicao MAIS LARGA que a da via HTTP (!respondeu OU mudou===false) por medicao: presa a mudou===false ela perderia o caso da fase D. Achado lateral corrigido: lerTela().janela nunca via popup nenhum (era dePapel(janela)[0], sempre wnd[0]) — agora e a janela ATIVA. Contraste com o item 58 medido dos dois lados: com o SPOP aberto, comandar(/nSE38) pega em 170 ms na via HTTP e no navegador devolve respondeu:false pagando 21 s de teto (2/2); acionar(s,12) nem tenta, o btn[12] sai do DOM com a modal aberta. O carimbo NAO foi trocado (ficou documentado no JS_CARIMBO): virou o item 162; clicar/acionar sem respondeu virou o item 163. adt-tools 4d03d4d; webgui.test.mjs +3 (97), suite 938 verde; receita-webgui.md com a secao do que o gesto devolve. Medicao: sap-accelerate/work/POC_webgui_mensagem/medicoes/item100-mensagem.md (scripts fase-a..f, brutos raw/*.json + PNGs).

- [x] 101. O pedagio ainda PENDURA: toda chamada do canal GUI (rodarGui, sessoesAbertas, lerTela, fecharSapGui) espera o timeout inteiro com o popup de pe, o execFile mata o cscript e o popup fica ORFAO na tela — a chamada seguinte empilha mais um (medido item 34: tres sondas, tres orfaos). O item 61 deu ao vazio o NOME certo (pede-autorizacao) mas so DEPOIS do prazo. Falta vigiar as janelas ENQUANTO o cscript roda (janelasSapGui + ehPopupAutorizacao; primeiro poll ~3 s, para custar zero no caminho feliz com WarnOnAttach=0), abortar cedo com o erro pede-autorizacao, e decidir o que fazer com o orfao (clicar Cancelar nega a autorizacao e limpa a tela; clicar OK burlaria o aviso de seguranca do cliente — nao fazer sem combinar). Reproduzir exige RELIGAR o aviso (apagar WarnOnAttach do HKCU) e devolver o valor 0 no fim. De quebra, o VBS do fecharSapGui ainda colapsa Err.Number<>0 e Children.Count=0 no mesmo "@nada sem conexao".
> pista: O item 62 (05/09/2026) mediu o que falta para dimensionar este item: o popup aparece em 0,48-0,73 s (nao 1,4 s — aquilo era piso de amostragem), entao um primeiro poll aos ~2 s ja o pega; orfao NAO bloqueia chamada nova (com 5 de pe a seguinte abre o sexto em ~0,6 s); e destravar nao exige o botao — um SendKeys {ENTER} no dialogo em foco fechou e a chamada respondeu em 7,2 s (scripts/item62-enter.ps1). Isso vale para a decisao do orfao: OK e ENTER sao o mesmo gesto de autorizar, so Cancelar nega.
> adiado: 2026-09-05 22:44 — a sessão falhou (claude-code exited with code 1:
> FEITO em 06/09/2026 (adt-tools 8cc8d0a). O canal nao paga mais o pedagio de espera: rodarVbs vigia as janelas ENQUANTO o cscript roda (janelasSapGui + ehPopupAutorizacao, primeiro poll aos 2 s) e ABORTA ao ver o popup. Medido com o pedagio religado (WarnOnAttach apagado do HKCU e devolvido a 0 no fim; conferido), pad 13060 sem sessao: sessoesAbertas 15 s + orfao -> 5,6 s com abort aos 3,4 s e estado pede-autorizacao; rodarGui 120 s MUDO (devolvia resultados null SEM erro) -> 5,3 s lancando com e.estado; fecharSapGui 120 s + orfao -> 5,7 s. Custa ZERO no caminho feliz (6 pares alternados: 2607 ms com vigia x 2638 ms sem — o poll aos 2 s nunca chega a olhar, porque com WarnOnAttach=0 a chamada termina em ~0,8 s). DECISAO DO ORFAO: cancelarPedagio clica CANCELAR (BM_CLICK por PostMessage em GetDlgItem(hwnd,2), o IDCANCEL achado por id, nao por rotulo) — NUNCA OK, que autorizaria o script e burlaria o aviso de seguranca do cliente; ha trava dura (controle 2 rotulado OK => recusado-ok, sem clique) e nada de SendKeys ENTER (o Enter aciona o botao default, que e o OK). Opcoes vigiarPedagio/primeiroPollMs/intervaloPollMs/limparPedagio passam pelas tres portas. De quebra, o VBS do fecharSapGui separou Err.Number<>0 de Children.Count=0 (vbsFecharConexao + estado fechada|erro-scripting|sem-conexao|pede-autorizacao|expirou|mudo), e o sensor de janelas passou a emitir o hwnd. 120 testes no gui.test.mjs, 941 no pacote. Medicao: sap-accelerate/work/POC_rot_sapgui/medicoes/item101-vigia.md (brutos raw/item101-vigia.json e raw/item101-eventos.ndjson; scripts item101-vigia-aborta.mjs e item101-custo-vigia.mjs). Receita atualizada: packages/adt-client/docs/receita-gui-scripting.md § A lib nao paga mais o pedagio de espera. NAO medido: o mesmo com sessao LOGADA de pe. Achado que saiu daqui: item 164 (abrirSapGui e a unica porta que ainda ignora o estado autorizacao).

- [x] 102. Por que as POCs de 04/09 no SXD rodaram dezenas de rodarGui SEM pedagio? Agora e discordancia dura, nao folclore: o item 62 (05/09/2026) mediu o pedagio DETERMINISTICO — 12 chamadas, 12 popups, 0 destraves, nenhuma memoria de autorizacao — e o item 60 verificou que o HKCU...Security desta maquina NAO tinha WarnOnAttach naquele dia. Pelas duas medicoes, aquelas POCs deveriam ter pago pedagio a cada chamada. Hipoteses a separar: (a) havia uma pessoa clicando/teclando (o mesmo mecanismo do item 62 — e o mais barato de acreditar); (b) os roteiros mandavam TODOS os passos num rodarGui so, pagando UM pedagio; (c) o SXD tem algo que o S4H nao tem (parametro de servidor, ou politica em HKLM na rede do cliente). Comecar por (b), que se responde SEM VPN: contar quantas chamadas cscript os scripts daquele dia fazem. So (c) precisa da rede do cliente.
> adiado: 2026-09-05 22:45 — a sessão falhou (claude-code exited with code 1:
> Respondido em 06/09/2026 SEM VPN, pelo registro (transcrições do Claude Code + os próprios roteiros). A PREMISSA É FALSA: o SXD nunca apareceu no ROT — em toda a história das transcrições o canal GUI só enxergou S4H (10 respostas: 02, 04, 05 e 06/09). Não houve dezenas de rodarGui no SXD; houve 5 tentativas em 03/09 à noite (19:44–19:56 local), TODAS sem resposta (estouro de 90–120 s, saída vazia, e o cscript ainda de pé aos 25 s). Os roteiros de 04/09 à tarde (flp-gui-inspect, flp-create-dialog-inspect, flp-criar-catalogo) foram escritos e NUNCA executados. (b) refutada de qualquer forma: nenhum roteiro concentra passos — gui-tu.mjs é 1 chamada por invocação, flp-gui-inspect faz 1 rodarGui POR BOTÃO e 2 por shell. (a) também refutada por aritmética: em 04/09 11:20 (S4H 250) TRÊS chamadas seguidas do canal completaram em 9,1 s de relógio, e o caminho assistido mais rápido já medido é 4,8 s por chamada (item 34) — não havia popup para clicar. (c) perde o objeto: não existe medição do SXD para explicar, e nada disso precisava da rede do cliente. Medição: sap-accelerate/work/POC_rot_sapgui/medicoes/item102-o-sxd-nunca-existiu.md; script item102-contar-cscript.mjs; bruto raw/item102-contagem.json. A lição virou técnica na skill investigacao (Interrogar o registro) e disciplina (fato sem carimbo que atravessa itens é folclore). Abriu os itens do pedágio datado e do abrirSapGui.

- [x] 103. ACHADO do item 63 (05/09/2026): GetWindowText cross-process NAO envia WM_GETTEXT — devolve o texto em cache do gerenciador de janelas, e os dois divergem em controles do SAP GUI. Medido em raw/item63-filhos-senha.txt (dialogo Caracteristicas do atalho SAP): Button 2166208 tem txt="Connection Parameters" (ingles) e msg="Parametros de conexao" (PT); o ComboBox 3085068 devolve msg="杀ࣆscrição sistema:" (lixo). Nas janelas que lerJanelas() usa (mensagem, senha) txt e msg coincidem, entao nada quebra hoje. Vale medir: existe algum controle cujo estado a lib precise ler onde o cache mente? Se sim, psJanelas precisa de WM_GETTEXT com timeout (SendMessageTimeout) em vez de GetWindowText.
> MEDIDO em 06/09/2026 (sap-accelerate/work/POC_rot_sapgui/medicoes/item103-cache-mente.md; bruto raw/item103-*.txt|json, fotos raw/item103-fotos/). Sim, o cache mente num controle do tipo que a lib le: no dialogo Ligacao SAP GUI - logon o GetDlgItem(h,2) tem GetWindowText="&Cancel" (ingles, do template do recurso) e WM_GETTEXT="&Cancelar" (PT) — e e o mesmo id que a trava de psCancelarDialogos le para RECUSAR clique em OK. No popup do PEDAGIO, que e a janela onde a trava roda de fato, as duas vias coincidem nos 5 controles (0 divergentes em 50); titulo e Static coincidem nas 4 janelas medidas. Entregue: psCancelarDialogos passa a ler o rotulo pelas DUAS vias e falha fechada (basta uma dizer OK => recusado-ok), leitura por SendMessageTimeout(SMTO_ABORTIFHUNG, 300ms) e nao SendMessage; saida ganhou o campo botaoCache. Provado ponta a ponta contra o pedagio real: cancelados=1, botao=Cancelar, botaoCache=Cancelar, restantes=[]. psJanelas NAO muda, por medicao: o Edit de senha RECUSA WM_GETTEXT (ERROR_ACCESS_DENIED) e o buffer lido ate o NUL devolve lixo — que e a explicacao do msg="lixo"scricao sistema: do item 63 (o controle nao respondeu). Ambiente restaurado: WarnOnAttach=0, so-pad, sem orfaos. Achado novo -> item 167.

- [x] 104. O cmd() da sessao do webgui PENDURA PARA SEMPRE quando o WebSocket do CDP fecha: as promessas em pendentes nunca sao rejeitadas. Medido em 05/09/2026 (item 65, POC_webgui_porta/medicoes/raw/porta-presa-antes.txt) — com duas sessoes anexadas ao mesmo Chrome, o fechar() de uma matou o target da outra e o await a.cmd(...) seguinte nunca resolveu nem rejeitou (o Node saiu com "Detected unsettled top-level await"). Vale alem daquele cenario: Chrome morto por crash, OOM ou kill externo trava qualquer script que esteja em await. Corrigir: ws.onclose/onerror rejeitando todas as pendentes com a causa, e teto por comando.
> criarCanalCdp em webgui.mjs: onclose/onerror derrubam todas as pendentes com a causa, teto por comando (TETO_CMD_CDP_MS=120s, tetoCmdMs por sessao, tetoMs por chamada), comando novo em canal morto rejeita na hora (ws.send em socket fechado e descartado em silencio pela spec) e handshake com onclose+teto. Medido antes/depois matando o Chrome de fora: em voo PENDUROU 15s -> rejeita em 0 ms; novo depois da morte, idem (sap-accelerate/work/POC_webgui_canal/medicoes/item104-canal.md). Testes puros com WebSocket falso (webgui.test.mjs, 101 passam); receita: docs/receita-webgui.md § Chrome morto. Commit adt-tools 81c0378.

- [x] 105. O descarte pela via HTTP nao foi medido contra uma dynpro que CRIA ao entrar (o analogo do FLP Designer do item 38): o item 66 provou a primitiva com gestos reversiveis (/nSE38 <-> /nSE16), nao o comportamento de uma tela de criacao. Achar no s4h 758/250 uma dynpro que numere/insira ao entrar (candidatas: ME21N, transacao Z descartavel criada para isso) e medir o ciclo transacional(abrir->descartar) com assert em OUTRA LUW. Cuidado: e criar lixo de proposito num sistema alheio — o descarte tem que estar medido ANTES.
> MEDIDO no s4h 758/250 em 06/09/2026 contra uma dynpro que CRIA AO ENTRAR. Nenhuma transacao padrao do 758 insere ao entrar sem criar documento de negocio de verdade (ME21N numera no gravar), entao a tela foi feita descartavel: report YJBV_POC_105_CRIA ($TMP) cuja INITIALIZATION faz EXPORT TO DATABASE indx(zz) id YJBV105 + COMMIT WORK AND WAIT, com o descarte na propria tela (P_ACAO=DESCARTAR + Enter), e a transacao de report YJBV105. O DESCARTE FOI MEDIDO ANTES por classrun (0 -> 1 -> 0) e o deposito e a INDX padrao, sem tabela Z nova. Ciclo pela via HTTP, assert por runClass em sessao NOVA (outra LUW) a cada etapa: depois do abrir, antes do descarte = 1 linha (a tela criou ao entrar); nao confirmado -> descarte no finally (delta 69 ms) = 0 linhas; confirmado -> desarmado = 1 linha com carga nova; pendente do fechar (POST 68 ms, antes do /nex) = 0 linhas, desfeito ok:true, pendentes vazio. Ciclo completo 563 ms. CONTRA-PROVA (o roteiro do item 38 sem transacional): abrir + fechar deixou 1 linha no banco — fechar NAO e rollback na via HTTP. Sistema devolvido ao estado: INDX 0 linhas, transacao apagada (TSTC/TSTCT/TSTCP/TSTCC/TADIR exists:false), programa e as 2 classes com GET 404. Medicao: sap-accelerate/work/POC_webgui_cria_ao_entrar/medicoes/item105-cria-ao-entrar.md; receita-webgui.md secao nova 'E contra uma dynpro que CRIA AO ENTRAR (item 105)'. Aberto: descarte por BOTAO/menu e popup de confirmacao nao foram exercidos. Achado virou item 168.

- [x] 106. Sessao do ITS morta com pendencias no desfazer: hoje o fechar avisa e PRESERVA a pilha (item 66), mas ninguem a executa. A via HTTP tem o que o navegador nao tem — abrir OUTRA sessao custa ~1 s (GET+boot). Medir se um descarte armado na sessao A roda numa sessao B: o gesto depende do estado de tela (a sessao B comeca no menu) e do LOCK do objeto pela A. Se rodar, o fechar pode oferecer retomada; se nao, documentar por que o rotulo preservado e tudo que da para fazer.
> MEDIDO no s4h 758/250 em 06/09/2026 (POC_its_retomada/medicoes/item106-retomada-sessao-b.md). A retomada RODA, mas o que esta na pilha nao e retomavel. Tela nova para a medicao: cria ao entrar E bloqueia (ENQUEUE_E_TABLEE no INITIALIZATION), assert sempre em outra LUW lendo INDX + locks. (1) LOCK: com a sessao A viva, a B entra na tela e le "LOCK ALHEIO", e o descarte responde "Sem o lock" — banco inalterado; quando a A morre, o lock cai JUNTO (pelas duas mortes), e a B passa. (2) TELA: o gesto cru na B estoura ("campo P_ACAO nao esta na tela — tenho (nenhum)"), porque a B nasce no SAP Easy Access; depois de navegar (/nYJBV106, 117 ms) o descarte roda em 92 ms — ~843 ms com a sessao nova. As duas dependencias sao a mesma: enquanto a A segura o lock a B nao chega, e quando a A morre o lock cai — que e o instante da retomada. (3) ACHADO FORA DO ROTEIRO, e o mais grave: a morte POR TRAS (timeout, logoff ICF) deixa sessao.aberta=true, e o POST do descarte volta sem-sessao SEM estourar — o fechar relatava desfeito=[{ok:true}], pendentes=[] e o rascunho FICAVA no banco, sem nome. Corrigido: executar({guarda}) na pilha (para o laco e PRESERVA o que nao rodou) + sonda de vida no its.fechar (POST BOOT, 53-63 ms viva / 87-92 ms morta, so quando ha pendencia). Contra-prova no mesmo cenario: pendentes preservado, desfeito=[]. adt-tools af80d74, 948 testes. Laboratorio limpo (INDX 0 linhas, sem locks, YJBV106/report/classes apagados, GET 404). Desdobramentos: item 169 (retomada exige a pilha guardar RECEITA, nao closure) e item 170 (o mesmo falso ok dentro do transacional).

- [x] 108. Canal navegador/ITS: ao dirigir app UI5, medir e curar recurso vazio antes de confiar na pagina. Hoje curarRecursoVazio (icm.mjs, item 67) existe e funciona, mas ninguem o chama: quando o sap-ui-core.js volta vazio a pagina fica sem window.sap, o onload dispara e o canal culpa o timing. Decidir onde entra (abrirNavegador? um verificarUi5(s) que le window.sap e, se faltar, mede os recursos e cura) e nao carimbar URL — o carimbo entope o cache do ICM.
> A cura entra no inventario do fiori.mjs (verificarUi5), nao no abrirNavegador — la a aba e about:blank e a sessao serve o canal WebGUI, que nao usa UI5. Le window.sap; se faltar, mede por HTTP so o script que o Resource Timing denunciou com 0 byte, cura com curarRecursoVazio e recarrega com ignoreCache. Devolve a causa: recurso-vazio | nao-e-o-cache | sem-o-que-medir. Nao carimba URL. Medido no s4h 758/250 06/09/2026: pagina saudavel = 0 requisicao; sintoma reproduzido por intercepcao do CDP = window.sap false, mede 1 URL, e com o servidor inteiro NAO invalida nada; 25 entradas de sap-ui-core.js no cache do ICM antes e depois, nenhuma nova. adt-tools 6fae447 · sap-accelerate/work/POC_ui5_recurso_vazio/medicoes/item108-verificar-ui5.md

- [x] 109. clicar({ dentro }) casa por RÓTULO, e rótulo é traduzido: { dentro: 'Adicionar' } não casa num sistema em EN/DE. Medido no s4h 758/250 em 05/09/2026 (item 68) que o NOME do ícone não chega ao DOM — mas o caractere chega, em data-sap-ui-icon-content, e ele é estável entre idiomas. Medir se sap.ui.core.IconPool.getIconInfo('add').content dá a via neutra ({ icone: 'add' }) numa página UI5 real, e o que acontece quando a IconPool não está carregada.
> Medido no s4h 758/250 (UI5 1.114.0, 06/09/2026): IconPool.getIconInfo(add).content = U+E058 nos tres idiomas (pt/en/de) enquanto o aria-label do mesmo botao vira Adicionar/Add/Hinzufugen — { dentro: Adicionar } so aciona em PT, { icone: add } aciona nos tres. IconPool responde pelo namespace global desde o primeiro script do boot, mesmo com data-sap-ui-libs vazio (o require sincrono so responde depois de alguem carregar o modulo). Sem UI5 na pagina a recusa diz isso. Virou { icone } no webgui.mjs (jsAlvoEfetivo/apontar/recusa/clicar), 4 testes novos, receita-webgui § icone, medicao POC_ui5_clicar_descendente/medicoes/item109-icone.md

- [x] 110. A suíte do adt-client só passa se rodada de DENTRO de packages/adt-client: da raiz do monorepo, tipos.test.mjs quebra com ENOENT scandir adt-tools/tipos (caminho relativo ao cwd) e os examples/wdi5* falham por wdio-ui5-service ausente. Visto em 05/09/2026 rodando npx vitest run --dir packages/adt-client da raiz. Ancorar o caminho no import.meta.url e decidir se os examples entram no vitest.
> Suíte passa da raiz. (1) tipos.test.mjs ancorado em import.meta.url (DIR_TIPOS) — o readdirSync('tipos') resolvia contra o cwd e dava ENOENT adt-tools/tipos. (2) examples NÃO entram no vitest (são specs do wdio): vitest.config.mjs na raiz com projects: [packages/adt-*] faz cada pacote entrar com o SEU config, e a decisão que já estava no config do adt-client passa a valer da raiz. (3) efeito colateral coberto: config na raiz vaza para quem roda de dentro do pacote — adt-git/query/sandcastle/todo ganharam vitest.config.mjs de fronteira, senão pnpm -r test morria com "No projects were found". Medido 06/09/2026: npx vitest run da raiz e pnpm -r test → 87 arquivos, 1168 testes, verde nos dois. Filtro por pacote é --project <nome>, não --dir (documentado no README). Commit adt-tools ca8da85.

- [ ] 111. Confirmar o campo `ca` contra o SXD 816/100 de verdade — o que o item 69 nao pode medir sem a rede do cliente. O mecanismo esta provado em laboratorio (05/09/2026, POC_https_cert item69-ca-fetch.md): com a CA declarada, fetchToken e sondarWebgui passam num HTTPS de CA interna e uma raiz nao declarada continua barrada. Falta: (a) pedir a infra o .pem da CA que assina awskartsxd01.acclab.com:44300 (ou confirmar que ela ja esta no store do Windows desta maquina, caso em que "ca": "sistema" basta — a fonte resolve e le 278 CAs, mas nunca foi provada com uma CA real do store); (b) gravar em sistemas.json e ver o connect sxd fechar por HTTPS; (c) conferir o pino do Chrome (`certificado`) no MESMO host, que o item 41 tambem deixou por medir. Os dois campos sao validadores diferentes e o SXD quer os dois.
> bloqueado: sem VPN para o SXD

- [x] 112. O TETO do Import do ITSDoc: quanto arquivo o canal HTTP puro do WebGUI aguenta num POST, e o que acontece no limite. Medido em 06/09/2026 (item 72, s4h 758/250) que 256 KB de binario sobem num POST so, com sha256 identico na volta — mas o teto nao foi tocado. Duas pistas LIDAS no renderer (lsgui/js/webgui_min.js) e nao medidas: a funcao Import compara o tamanho com uma constante (aa, "maximum file size") e ABORTA antes de mandar, com SAPITS_FS_ENTRY_FILE_ERR; e o UpDownSendRequest trata HTTP 413 explicitamente (UpDownFSResponseError413), o que sugere que o servidor tambem tem o seu limite. Medir: de onde vem o aa (parametro de sistema? updown_*), onde o servidor comeca a recusar, e se a ENTRADA fatia como a SAIDA fatia (o HTML do item 45 veio em dois Export). Importa porque anexar arquivo de verdade (planilha, PDF, transporte) e o caso de uso, e 256 KB nao prova nada sobre 20 MB.
> Teto do Import MEDIDO ao byte no s4h 758/250 (06/09/2026). Quem corta NAO e o renderer: o maximum file size dele e Math.pow(2,31)-1, constante literal, sem parametro de sistema (nenhum updown*/its/* na RSPARAM inteira). Quem corta e o ICM, pelo icm/HTTP/max_request_size_KB (=102400), sobre o CORPO do POST: passa enquanto floor(corpo/1024) <= max_request_size_KB. Contra-prova pelo canal real da CG3Z: 104.858.437 B de arquivo ACEITO (16 s), 104.858.438 B RECUSADO. Multipart custa 186 B fixos. As duas direcoes NAO sao simetricas: a saida fatia em 5.120.000 B por Export (64 MB em 14 partes), a ENTRADA nao fatia (64 MB num Import so, sha256 identico, ~6,5 MB/s). ⚠️ a recusa nem sempre e 413 — no multipart o ICM FECHA a conexao (UND_ERR_SOCKET/ECONNRESET). Virou codigo: tetoDoImport()/MULTIPART_IMPORT, updown estourando em status != 200, atenderItsdoc acusando truncamento por voltasMax (padrao 12 -> 40, que truncava qualquer download > ~50 MB) — 3 testes novos, 960 verdes. Medicao: sap-accelerate/work/POC_webgui_import/medicoes/item112-teto.md; receita: packages/adt-client/docs/receita-webgui.md; commit adt-tools 95ac164.

- [x] 113. Os metodos do ITSDoc que NAO sao arquivo — o servidor mandando o "frontend" EXECUTAR, LISTAR ou APAGAR. Lidos no despacho invoke_itsdoc do renderer (item 72) e todos hoje respondidos com cancel pela lib: Execute (CommandLine!), ShowDocument, DirectoryListFiles, Directory, DirectoryCreate, DirectoryRemove, Delete, FileCopy, GetTempPath, FileBrowser, DpUrlCopy, DpGetStreamFromUrl. Duas perguntas: (1) quais transacoes os pedem de fato, e o que a dynpro faz quando o cliente cancela — hoje a lib cancela as cegas e o programa pode travar ou seguir errado; (2) o que significa um servidor SAP poder pedir Execute/Delete ao cliente HTTP — do lado do agente e superficie de ataque a conhecer antes de rodar num sistema de cliente. Comecar pelo GetTempPath e DirectoryListFiles, que sao os inofensivos e revelam o formato da resposta.
> GuiSapInfo e ENVELOPE (o verbo real esta em Method) e o default do renderer e exception, nao cancel — o cancel as cegas fazia o ABAP escrever FILE_DELETE RC=0 (dizendo que apagou) e DIRECTORY_DELETE RC=6357109 (lixo), e PULAR 4 pedidos. Os 13 verbos agora respondem o codigo do proprio webgui_min.js; GetTempPath (Z:	emp) e DirectoryListFiles provados ponta a ponta (subrc=0, count=3). A lib nao tem filesystem e nao vai ter. adt-tools 56a52fe; medicao sap-accelerate/work/POC_webgui_itsdoc/medicoes/item113-nao-arquivo.md; achados novos viraram os itens 173 e 174.

- [x] 114. O COMBOBOX (`ct="CB"`) do canal HTTP puro: escolher uma opção não está medido. Achado no item 73 (s4h 758/250, 06/09/2026): o popup Export As tem dois combos (`cmbGS_EXPORT-FORMAT`, `cmbGS_EXPORT-DESTINATION`) e ali cada um tinha UMA opção só, então nunca foi preciso escolher. O que a tela declara: o `lsdata` do input traz a chave corrente (índice 4, ex.: `xlsx-CUSTOM`) e o texto (índice 5), o `lsevents` publica `Select -> value`, e a lista inteira vem NO MESMO delta como um `<div ct="LIB_PS" id="<CAMPOPROGRAMA>">` com um `<div ct="LIB_I" data-itemkey="…" data-itemvalue2="…">` por opção. Falta medir: posta-se a CHAVE ou o TEXTO no `value/<SID>`? Vale porque combo é controle comum de dynpro (não só do Export As) e hoje `preencher` num combo é chute.
> MEDIDO: posta-se a CHAVE (data-itemkey), nunca o texto. s4h 758/250, 06/09/2026, combo cmbGS_EXPORT-FORMAT do Export As do btn[43] (que, ao contrario da via btn[45] do item 73, tem DUAS opcoes). Chave csv-LEAN-STANDARD -> delta, e o servidor traduz o texto de volta; o TEXTO que a tela mostra -> multipart -107 failed to set value: invalid value, O MESMO codigo que BANANA. A escolha chega ao ABAP: DefExt virou csv e o arquivo saiu CSV UTF-8 BOM (156 528 B) contra 88 061 B de XLSX. Lib (its.mjs, secao o COMBOBOX que a tela DECLARA): opcoesDaLista, combosDoDelta, comboDoSid, chaveDaOpcao, opcoes(sessao, alvo), e preencher num GuiComboBox traduz chave/texto/indice para a chave ({ cru: true } desliga). 4 testes novos. Achado colateral -> item 175: o default do combo PERSISTE entre sessoes (memoria do usuario), e o exportarPlanilha pode entregar CSV com nome .xlsx. Laboratorio restaurado para xlsx. Medicao: sap-accelerate/work/POC_webgui_combo/medicoes/item114-combo.md; receita: packages/adt-client/docs/receita-webgui.md sect. O COMBOBOX.

- [x] 115. O `btn[40]` da barra do ALV não foi seguido. Medido no item 73 (s4h 758/250, 06/09/2026) que a faixa de exportação da barra da lista publica TRÊS botões — `btn[40]`, `btn[43]` ("Planilha eletrônica...", CTRL_SHIFT_F7, medido) e `btn[45]` ("File local...", CTRL_SHIFT_F9, medido no item 45). O `btn[40]` só apareceu no despejo; não tem rótulo no `OKCODES` e ninguém o acionou. Vale porque é o último gesto de exportação da barra ainda sem medição — e porque um `btn[n]` sem nome é exatamente o tipo de endereço que se descobre uma vez e vale para sempre.
> A premissa caiu na medição: o btn[40] NÃO é exportação — é "Ordenar em ordem decrescente" (CTRL_SHIFT_F4, ícone s_b_srtd), o par do btn[28] (crescente, CTRL_4); rótulo ele sempre teve no title/lsdata[0], a "faixa de três" era artefato da regex /btn[4[0-9]]/ do script do item 73. No lugar, ficou medido ORDENAR o ALV por HTTP PURO (não estava — o item 77 mediu só pelo navegador): action/46 columns=;n; + action/3 num POST só, sem as quatro ações que o renderer manda a reboque. Marca inválida (nenhuma, coluna 0, coluna fora do grid) não é recusada, é IGNORADA, e o botão abre o diálogo Ordenação — que NESTE canal é wnd[1] de verdade, ao contrário do navegador. columns é CONJUNTO: a precedência é a das colunas na tela (;2;5; == ;5;2;), o empate é estável, e quem reordena é o servidor (o XLSX saiu na ordem da tela). A tela não declara a ordenação. Lib: ordenarGrid/botaoDeOrdenacao/batchOrdenar/indiceDaColuna no its.mjs, 83/83 testes; medição em sap-accelerate/work/POC_webgui_btn40/medicoes/item115-btn40.md.

- [ ] 116. O FILTRO e o drill-down pelo lerGridInteiro — a lacuna que o item 74 nao fechou. Medido em 05/09/2026 (item 74) que com o ALV ORDENADO na tela (btn[28]) o lerGridInteiro devolve a ordem nova e a sessao HTTP paralela devolve a original — mas so a ORDENACAO foi medida. A tela oferece btn[29] "Definir filtro (Ctrl+F5)": falta provar que com filtro aplicado o lerGridInteiro devolve so as linhas filtradas, o que o totalRows passa a declarar, e o mesmo para drill-down e linha selecionada (btn[2]). E o que sustenta a afirmacao de que a sessao da tela ve o que a de fora nao ve.

- [ ] 117. O fetch na sessao da PAGINA para os outros action/ do its.mjs — dar ao webgui.mjs o repertorio inteiro sem segunda sessao. Medido em 05/09/2026 (item 74) que um POST em <action>batch/json de dentro da pagina, com o moin dela, fala com a MESMA sessao da tela e e inocuo (o framework continua pedindo fragmento sozinho depois, e o round-trip btn[3] traz tela viva). O truque nao tem nada de especifico do action/710. Nao medido: se vale para o ITSDoc (exportarLista/atenderItsdoc, que postam FORA do batch, numa URL propria), para o comandar e para o action/622 da escrita — e o que acontece quando a resposta e um delta COMPLETO que ninguem aplica no DOM (o do 710 e parcial, por isso nao ha o que aplicar).

- [ ] 118. Duplo clique como GESTO da lib webgui — o `clicar` so faz clique simples, e o drill-down do ALV precisa de dois pares press/release com clickCount 1 e 2 (medido em 05/09/2026, item 75: e assim que a linha 1234 abriu o popup "Exibir parametro de perfil"; um clique simples nao gera requisicao nenhuma, a selecao e puro cliente). Hoje isso esta a mao nos scripts da POC_webgui_grid. Falta decidir se vira `duploClique(sessao, alvo)` ou uma opcao `{ cliques: 2 }` do `clicar`, e medir em que outros controles o duplo clique e o gesto (arvore, campo com match code).

- [ ] 119. Grid do WebGUI: DESMARCAR a selecao. Medido no item 76 (05/09/2026, POC_webgui_grid_sel/medicoes/raw/g-distante.json) que o cabecalho grid#<cid>#0,0 — o <th subct="HC"> cujo lsdata diz {"2":"SELECTIONCOLUMN","3":"SELECTION_TOGGLE"} — MARCA TUDO com a selecao limpa (3 de 3 linhas, ZERO requisicao); mas com as 3 ja marcadas o mesmo clique NAO desmarcou (fase F, caso 6). Apesar do nome TOGGLE, o outro sentido nao existe medido, e nao ha gesto conhecido para limpar a selecao — nem ctrl+clique na unica linha marcada foi tentado. Medir: o que desmarca UMA linha, o que limpa TUDO, e se o cabecalho tem um segundo alvo clicavel dentro (o <div> interno). O laboratorio ZJBV_ALV47_EDIT ja tem o fcode FC02 que despeja get_selected_rows, entao a prova ABAP e de graca. Vira selecionarTudo/limparSelecao na lib.

- [ ] 120. Grid do WebGUI: selecionar COLUNA e BLOCO de celulas. Medido no item 76 (05/09/2026) que a selecao de LINHA sai como action/47 e que a de celulas e action/48 (cells=) e a de bloco action/50 (top_left/bottom_right/reference row+column) — as duas ja saem sozinhas ao clicar numa celula, e nenhuma foi exercitada de proposito. O lsevents do grid publica ClientColumnSelect e BlockSelect; o lsdata entrega selectedColumns (mesmo formato ";n;n;" com faixa compactada), selectedCells e selectedBlock. No item 76 o get_selected_columns do fcode FC02 respondeu 0 em TODOS os casos, porque ninguem selecionou coluna. Medir: clicar o cabecalho de UMA coluna (grid#<cid>#0,<c>), N colunas com ctrl, arrastar um BLOCO de celulas, e provar pelo FC02 do ZJBV_ALV47_EDIT que get_selected_columns/get_selected_cells enxergam. Vira selecionarColunas/selecionarBloco na lib, ao lado do selecionarLinhas.

- [ ] 121. O DIALOGO "Ordenacao" do ALV no WebGUI — a ordenacao por VARIAS colunas, que o botao da barra nao alcanca. Achado do item 77 (s4h 758/250, 05/09/2026): SORT_ASC/SORT_DSC ordenam por UMA coluna e SUBSTITUEM o criterio anterior (medido: com a coluna 2 em desc, ordenar a 3 zerou a ordem da 2). Varios criterios de uma vez so pelo dialogo SAPLSALV_CUL_..., que aparece quando o botao e acionado SEM coluna marcada — e la ele so foi visto como MODO DE FALHA (o ordenarGrid estoura). ⚠ Ele NAO e wnd[1]: o lerTela responde janela.principal: true e nao o ve; quem o enxerga e o seletor [ct^="PW"] visivel (JS_MODAL_DO_ALV). Medir: o vocabulario de dentro dele (as linhas de criterio, ASC/DESC, o botao OK e o de fechar), se os campos tem SID como o dialogo de filtro (%%DYN001-LOW tem), e provar uma ordenacao por 2 colunas ponta a ponta. Laboratorio: ZJBV_ALV47_EDIT.

- [ ] 122. O MENU DE CONTEXTO da celula do ALV (CellContextMenu) — a outra porta para os fcodes do grid, nunca acionada. Publicado no lsevents do grid desde o item 25 e citado no 47 e no 77, e ate hoje nenhum gesto o abriu. Interessa porque e por ele que o usuario final chega em ordenar/filtrar/somar/totalizar sem a barra — e ha ALV com barra escondida ou sem os botoes que o item 77 usa (o jsBotaoDaBarra estoura listando os fcodes que a barra TEM; quando a lista vem curta, o menu de contexto e a saida). Medir: qual gesto o abre (botao direito no DOM? action/NN?), se o menu vem no DOM ou por round-trip, o vocabulario dos itens (dao para reusar interpretarItemDeMenu/navegarMenu do item 26?) e provar um acionamento que a barra nao oferece. Laboratorio: ZJBV_ALV47_EDIT e a lista do RSPARAM.

- [ ] 123. Grid do WebGUI: DUPLICAR linha (LOCAL&COPY_ROW), o quarto botao de linha da barra do ALV editavel. Medido no item 78 (fase A, s4h 758/250, 06/09/2026) que ele existe e esta visivel — C102_toolbar_btn13, title "Duplicar a linha", SID <sid>/tbar/btn&LOCAL&COPY_ROW — mas o gesto nao foi exercitado. Vale porque duplicar entrega a linha nova JA PREENCHIDA: hoje inserirLinha({ valores }) custa um escreverCelula por coluna, e cada um e um clique + digitacao + blur. Medir: de onde ele copia (a selecionada? a corrente?), onde a copia entra (antes/depois), e se a chave vem junto (no ZJBV_ALV47 isso seria chave duplicada — o MODIFY do FC01 aceita, mas o dado fica errado). Laboratorio: ZJBV_ALV47_EDIT.

- [ ] 124. WebGUI: a caixa de selecao que NAO PINTA depois de um round-trip — falha intermitente do selecionarLinhas. Visto uma vez no item 78 (06/09/2026): o selecionarLinhas de dentro do inserirLinha({ antesDe: 1 }), chamado logo apos o round-trip de um LOCAL&APPEND, estourou com "pedi [1] e a tela ficou com [] pintada(s)" — o clique saiu e a caixa nao pintou. A hipotese de timing FOI TESTADA E CAIU (fase H, raw/h-assentar.json): 4 rodadas imediatas contra 4 com esperarQuieto(700ms), 8/8 selecionaram, e o retangulo da caixa grid#C102#1,0 era identico (x=1 y=215 w=25 h=25) ja no instante imediato. Causa aberta. Proximo passo: instrumentar o proprio selecionarLinhas (guardar o rect apontado, o Input.dispatchMouseEvent e o lsdata antes/depois) e rodar em laco ate reproduzir — sem reproducao nao ha o que consertar. Impacto: quem chama estoura com a mensagem certa, mas um script longo morre no meio.

- [ ] 125. ACHADO do item 79 (06/09/2026): escrita em LOTE pela via HTTP (its.mjs), sem navegador. O 79 mediu que colar um bloco no ALV editavel posta, num batch so, action/50 (area) + action/53 (celula corrente) + um action/770 c0=..&c1=..&curColIdx=<1-based>&curRowIdx=<0-based relativo a ancora> por linha colada — e o its.mjs ja monta batch e ja endereca por SID. Se o action/770 pegar pela via HTTP, a lib escreve N celulas de uma vez SEM Chrome (hoje o its.mjs LE o grid e nao escreve nele; escreverCelula/colarBloco so existem no navegador). Medir: se o action/770 cru e aceito (o item 50 mostra que compor nao e funcionar — action/1 e action/74 sao declarados e inexistentes), se precisa do action/53 antes para ancorar ou se o curColIdx/curRowIdx bastam, se a linha que estoura embaixo tambem anexa (action/771 pasteOption=Append), e se o valor chega ao ABAP. Contra-prova pareada em outra LUW, como no 79. Laboratorio pronto: ZJBV_ALV47_EDIT no s4h 758/250 (SA38, okcode STRT, FC01 grava).

- [ ] 126. webgui: propagar o sinal de round-trip (esperarTroca) aos OUTROS gestos que hoje esperam so pelo carimbo — acionar/clicar({esperarResposta}), ordenarGrid, filtrarGrid, inserirLinha, apagarLinhas. Medido no item 80 (06/09/2026, POC_webgui_grid_edit/medicoes/item80-carimbo-repaint.md) que o JS_CARIMBO le so o CABECALHO (300 chars do inicio do innerText) e fica IGUAL em 3 de 5 round-trips completos num ALV — cada um desses gestos paga o teto inteiro quando a tela volta igual, e ordenar/filtrar um grid pequeno e exatamente esse caso. ⚠ Cada um precisa da sua contra-prova antes de migrar: abrirMenu muda a tela SEM round-trip nenhum (ali o carimbo e o sinal certo), e navegarMenu depende dele. A peca ja existe e esta testada: esperarTroca/roundTrips/ehTelemetria em webgui.mjs.

- [ ] 127. PUSHBUTTON de dynpro: acionar pelo id e GUARDA do botao desabilitado. O item 81 fez o lerTela devolver o pushbutton de tela (wnd[n]/usr/btn..., okcode: null, habilitado) — mas acionar() so sabe okcode, e nada impede o script de clicar num botao com habilitado: false. Medido no item 81 que o clique no cinza gera ZERO POST: quem espera resposta espera para sempre. Fazer: (1) medir o gesto pelo id em ZJBV_BTN81 (clicar(s, id) no BT_ON — round-trip e mensagem ITEM81 CLICOU BT_ON), (2) decidir se acionar() aceita { id } ou se e uma funcao nova, (3) guarda que LANCA dizendo rotulo e SID, o par do que navegarMenu ja faz no menu (item 48). Laboratorio pronto: ZJBV_BTN81 ($TMP, s4h 250), scripts em sap-accelerate/work/POC_webgui_botao/scripts/lab.mjs. Medicao: POC_webgui_botao/medicoes/item81-botao-cinza.md

- [ ] 128. Os 39 POMNI de wnd[0]/usr/mnu/menu[...] que vem nos MESMOS <xmp> do menu da barra (achado do item 82, s4h 758/250, 06/09/2026: 185 POMNI no total, 146 da barra, 39 nao). Que menu e esse? O do botao direito da area de trabalho? Ele tem gesto proprio? O arvoreDeMenu os descarta hoje pelo daBarraDeMenu.

- [ ] 129. Da para INFLAR um popup direto da marcacao do <xmp> e matar a cascata tambem no ACIONAMENTO? Achado do item 82: a marcacao de cada popup esta no DOM como texto inerte; o renderer a infla sob demanda, um popup por clique — e por isso a folha so vira elemento depois de o pai ser clicado. Medir: um innerHTML no container cria os elementos e o renderer os reconhece (o clique e por delegacao, o que sugere que sim)? o estado do popup fica coerente? Se sim, navegarMenu vira UMA leitura + um clique na folha, e a cascata (2,3 s) cai para um clique.

- [ ] 130. its.test.mjs:511 falha no HEAD (visto no item 82, 06/09/2026, branch revisao-readmes): tela.botoes de um delta de POPUP traz [null, null, "btn[3]"] onde o teste espera ["btn[3]"] — dois botoes sem okcode entram em tela.botoes. Sem relacao com o menu. Descobrir se o teste envelheceu ou se o montarTela/teclaDoBotao regrediu.

- [ ] 131. Guarda que FALTA no its.navegarMenu: com modal aberta o action/4 e ENGOLIDO em silencio. Medido no item 83 (06/09/2026, s4h 758/250, passo 3 de item83-com-modal-aberta.mjs): com wnd[1] na frente os 146 itens do menu CONTINUAM no delta e navegarMenu resolve o caminho sem reclamar, mas o POST volta multipart/pegou:false/mudou:false — nada acontece e ninguem avisa. A guarda do item DESABILITADO (item 48) ja lanca; esta nao existe. Fazer: lancar (ou avisar) quando janelaAtiva !== wnd[0] antes de postar o action/4, dizendo qual modal esta na frente e que ela se responde por popup.botoes. Mesma familia do aviso do item 59.

- [ ] 132. webgui.navegarMenu (via NAVEGADOR) nao devolve o popup — o mesmo buraco que o item 83 fechou na via HTTP. Ele volta { caminho, passos, folha, mudou } e o mudou vem do esperarMudanca sobre o carimbo do DOM: uma folha que abre modal (Sistema > Status..., Ajuda > Configuracoes...) fica indistinguivel de uma que troca de tela. Na via HTTP a separacao saiu de popupDaSessao(s) (SIDs ja extraidos decidem se paga o parse). Aqui o equivalente e o carimbo/lerTela do DOM — medir qual o sinal barato de modal aberta no DOM e devolver popup no resultado, com a mesma leitura (!mudou ? nada : popup ? popup : tela).

- [ ] 133. Teste QUEBRADO no its.test.mjs (anterior ao item 83, ja falhava no HEAD 9f46fe6): "com POPUP aberto o delta traz a wnd[1]..." — expect(tela.botoes.map(b => b.okcode)).toEqual([btn[3]]) recebe [null, null, btn[3]]. Ou seja, montarTela passou a incluir em tela.botoes dois controles com okcode null que antes nao entravam (os Sim/Nao do SPOP?), contrariando o proprio comentario do teste ("Sim/Nao NAO sao btn[n]: nao entram em tela.botoes"). Decidir qual lado esta certo: se os botoes do popup devem sair de tela.botoes (e o teste vale) ou se a mudanca foi deliberada (e o teste envelheceu). Achado ao rodar a suite no item 83 — 894 passam, so este falha.
> medido: ainda quebrado no HEAD 5775e56 (06/09/2026, conferido de raspao no item 84 — a suite inteira: 1 failed | 896 passed). Os dois intrusos sao os botoes do popup (SPOP-OPTION1/2). Suspeitos a bissetar: cb59082 (botao desabilitado / lsdata[5]) e 83b4595 (navegarMenu devolve o popup).

- [ ] 134. A suite do adt-client esta VERMELHA no HEAD — 1 teste falha em its.test.mjs:516 ("o popup SPOP: os botoes Sim/Nao NAO sao btn[n]"). tela.botoes.map(b => b.okcode) esperava ["btn[3]"] e devolve [null, null, "btn[3]"]: dois botoes SEM okcode passaram a entrar na lista, que e exatamente o que o comentario do teste diz que nao deveria acontecer. Medido em 06/09/2026 (item 85): falha identica no HEAD 4593c4b (stash das mudancas do 85) e com elas — nao e regressao do 85. Decidir qual lado esta certo: se botao sem okcode deve entrar em tela.botoes (e o teste envelheceu), ou se montarTela/interpretarControle regrediu. Enquanto isso a suite nao serve de porteiro.

- [ ] 135. Os dois comandos do container da arvore que ninguem exercitou — action/901 (RequestData) e action/62 (HorizontalScroll), lidos do lsevents do STCS do SMEN em 06/09/2026 (item 85, raw g0-lsevents-container.json). O RequestData cheira a PAGINACAO: a arvore do SMEN cabe inteira no delta, mas uma arvore grande (hierarquia de custo, estrutura de produto) pode vir por pedaco, e ai ler os TV da tela nao basta. Medir com uma arvore grande o suficiente para nao caber de uma vez.

- [ ] 136. A RAIZ da arvore nao fecha por gesto na via do navegador (medido no item 86: o duplo clique em Root e em Favo posta o action/2 OnNodeDoubleClick, o servidor responde e a arvore fica identica; um no de nivel 1 fecha em 253 ms). O candidato para cobrir o buraco e o TECLADO: focar o no e mandar seta-esquerda / `-` (o SAP Tree colapsa assim no SAP GUI). Medir se algum gesto de teclado fecha a raiz pelo CDP; se fechar, virar o caminho do colapsarNo do webgui.mjs para raiz. Se nao, a limitacao fica documentada e quem precisa encolher a arvore usa a via HTTP (action/9).

- [ ] 137. Teste VERMELHO no HEAD (nao e regressao do item 86 — conferido com git stash): its.test.mjs "com POPUP aberto o delta traz a wnd[1] e ESVAZIA a wnd[0]/usr" falha em tela.botoes.map(b => b.okcode) — esperado [btn[3]], veio [null, null, btn[3]]. Dois botoes a mais estao entrando em tela.botoes sem okcode (os Sim/Nao do SPOP?). Achar o commit que mudou montarTela/anotarBotoes e decidir: o teste envelheceu ou a montagem regrediu.

- [ ] 138. O packages/adt-git tem uma COPIA ATRASADA de sap-connection.mjs + session.mjs (170 linhas contra ~240 do adt-client): sem sessaoNasceuMorta, sem nasceuMorta, sem erroSessaoMorta — o conectar dele ainda grava .sessao.json so exigindo token (o bug que o item 87 fechou no adt-client), e o call dele nao recusa a sessao morta (bug do item 52). Achado ao fechar o 87. Decidir: adt-git importa a lib do adt-client (workspace) ou sincroniza a copia — e medir o que a copia tem a MENOS antes de trocar.

- [ ] 139. REGRESSAO NA SUITE: its.test.mjs:516 falha desde o commit fe4f2c8 (item 86, a arvore do Easy Access pelo navegador) — 'com POPUP aberto o delta traz a wnd[1]' esperava botoes [btn[3]] e recebe [null, null, btn[3]]: o parser passou a emitir botoes SEM okcode (dois nulls) no delta do popup. Decidir qual lado esta certo — se botao sem okcode nao e botao (filtrar na leitura da tela) ou se o teste e que esta desatualizado — e medir contra a resposta ITS real antes de mexer. Achado ao rodar a suite completa fechando o item 87.

- [ ] 140. O logoff da sonda do WebGUI NAO FECHA no caminho padrao da lib — achado medido do item 88. urlWebgui so poe sap-language quando o cfg traz `idioma`, e a lib inteira usa `lang` (sistemas.json, .env, criarConexao); sem idioma na URL a sessao nasce sem sap-language, o logoff dumpa (TEXTENV_UNICODE_LANGU_INVALID, item 64) e responde 500 — a sessao FICA. Medido no s4h 06/09/2026 com TH_USER_LIST: sondarWebgui(cfg.lang) 9->10 sessoes (+1), sondarWebgui(cfg.idioma) 10->10 (+0); na varredura causas.mjs 8 dos 9 logoffs deram 500. A regra "quem sonda fecha" (item 28) esta escrita e nao esta acontecendo, e cada sonda soma uma sessao ao teto de ~150. Decidir se urlWebgui passa a ler lang ?? idioma (e varrer quem mais le cfg.idioma).

- [ ] 141. Teste VERMELHO pre-existente em its.test.mjs:516 (`popup: o que a tela mostra e o que da para acionar`): tela.botoes traz [null, null, "btn[3]"] onde o teste espera ["btn[3]"] — os botoes Sim/Nao do popup estao entrando em tela.botoes com okcode null. Ja falhava em HEAD antes do item 88 (verificado por git stash em 06/09/2026). Decidir se o certo e o teste ou o codigo: o comentario do teste diz que Sim/Nao NAO sao btn[n] e nao devem entrar.

- [ ] 142. ABERTO do item 88: o que o no do WebGUI responde SEM cookie com o servidor no TETO de sessoes. E a premissa que o item 52 escreveu sem medir ("a sonda cai em inesperado exatamente no estado doente") e que o item 88 nao pode confirmar nem negar. Do teto so esta medido o GET com o cookie envenenado: 400 (item 28 M5). Custo: reproduzir o estado deixa o s4h inutilizavel por ~30 min (item 28 M8 — a cura levou ~26 min com as sessoes fechadas no 1o minuto) e o laboratorio e COMPARTILHADO.
> bloqueado: preciso da sua autorizacao para envenenar o s4h 250 por ~30 min (o laboratorio e compartilhado) — sem isso a premissa do item 52 fica sem medicao. Autoriza, e em que janela?

- [ ] 143. A suite esta VERMELHA e nao e do item que a encontrou: its.test.mjs > "com POPUP aberto o delta traz a wnd[1] e ESVAZIA a wnd[0]/usr" falha com expected [null, null, "btn[3]"] to deeply equal ["btn[3]"] — tela.botoes passou a devolver DOIS okcodes null antes do btn[3]. Confirmado PRE-EXISTENTE em 06/09/2026 (git stash das mudancas do item 89 e a falha continua; 917 de 918 passam). Ou o parser de botoes do delta parou de filtrar botao sem okcode (Sim/Nao do popup, que o proprio comentario do teste diz que NAO entram em tela.botoes), ou a mudanca foi deliberada e o teste ficou para tras — decidir qual, medindo contra o delta real do popup, e nao so ajustar o expected ate ficar verde.

- [ ] 144. O nó /sap/public/bc/icf/logoff responde HTTP 500 no S4H 250 — medido 06/09/2026 (item 90) em três variantes: com cookie do SOAP, com cookie do ADT e SEM cookie nenhum, e também sem ?sap-client. Corpo: página "Application Server Error" gerada pelo ICM. Em 04/09 (item 53, M-b) o MESMO logoff respondia 200 e derrubava as sessões usadas (10 de 10). Algo mudou entre 04/09 e 06/09. Achar a causa (SICF do nó, ST22, SMICM trace) e decidir se encerrarSessaoSoap/encerrarSessao continuam apostando nele. Medição: sap-accelerate/work/POC_sessoes_icf/medicoes/item90-o-remedio-reuso-de-sessao.md

- [ ] 145. Quais OUTROS caminhos da lib ainda fazem requisição autenticada SEM cookie? O item 90 curou o canal SOAP (rfc-soap.mjs), mas o custo de 1 sessão de segurança por requisição sem cookie vale para TODO caminho HTTP autenticado. Auditar webgui.mjs, its.mjs, gui.mjs, icm.mjs, fiori.mjs e sap-connection.mjs (sessaoNova em laço) contra a sonda do item 53, e aplicar o mesmo remédio onde couber. Instrumento pronto: readTable(SECURITY_CONTEXT, reusarSessao:false) + o FM YJBV_POC_FM_SECCURA.

- [ ] 146. Teste quebrado em packages/adt-client/its.test.mjs:516 — "tela.botoes.map(b=>b.okcode)" devolve [null, "btn[3]"] e o teste espera ["btn[3]"]. Um botão sem okcode passou a entrar em tela.botoes. Falha PRÉ-EXISTENTE (confirmada em 06/09/2026 com git stash das mudanças do item 90: mesma falha no HEAD). 1 de 926 testes do pacote. Decidir se o certo é o parser filtrar okcode null ou o teste passar a aceitá-lo.

- [ ] 147. Curar o -103 da arvore depois da ida-e-volta ao menu: acionarNo/expandirNo/colapsarNo devem reenviar apos um ESTADO. Medido no item 91 (s4h 758/250, 06/09/2026, POC_webgui_sid_smen/medicoes/item91-sid-arvore-smen.md): depois de sair para outra transacao e voltar ao SMEN, o PRIMEIRO POST de acao na arvore (action/41, /8 ou /2) e sempre recusado com multipart -103 failed to fire action: not available — o SID esta correto e o delta inteiro, o que falta e o registro do controle no ITS. Um postar(s,[ESTADO]) sozinho (~78 ms) cura, e o POST recusado NAO aquece (tres seguidos deram -103). Implementar: ao levar -103 num action de arvore, postar ESTADO e reenviar UMA vez; medir o custo e provar com o mesmo ciclo /nSE38 + /nSMEN.

- [ ] 148. Anomalia -102 control not found na arvore: por que a ORDEM muda o que o ESTADO registra? Medido no item 91 (s4h 758/250, 06/09/2026): volta ao SMEN -> action/41 (-103) -> postar([ESTADO]) -> action/41 deu -102 control not found (sonda b-ressuscitar, braco R1), enquanto volta -> postar([ESTADO]) -> action/41 deu delta (sonda c-quantos-posts, braco C2). Um -103 antes do ESTADO parece mudar o que ele registra, e -102 e um erro DIFERENTE (controle nao existe) de -103 (existe mas nao aceita acao). Importa para o retry do item anterior: se o retry acontecer depois da recusa, pode cair no caso ruim. Reproduzir os dois caminhos lado a lado e guardar os dois corpos.

- [ ] 149. its.test.mjs falha SÓ na suíte inteira — `its: com POPUP aberto o delta traz a wnd[1] e ESVAZIA a wnd/usr`. Isolado (`npx vitest run its.test.mjs`) passa; `npx vitest run` do pacote falha em tela.botoes (esperado [btn[3]]). Reproduzido nos dois lados do stash em 06/09/2026, portanto é PRÉ-EXISTENTE, não regressão do item 93. Cheiro de estado compartilhado entre arquivos de teste (módulo com estado no nível do módulo, ou fixture mutada). Achar quem contamina — a suíte verde é o que dá direito de dizer que uma medição fechou.

- [ ] 150. its.test.mjs tem 1 teste VERMELHO pre-existente — nao e regressao do item 94 (medido em 06/09/2026: falha igual com a mudanca stashed). O teste `its: … popup …` espera `tela.botoes.map(b => b.okcode)` = [`btn[3]`] e recebe outra coisa, na linha ~516 do its.test.mjs (fixture DELTA_POPUP). Descobrir se o parser mudou de comportamento ou se a expectativa e que envelheceu, e fechar o vermelho — enquanto ele estiver la, `npx vitest run` nunca fica limpo e a proxima regressao de verdade passa despercebida.

- [ ] 151. its.test.mjs tem 1 teste QUEBRADO (1 de 64) e nao e regressao desta sessao: medido em 06/09/2026 que ele falha igual com o its.mjs do HEAD (git stash). O assert e tela.botoes.map(b => b.okcode) esperando [btn[3]] num DELTA_POPUP — o popup passou a trazer botao a mais/a menos. Achar o que mudou (item 94 mexeu em popup/menu?) e decidir: o assert esta velho ou o parser regrediu.

- [ ] 152. Abrir por URL a transacao em que a sessao JA esta nao volta para a 1a tela. Medido em 06/09/2026 (POC_webgui_tela_limpa, fase B): na SE16, depois de um Enter que avancou para a tela de selecao, ir() para a URL ~transaction=SE16 devolveu uma tela SEM o campo DATABROWSE-TABLENAME (o assert veio null) — nas outras cinco transacoes, que ficaram na 1a tela, a reabertura voltou certa. Medir: a URL reinicia a transacao ou reata a sessao onde ela estava? Se reata, todo roteiro que usa abrirTransacao como reset esta errado, e falta um gesto de reset (/n antes? qual?).

- [ ] 153. ACHADO do item 96 (06/09/2026): teste VERMELHO no master do adt-client, anterior a este item (medido com git stash: falha identica com e sem as mudancas). its.test.mjs > "its: com POPUP aberto o delta traz a wnd[1] e ESVAZIA a wnd[0]/usr". ⚠️ E o pior tipo: passa quando o arquivo roda SOZINHO (npx vitest run its.test.mjs = 64/64 verde) e falha na SUITE INTEIRA - ou seja, ha estado compartilhado entre arquivos de teste, ou ordem/paralelismo. Investigar: rodar com --sequence.shuffle e --no-file-parallelism para isolar o vizinho que contamina, e ver se sidDoAlvo/sidsDaResposta guardam estado de modulo.

- [ ] 154. IDEIA (achado do item 96, 06/09/2026): o where-used SO responde escrita quando se pergunta pela TABELA ou pela CLASSE dona do campo - pelo DATA ELEMENT as linhas de escrita nem aparecem (vem so as declaracoes). Se a pergunta do usuario e "quem grava neste CAMPO", a lib poderia resolver o pulo sozinha: dado um DE, achar as tabelas/estruturas que o usam (isso o where-used do DE JA da, sao as declaracoes TABL/DSF) e disparar o where-used de cada uma com acesso: escrita, juntando os resultados. Medir o custo (N tabelas = N chamadas de usageReferences + snippets) e se o campo casa como gradeComponent em todas.

- [ ] 155. IDEIA (achado do item 96, 06/09/2026): escrita no BANCO nao e accessWrite - INSERT saiu accessRead e UPDATE ... SET saiu accessUnknown (medido). Entao "quem grava esta TABELA" nao sai do access, mas talvez saia da SINTAXE: com os snippets do where-used da tabela ja em maos (arquivo, linha e o trecho), da para classificar a linha por padrao Open SQL (INSERT/UPDATE/MODIFY/DELETE ... <tabela>) e responder a pergunta sem canal de fonte/debug. Medir contra um objeto real com muitas gravacoes, e cuidar do falso negativo do comando que quebra em varias linhas.

- [ ] 156. ACHADO do item 97 (06/09/2026): a acao CODE_LINES do /whereused NAO tem formato medido. POST /repository/informationsystem/whereused?RIS_REQUEST_TYPE=CODE_LINES com o corpo <ris_request> responde 400 ExceptionResourceWrongData nos dois alvos testados (DTEL e TABL). No fonte de CL_RIS_ADT_RES_WHERE_USED o metodo get_code_lines usa OUTRO content handler (cl_ris_adt_rest_content_handlr=>c_where_used_result, tipo RIS_S_ADT_WHERE_USED_RESULT) e nao o c_request — logo o corpo e um <ris_where_used_result>, nao um <ris_request>. Ler a ST RIS_ST_ADT_WHERE_USED_RESULT no servidor (ja baixada em POC_whereused_endpoint/medicoes/raw/st-ris_st_adt_where_used_result.xml) e alimentar com um resultado devolvido pelo WHERE_USED_LAZY. Vale porque CODE_LINES e o analogo do usageSnippets do RIS classico: se der linha/coluna a partir de um resultado ja filtrado por tipo, fecha o where-used barato do item 97 (54ms por tipo) com localizacao.

- [ ] 157. ACHADO do item 97 (06/09/2026): o <scope_object> do WHERE_USED_LAZY nao teve efeito medido. No fonte, where_used_lazy e o UNICO dos tres ramos que le scope_object (scope_type + scope_objects) e full_name — o WHERE_USED simples ignora os dois. Mas a unica tentativa (scope DEVC/DP objeto J1BA, alvo DTEL J_1BNFNUM_UTILITIES, object_type TABL/DTF) devolveu EXATAMENTE os mesmos 1797 bytes que sem escopo: ou o filtro nao e por pacote, ou falta preencher outro campo (scope_encl_object_name?), ou o LAZY exige que o scope seja um dos tipos do WUL_TYPES_COMPLETE. Medir com escopo = um objeto concreto (uma classe, um grupo de funcao) e com contraprova nos dois sentidos. Importa porque escopo por PACOTE seria a pergunta cara que nenhum dos tres canais responde hoje: quem no pacote X usa Y.

- [ ] 158. ACHADO do item 97 (06/09/2026): o `call` do sap-connection nao tem timeout, e o fetch do Node morre aos 300s com UND_ERR_HEADERS_TIMEOUT (headersTimeout default do undici) — medido chamando /whereused com object_type=TABL/DTF e payload=DTEL/DE MATNR, que o SAP ainda estava processando. O sintoma e uma EXCECAO do cliente, nao um status: quem chamou nao consegue distinguir servidor lento de servidor morto, e nao ha como pedir mais tempo. Ja mordeu antes por outro nome no item 57 (`fetch failed` depois de 308s com lote de 2000 ids no usageSnippets) — duas vezes, mesma causa, e ninguem sabia que era o teto do undici. Decidir se `call` ganha um `timeout` opcional (AbortSignal.timeout, ou um dispatcher com headersTimeout proprio) e uma mensagem que diga qual teto estourou.

- [ ] 159. IDEIA do item 97 (06/09/2026): implementar o canal /whereused (RIS classico) na lib, hoje so DOCUMENTADO no fim do search.mjs. O que a medicao ja fixou: POST whereused?RIS_REQUEST_TYPE=WHERE_USED, corpo <ris_request> application/xml SEM namespace (ST RIS_ST_ADT_REQUEST), <object_type> = tipo do RESULTADO e <payload> = objeto PROCURADO (trocar responde 200 com ZERO byte, sem erro), codigos vindos do /fullnamemapping?RIS_REQUEST_TYPE=MAP_URI_TO_RIS_REQUEST&uri=, escopos validos do /metadata?RIS_REQUEST_TYPE=WUL_TYPES_COMPLETE. O ganho medido e a pergunta ESTREITA: que classes usam este DE custa 54ms contra 913ms do usageReferences (17x), com enclosing_object+object+uri ja resolvidos e sem colapsado. So vale se a API for por TIPO (ex.: quemUsa(session, uri, { tipos: [CLAS/OC] })) — varrer todos os tipos e ~5x mais caro que o canal atual e nao deve ser o default. Decidir tambem o alarme para o 200 vazio, que hoje mente calado.

- [ ] 160. ACHADO do item 97 (06/09/2026): a suite do adt-client esta VERMELHA no branch revisao-readmes, e nao e de agora. its.test.mjs > 'com POPUP aberto o delta traz a wnd[1] e ESVAZIA a wnd[0]/usr' falha na linha 516: espera tela.botoes.map(b => b.okcode) igual a [btn[3]]. Suite: 1 failed | 932 passed (933). CONTRAPROVA feita: com git stash das mudancas da sessao a falha e IDENTICA, entao nao veio do item 97 — ja estava quebrada antes. Nao investiguei a causa (o item 97 era outro assunto) e nao vou palpitar se o teste envelheceu ou se o parser regrediu; o item 59 mexeu em its.mjs e no carimbo dos SIDs em 05/09/2026, e e o suspeito obvio a olhar primeiro. Importa fechar rapido porque suite vermelha vira ruido de fundo: a proxima sessao que quebrar algo de verdade nao vai perceber.

- [ ] 161. ACHADO do item 98 (06/09/2026): desde o item 81 (commit cb59082) o filtro de montarTela deixa passar o PUSHBUTTON DA DYNPRO (wnd[n]/usr/..., sem okcode) — e com isso os botoes do POPUP (btnSPOP-OPTION1/2) entram em tela.botoes junto com a barra da wnd[0]. O teste do popup em its.test.mjs ficou VERMELHO no HEAD desde entao (esperava so [btn[3]]); no item 98 a expectativa foi ajustada ao comportamento atual, mas a DECISAO nao foi tomada: tela.botoes deve mesmo misturar barra e modal numa lista so? Hoje quem le tela.botoes com popup aberto recebe botao de tres janelas diferentes sem separacao, e o okcode null e a unica pista. Opcoes: (a) manter e documentar; (b) separar (tela.botoes = barra; o da modal so em popup.botoes); (c) marcar cada botao com a janela. Ver tambem a receita-webgui, secao dos botoes do popup.

- [ ] 162. O carimbo do DOM do webgui.mjs (JS_CARIMBO = title | nEl | 300 chars do innerText) tem que virar o ESTRUTURAL, o pareado do carimboDosSids do its.mjs: SIDs + lsdata, SEM a MESSAGEBAR, com a janela ATIVA dentro. Medido em 06/09/2026 (item 100) que o atual inclui a barra de mensagem POR ACIDENTE e pelas duas portas — a mensagem nascer conta como tela mudou (nEl 630->632) e o texto dela trocar nao conta (msgbar no char 317, o carimbo le 300) — e que o mesmo evento conta ou nao conforme o comprimento do cabecalho da tela (depois de um /n a msgbar cai no char 58 e passa a contar). Pior: com o SPOP aberto, cada F12 liga/desliga itens de menu dentro dos 300 chars e o carimbo OSCILA entre dois valores (mudou: true com respondeu: false, 2/2). Em repouso ele e estavel (4 leituras/7,5 s = 1 valor), entao o problema e o gesto, nao deriva. O JS ja existe (JS_DESPEJO_CONTROLES): hashear dentro da pagina e devolver so a string. MEDIR antes de adotar: custo por chamada (o carimbo e lido em laco de 200-250 ms) e estabilidade do lsdata do DOM entre repaints — os SIDs do delta sao byte a byte iguais, mas o DOM pode carregar coisa que o delta nao carrega.

- [ ] 163. clicar/acionar do webgui.mjs medem so o carimbo (esperarMudanca), nao o round-trip: nao devolvem respondeu, e o comandar devolve. Duas consequencias medidas em 06/09/2026 (item 100): (1) o aviso do popup so dispara por mudou === false neles, e a fase D mostrou que o carimbo mente mudou: true justamente no caso que o aviso existe para pegar; (2) esperarMudanca paga o TETO inteiro quando o repaint confirma o que ja estava no DOM — o mesmo defeito que o item 80 tirou do comandar ao trocar para esperarTroca. Avaliar mover clicar para esperarTroca (e com ele navegarMenu e duploClique, que tambem usam esperarMudanca), medindo o que muda no tempo e se algum chamador depende de o mudou ser booleano puro.

- [ ] 164. O abrirSapGui e o UNICO ponto do canal GUI que ainda ignora o pedagio: o laco trata lidas.estado sessao/pede-senha/mensagem, e autorizacao (que lerJanelas devolve ANTES de sessao desde o item 61) nao tem ramo — com o popup de pe ele fica polindo ate esperaMs e morre com "nenhuma sessao logada no ROT em 30000 ms", enquanto a trilha ja escreveu "janelas: autorizacao". Depois do item 101 as outras tres portas (sessoesAbertas, rodarGui, fecharSapGui) abortam em ~3,3 s com o nome certo; esta deveria falhar igual (erro pede-autorizacao com a receita do WarnOnAttach, e decidir se cancela o orfao — a sessao pode estar NASCENDO, entao cancelar aqui talvez mate o logon em curso: medir antes). ACHADO POR LEITURA DE CODIGO em 06/09/2026, sem medicao — reproduzir exige religar WarnOnAttach e chamar abrirSapGui com o pad de pe.

- [ ] 165. O pedágio LIGOU entre 04/09 11:20 e 05/09 de manhã — na MESMA máquina. Medido no item 102 (06/09/2026): em 04/09 11:20 local, três chamadas seguidas do canal GUI contra S4H 250 (fecharSapGui con[1], fecharSapGui con[0], sessoesAbertas) completaram em 9,1 s de relógio, com resposta útil — rápido demais para o caminho com popup (4,8 s por chamada no melhor caso do item 34, 7,2 s com ENTER no item 62). Em 05/09 de manhã o item 34 já pegava o popup a cada GetObject, e o item 62 mediu 12/12 determinístico. O item 60 só olhou a chave em 05/09 21:36 (WarnOnAttach ausente do HKCU) — depois dos dois. PERGUNTA: o que mudou na máquina no intervalo? Pistas baratas e offline: LastWriteTime de HKCUSoftwareSAPSAPGUI FrontSAP Frontend ServerSecurity e da ...Scripting, data de instalação/patch do SAP GUI, e se houve reinício da máquina. De quebra, o item 60 registra WarnOnConnection=0 INTOCADO na mesma chave que diz não ter existido — a chave existia parcialmente, e datar quem a escreveu data a virada.

- [x] 166. abrirSapGui pode pagar um pedágio POR POLL. Lendo gui.mjs (não medido): o laço de espera chama sessoesAbertas() a cada 500 ms enquanto lerJanelas disser estado sessao, e cada chamada é um cscript novo. Com o pedágio ligado, o vigia do item 101 aborta aos ~2 s e CANCELA o órfão — mas o laço chama de novo, e cancelar NEGA a autorização: em 30 s de esperaMs isso é da ordem de 8 popups abertos e negados, e abrirSapGui termina com o erro de prazo em vez de dizer pede-autorizacao. Medir com WarnOnAttach apagado (contar popups pelo vigia) e decidir: desistir na PRIMEIRA negativa, propagando pede-autorizacao.
> DUPLICATA do item 164, que é mais preciso — e corrige este: com o popup de pé, lerJanelas devolve estado autorizacao ANTES de sessao (item 61), então o laço do abrirSapGui NÃO chega a chamar sessoesAbertas por poll; ele fica polindo janelasSapGui até esperaMs e morre com o erro de prazo. Não há pedágio por poll. Trabalhar pelo 164.

- [ ] 167. ACHADO do item 103 (06/09/2026): a lib nao tem sensor do CONTEUDO dos campos do SAP GUI, e o caminho existe. Medido em raw/item103-controle-positivo.txt (dialogo Ligacao SAP GUI - logon): os Edit devolvem GetWindowText="" (o cache nao guarda texto de Edit cross-process) mas respondem WM_GETTEXT — o campo de usuario (id 1004) deu "MVJVELOSO". O Edit de SENHA (id 1005) e o unico que RECUSA: SendMessageTimeout volta 0 com GetLastError=5 (ERROR_ACCESS_DENIED), protecao do Windows. Hoje lerJanelas() so classifica por titulo/Static — nao sabe QUAL usuario/mandante o dialogo de logon esta oferecendo. Vale decidir se a lib precisa disso (ex.: conferir que o pad vai logar no mandante certo antes de mandar a senha) e, se sim, um psCamposDialogo() com WM_GETTEXT + retorno checado (o buffer NUNCA lido ate o NUL — item 103).

- [ ] 168. tran.mjs mistura DUAS convencoes de primeiro parametro no mesmo modulo: deployTransaction/deleteTransaction recebem CONEXAO e readTransaction recebe CFG (ele so faz readTable por SOAP RFC). Quem passa a conexao nao leva erro de tipo — leva 'Failed to parse URL from undefined/sap/bc/soap/rfc' (medido 06/09/2026 no script 5-limpar.mjs da POC do item 105), que nao diz nada sobre o parametro. Decidir: aceitar tambem conexao (ler cfg de dentro dela) ou falhar cedo com mensagem que nomeia o parametro. Varrer o resto da lib pelo mesmo padrao (quem recebe cfg e quem recebe conexao).

- [ ] 169. Retomar a pilha de desfazer numa sessao NOVA: o item 106 mediu que RODA (navegar 117 ms + gesto 92 ms; ~843 ms com a sessao nova) e que o lock cai junto com a sessao que o pegou. O que impede e a API: registrar(rotulo, fn) guarda um CLOSURE que ja capturou a sessao A. Para retomar, a pilha precisa guardar uma RECEITA — como chegar na tela (a transacao/o caminho) + o gesto parametrizado pela sessao —, e o fechar de sessao morta poder oferecer "abro outra e limpo?". Medir tambem o caso de lock de OUTRO usuario, que o 106 nao exerceu.

- [ ] 170. O falso ok=true tambem existe dentro do transacional? O item 106 corrigiu o fechar do its (sonda de vida + guarda na pilha), mas o transacional chama descartar() direto no finally e da a criacao por descartada quando a funcao RESOLVE — e contra sessao morta por tras o gesto resolve sem estourar (POST volta sem-sessao 400 sem lancar). Medir; se confirmar, o transacional precisa da mesma prova de vida, sendo ele PURO e compartilhado com o navegador.

- [ ] 171. ACHADO do item 107 (06/09/2026): o cache do ICM guarda corpo ERRADO, nao so corpo VAZIO — e diagnosticar() do icm.mjs so enxerga o vazio. Medido no s4h 758/250: o cache tem 265 chaves com dsize divergente entre copias da MESMA chave, e um caso claro vivo — /sap/bc/ui5_ui5/sap/zapontamentospx/resources/sap-ui-core.js GZ=B tem tres entradas: uma de 196.392 bytes (a boa) e DUAS de 388 bytes (resposta ruim persistida sob a chave do recurso bom, exp 7 dias). Hoje diagnosticar() so marca envenenado quando bytes===0 com outra variante cheia; um recurso servido com 388 bytes onde o certo tem 196.392 passa por SAUDAVEL e a pagina UI5 quebra igual. Decidir se a lib ganha um sensor de tamanho divergente ENTRE ENCODINGS da mesma URL (identity vs gzip vs br devolvem conteudos equivalentes — se um deles destoa em ordem de grandeza, e veneno) e/ou um limiar minimo por recurso. Cuidado medido: comparar bytes no fio entre encodings NAO serve direto (gzip 213 KB vs identity 775 KB e o normal); o que compara e o corpo DESCOMPRIMIDO. Raw: POC_icm_static_cache/medicoes/raw/ 21-divergentes.txt e 22-caso-vivo.txt.

- [ ] 107. Reproduzir o GATILHO que envenena a entrada do cache do ICM (item 67). Ja descartadas no s4h 758/250 em 05/09/2026, todas partindo do cache invalidado: download gzip abortado no meio (5x), 10 GETs gzip concorrentes, 8 GETs concorrentes misturando gzip/br/identity — o recurso seguiu completo nas tres. Unico sinal proximo: a rodada concorrente criou DUAS entradas GZ=1 com a MESMA chave, ambas com o tamanho certo. Proximas hipoteses: cache cheio/eviction (o cache estava com 9.952 de 10.000 entradas — encher ate o teto e ver se a entrada em uso vira vazia), gravacao interrompida por reinicio/desconexao do work process, e limite de memoria por entrada. Sem o gatilho, a lib so sabe detectar e curar (icm.mjs), nao prevenir.
> adiado: MEDIDO em 06/09/2026 (sap-accelerate/work/POC_icm_static_cache/medicoes/item107-gatilho-hipoteses.md; bruto raw/20-estado.txt, 21-divergentes.txt, 22-caso-vivo.txt, 23-cacheia-erro.txt; adt-tools fa58c4d). O gatilho SEGUE NAO REPRODUZIDO, mas duas das tres hipoteses morreram e a premissa do item estava errada. (1) PREMISSA ERRADA: o "9.952 de 10.000 entradas" nao era a contagem — 9.952 e o TETO do que get_cache_entries() devolve. Deu 9.952 exato nas duas medicoes (05/09 e 06/09) enquanto CENTRIES, o contador real de get_cache_statistics(), ia de 6.336 para 6.204. O cache NUNCA esteve cheio. (2) CACHE CHEIO/EVICTION: DESCARTADA. NOSWAPS=0 num cache com 45 dias de entradas vivas (crea_time 1784762981 -> 1788690514), CENTRIES 6.204/10.000, CUSED 179 MB/419 MB. O ICM nunca evictou nada na janela em que o veneno apareceu, e nao reiniciou. Encher o cache ate o teto (o experimento que o item previa) testaria hipotese ja morta e queimaria a capacidade do cache do sistema — nao foi feito. (3) LIMITE DE MEMORIA POR ENTRADA: DESCARTADA. Maior entrada do cache = 3.069.030 bytes (sap/m/library-preload.js GZ=0), integra — 14x a entrada envenenada de 213 KB. (4) O "unico sinal proximo" do item 67 (duas entradas GZ=1 com a mesma chave) NAO era sinal: o cache tem 1.159 chaves duplicadas, 265 delas com dsize divergente. Duplicata na mesma chave e o normal aqui. (5) A listagem do cache e um retrato podre: trunca em 9.952 e segue mostrando entradas ja invalidadas com o mesmo crea_time (ICM_CACHE_INVALIDATE_ONE deu subrc=0 nas duas coption e as 3 entradas da chave continuaram listadas). A entrada envenenada nao era "invisivel" — a lista mente. So o GET diz a verdade. PROXIMO PASSO — a unica hipotese que sobra e a GRAVACAO INTERROMPIDA DO LADO DO SERVIDOR (os testes do item 67 abortaram do lado do CLIENTE, que e outra coisa). Instrumento necessario: um no ICF com handler ABAP sob controle que devolva 200 com corpo VAZIO uma vez, com headers de cache; depois trocar o handler para o corpo cheio e ver se o ICM persiste e segue servindo o vazio. Se persistir, o gatilho e do BACKEND, nao do cache, e a lib nao tem como prevenir — so detectar e curar, que ja faz. Antes de comecar, medir se a lib sabe criar no ICF (ver POC_icf_estado e POC_sessoes_icf); se nao souber, isso e um item antes deste. Ambiente restaurado: 3 classes de POC apagadas do $TMP (verificado:true nas tres); unica escrita foi invalidar zapontamentospx/resources/sap-ui-core.js, URL que ja respondia 403. Achado novo -> item novo.
> adiado: 2026-09-06 12:32 — a sessão terminou sem fechar nem bloquear o item (estado no arquivo: emAndamento) (sessão 0ba9d17b-2843-4cf7-a820-e2899853c290)

- [ ] 172. clicar({ icone }) só conhece a coleção padrão SAP-icons: getIconInfo tem um parametro collection que a lib não usa, e um nome com barra (sap-icon://businessSuiteInAppSymbols/product) hoje cai em "a IconPool não conhece". Medido no s4h 758/250 em 06/09/2026 (item 109) só a coleção padrão. Medir se a coleção extra existe no S4H, como o nome dela chega ao getIconInfo, e se o caractere continua estável entre idiomas.

- [ ] 173. O Query(DE) da lib responde 1 SEMPRE — ela afirma que QUALQUER diretorio do frontend existe. Medido no item 113 (TEST_FRONT_SERVICES, s4h 758/250, 06/09/2026): na mesma rodada a lib respondeu RetDirectoryCreate=5 (nao criei 	emp_dir_created) e logo depois RetQuery=1 ao DE do mesmo caminho — o ABAP escreveu "Directory Z:	emp	emp_dir_created exists". Incoerente com o resto do frontend-sem-disco (FE=0, listagem count=0, Delete=2). Decidir o modelo: o que o frontend da lib DIZ que tem (a raiz Z: e o Z:	emp que ele mesmo anuncia no GetTempPath?) e responder DE por ele — sem quebrar o caso medido no item 72, onde o DE=1 respondia pela raiz.

- [ ] 174. Ler a LIST classica (WRITE) do WebGUI — hoje a lib le grid (lerGrid) e exporta arquivo (exportarLista), mas nao le a lista que um report escreve com WRITE. A POC do item 113 a reconstruiu a mao: os textos vem nos controles ct="ALT"/"ALI" com id="M0:<n>:::<linha>:<coluna>", e a pagina vira com o okcode P+ (P-, P--, P++ tambem) ate a pagina repetir. Ver POC_webgui_itsdoc/scripts/fase-e-comparar.mjs (funcao linhasDaList). Virar funcao da lib com teste sobre delta real, e decidir o criterio de fim de paginacao.

- [ ] 175. O FORMATO do `exportarPlanilha`/`exportarLista` sai do DEFAULT do usuário, e o default PERSISTE. Medido no item 114 (s4h 758/250, 06/09/2026, `POC_webgui_combo/medicoes/item114-combo.md` § 4): depois de EXPORTAR uma vez em `csv`, toda sessão NOVA abriu o popup Export As já em `csv-LEAN-STANDARD` — e só voltou a `xlsx` depois de exportar em `xlsx` de novo (escolher sem exportar não grava nada). Como `exportarPlanilha` não toca no combo e monta o nome do arquivo virtual com `.xlsx` na mão, ela pode entregar um CSV chamado `.xlsx`, calada. Fazer: `exportarPlanilha(s, { formato })` escolhendo a opção pelo combo (o `preencher`/`chaveDaOpcao` do item 114 já sabe), com default que FIXA o xlsx em vez de herdar o do usuário, e conferir a extensão contra o `DefExt` que o ITSDoc devolve. Vale porque o modo de falha é silencioso e o arquivo parece certo pelo nome.

- [ ] 176. O diálogo "Ordenação" (`SAPLSALV_CUL_CONFIGURATION1500_1`) dirigido DE PROPÓSITO — hoje ele só existe como modo de falha, nas duas vias. Medido no item 115 (s4h 758/250, 06/09/2026) que por HTTP puro ele É `wnd[1]` de verdade e o `popupDaSessao` o enxerga inteiro: dois grids (`cntlCONTAINER1_SORT` = colunas disponíveis, `cntlCONTAINER2_SORT` = critérios), `btnAPP_WL_SING` (incluir critério, F7), `btnAPP_FL_SING` (retirar, F6), `btnDTC_UP`/`DOWN`/`UPPOS1`/`DOWNEND` (prioridade) e Aceitar/Cancelar. Vale porque é a ÚNICA via para ordenação por várias colunas COM direção por critério — o `ordenarGrid` aplica uma direção só para todas — e porque dirigir um grid DENTRO de modal (selecionar linha num `cntl…/shellcont/shell` da `wnd[1]`) não está medido em canal nenhum.

- [ ] 177. O FILTRO da barra do ALV por HTTP puro (`btn[29]`, ícone `s_b_filt`, `CTRL_F5`) — o irmão do sort na mesma barra, medido só pelo NAVEGADOR (item 77). O item 115 mediu que o sort é `action/46 columns=;n;` + `action/3` num POST só, e que o mesmo par serve o `MB_FILTER` no canal navegador; falta seguir o que o botão abre por HTTP (o diálogo do filtro, seus campos e como o valor volta) e se filtrar mexe no `_linha` como ordenar mexe. Vale porque filtro é o gesto que corta 1617 linhas em dez antes de `lerGrid` gastar 12 MB.

- [ ] 178. Ordenar um ALV dentro de CONTAINER pelo HTTP puro — o `botaoDeOrdenacao` (item 115) acha o botão pelo ícone `s_b_srtu`/`s_b_srtd` no `lsdata[11]`, e o item 77 mediu que num ALV com barra própria esse botão vive em `<sid do grid>/tbar/btn&SORT_ASC`, não em `wnd[0]/tbar[1]/btn[n]`. A presunção é que o ícone é o mesmo e a função acha os dois; NÃO foi medido — o laboratório do 115 é ALV fullscreen (`SAPLSLVC_FULLSCREEN`). Laboratório pronto: o `ZJBV_ALV47_EDIT` do item 77. Vale porque é a diferença entre a função valer para um ALV ou para todos.
