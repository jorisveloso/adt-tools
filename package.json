{
  "name": "adt-tools",
  "version": "0.0.1",
  "description": "Monorepo do ecossistema adt — motor e ferramentas para conectar e trabalhar objetos SAP.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.6.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "start:server": "pnpm --filter adt-server start"
  }
}
